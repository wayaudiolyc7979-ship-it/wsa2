# 🗂️ 남은 작업 — 고칠 버그 · 검증할 것 · 빌드 · 문서

> **이 파일 = "안 된 것"만 모은 단일 뷰.** 사용자가 "해결해야될꺼/검증해야될꺼" 하면 이 파일을 연다.
> 전체 히스토리(완료 포함)·상세 코드위치는 `ISSUES.md`. 여기는 **미완 항목만** 추려서 유지.
> 끝나면 `[x]`로 체크하고 한 줄 결과 남기기. 새 버그/할일 생기면 맨 아래 INBOX에.
> 최종 갱신: 2026-06-22

---

## 🐛 A. 고쳐야 할 버그 (코드) — ✅✅ 2026-06-22 전부 해결

- [x] **#3 Extra 소스 스펙트럼 캘리브 오적용** ✅✅ 수정+헤드리스검증(HW불필요). 곡선 calib → 소스 자기 `_spl_source_calib`(`:18154/18169`). `/tmp/calib_test.py`로 extra 오프셋=128(자기), primary 아님 PASS.
- [x] **#1 Farina 스윕 THD/SNR** ✅✅ HW실측(Scarlett, 0.2%/40dB). 커밋 `774079b`.
- [x] **#2 Farina `_fft_lbl` 크래시** ✅ 커밋 `774079b`.
- [x] **Farina 결과 IR 덮어쓰기(freeze)** ✅ 코드·헤드리스(실앱 IR 육안만 C에 남김). 커밋 `774079b`.

> 🔻 아래 2건은 **코드는 끝났고 HW 확인만** 남아 C로 이동:
> - **#67 TF 제너레이터 출력 채널 변경** — 코드수정 완료(`_sig_out_ch_changed`). → **C**
> - **6/22-4 Single TF 엔진 −1.8dB/코히0.82** — 조사·낮음, 블로커 아님(Single 강등후보). → **C**

## ✅ B. 오래된 TF 버그 (2026-06-05 메모) — **2026-06-22 코드 재확인: 3개 전부 해결 확인**

- [x] **Magnitude Y축 스케일 점프** — ✅해결. `fit_y`(`:8247`) 재작성: 코히런스 마스크(coh≥0.3)+5/95 백분위(주석에 2/98 과도범위 회피 명시)+최소12dB폭+3dB격자+−120/60 한계. **더블클릭 등 수동 트리거만**(매프레임 자동 아님)이라 측정 중 점프 없음. primary+extra 합치되 백분위로 묶임 → 96dB 폭주 안 남.
- [x] **동일 장치 멀티채널 Card1 불작동** — ✅해결. `_start`가 동일장치 pair를 `same_dev`/`dev_groups`로 묶어 `MultiChannelAudioThread`/공유엔진 단일스트림 처리(`:11775~11868`). 6/8 M4 실측 통과.
- [x] **Pair별 표시 ON/OFF 버튼** — ✅구현. `is_graph_visible()`(`:8904`)+pair별 `display` 토글이 렌더 전반(`_render_inner`/_on_frame/캡쳐)에 연결.

---

## ✅ C. HW 실측 검증 대기 (외장 인터페이스/M4 필요)

- [ ] **Farina IR freeze 실측** — Sweep 후 IR에 깨끗한 임펄스 뜨는지 육안. *(코드·헤드리스는 검증됨, 실앱 IR만 남음. 커밋 `774079b`)*
- [ ] **외장 마이크 하이/로우 floor** — 외장 입력 **게인↓ 시 floor 같이 내려가는지**(=프리앰프 노이즈 증거). 같은 음원 미드대역 일치 확인. *(코드 대칭 검증 완료, HW만)*
- [ ] **TF 별도 창 STEP 1 — M4 2종** — ①풀스크린/Space에서 검은화면 없이 뜨나 ②TF 별도 창 켠 채 Stereo/Spectrum 동시구동 충돌 없나(공유엔진 회귀). 둘 다 통과=STEP1 종료. *(집검증 완료)*
- [ ] **(보너스) TF 제너레이터 채널버그 재현** — 4in4out 재현 + 로그 시작 장치목록 `in=?` 확인.
- [ ] **#67 TF 제너레이터 출력 채널 변경 미반영** 🟡 *(코드 완료, HW만)* — M4 등 다채널에서 재생 중 Out 3+4 바꿔 즉시반영되는지 실측. 핸들러 `_sig_out_ch_changed`.
- [ ] **6/22-4 Single TF 엔진 −1.8dB/코히0.82 (루프백)** 🟢 *(조사·낮음, 블로커 아님)* — Adaptive=완벽. Single만 −1.8dB·코히0.82. 확정진단=`_on_frame`에 ref/meas 상호상관 lag `_diag` 1줄. Single 강등후보.

---

## 🏗️ D. 빌드 / 배포

- [ ] **DisplayName(.app)** — 빌드된 .app이 메뉴바/Dock에 "SPECTRA"로 뜨는지. ⭐**이때 `docs/img/menubar.png` 촬영**(View/Help 메뉴 편 채 ⌘⇧4→Space) → MANUAL 스크린샷 11/11 완성.
- [ ] **윈도우 빌드 검증** — v태그 push→Actions 산출물 SPECTRA.exe·아이콘·속성 확인.
- [ ] **코드서명 + 노타라이즈** ⏸️보류 — Developer ID·노타리 프로필. 배포 전.
- [ ] **소스코드 보호/난독화** — Nuitka 또는 PyArmor. `_LIC_SECRET` 노출방지.

## 📄 E. 문서

- [ ] **README.md** — 깃허브 대문(소개·기능·설치·빌드·문서링크), 민감정보 주의.
- [ ] **LANDING.html 디테일** — 앱 미리보기·다운로드/문의·가격(배포방향 정해지면).
- [ ] **문서 오타/링크 검토** — MANUAL/LANDING/RELEASE_NOTES 브라우저로 확인.

---

## 🔮 F. v1.7 기능 백로그 (검증 아님 — 참고용)
쇼모드 재개 · THD 측정(스펙트럼탭 커서) · MTW 멀티해상도 · 탭 별도창 분리 · TF 매그+위상 줌/팬 · TF 레벨바 Peak 표시 · RTA 측정카드 선택 · 팝업 둥근모서리 · 스펙트럼 dB축 정책 · 멀티모니터 팝업 드래그 정밀검증
→ 상세는 ISSUES.md G섹션.

---

## 📥 INBOX (새로 떠오른 것 막 적기)

