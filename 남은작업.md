# 🗂️ 남은 작업 — 고칠 버그 · 검증할 것 · 빌드 · 문서

> **이 파일 = "안 된 것"만 모은 단일 뷰.** 사용자가 "해결해야될꺼/검증해야될꺼" 하면 이 파일을 연다.
> 전체 히스토리(완료 포함)·상세 코드위치는 `ISSUES.md`. 여기는 **미완 항목만** 추려서 유지.
> 끝나면 `[x]`로 체크하고 한 줄 결과 남기기. 새 버그/할일 생기면 맨 아래 INBOX에.
> 최종 갱신: 2026-07-04

---

## 🧪 v1.9 실앱 육안 검증 대기 (코드·헤드리스 검증 끝, 집에서 실조작만)

> 이번 세션 커밋들. 로직·렌더·selfcheck 33/33·앱 무예외까지 검증됨 → **실제 조작 육안 확인만** 남음.

- [x] **TF 매그+위상 주파수축 줌/팬** (커밋 `f4e75c6`) — ✅실앱 확인 완료(2026-07-04). 휠/박스드래그/Shift팬/Cmd±/리셋 정상.
- [x] **캡처 임포트 + IR 복원** (커밋 `ff80c1d`) — ✅실앱 확인 완료(2026-07-04).
- [x] **캡처 버벅임 해소** (커밋 `232851b`) — ✅실앱 확인 완료(2026-07-04), 문제 없음.
- [x] **TF Reference·Meas 미터 정렬** (커밋 `73e85b0`) — ✅실앱 확인 완료(2026-07-04).
- [~] **⭐TF 딜레이 시간영역 정렬(Smaart식)** (미커밋) — 딜레이 적용시 magnitude가 올바른 레벨로 **딱 스냅**(기존=위상만 정렬, 매그는 안 고쳐짐). **primary 실측✅ 완료("이거야")**. ⏳**추가카드는 2026-07-06(월) 인터페이스로 HW검증 후 커밋** — 물리 REF 채널+마이크 2개+로 각 카드 딜레이 스냅·IR위치·delay0 동일성 확인. ⚠️맥북 내장(SigGen+모노마이크)선 추가카드 자체가 안 돎(내부ref가 primary만 연결) → 인터페이스 필수. 상세=[[project_v19_delay_time_align]].
- [~] **⭐TF 응답 대칭화 (Smaart식, 커밋됨)** — 크기 곡선 상승·하강 **같은 속도**(비대칭 fast-release 제거), Response 값 짧게(Fast0.5·Quick1·**Normal2**·Smooth4·Stable8초). ⏳**2026-07-06(월) 인터페이스+실스피커로 HW검증** — 레벨 올렸다 내렸다 하며 위/아래 대칭·2초 속도 체감. ⚠️내부SigGen+맥마이크론 제대로 검증 불가(사용자 판단). 값(2초)이 라이브에 맞는지 조정 여지. 상세=[[project_v19_response_symmetry]].
- [~] **⭐TF 라이브 멀티마이크 평균 (크기/공간평균, 커밋 `0693e1e..ad792b2` develop)** — 여러 마이크 실시간 평균곡선(AVG). 툴바 Σ=on/off·바로시작, 카드 Σ아이콘/팝업 체크리스트로 마이크 선택(카드색), 카드목록 아래 AVG 카드(색변경·선택틴트). ⏳**2026-07-07(화)+ 인터페이스+마이크 2개+로 HW검증** — 두 마이크 다른 위치서 크기(공간)평균이 그 구역 평균응답으로 보이는지. selfcheck 39/39·헤드리스단위·offscreen통합·버튼/렌더 검증완료. ⚠️맥내장마이크 1개론 2개 못만들어 실측불가(인터페이스 필수). 라이브 배선=`[DIAG] tf_avg_render` 로그로 판정. 상세=[[project_v19_multimic_live_average]].
  - 🐛✅**"개별 곡선은 뜨는데 AVG만 안 뜸" 버그 수정(2026-08-28, 미커밋→커밋 예정)** — 원인 2건: ①per-card Σ 시그널(`avg_include_toggled`) 미연결 → 카드 Σ 눌러도 마스터 안 켜지고 무반응 ②마스터 Σ 켜도 포함 마이크 0개 기본(집 로그 `tf_avg_render n=0`이 증명). 수정=①시그널을 `_on_card_avg_include`로 연결(카드 Σ만 눌러도 마스터 자동ON+재렌더) ②툴바 Σ ON시 현재 카드 전부 자동포함(`_auto_include_all_mics`, 이미 고른 게 있으면 존중). 진단 강화(`tf_avg_render`에 카드별 `mics=P(iavg,disp,H)|E#(n,disp,iavg)`). AST OK·selfcheck 39/39·헤드리스(MeasCard Σ토글·평균DSP). ⏳**사무실 실측: 인터페이스+마이크2개 Start→툴바 Σ ON→AVG 곡선 뜨는지**(이제 자동포함). 안 뜨면 `WSA2_DEBUG=1`로 `mics=…` 로그 확인.
  - 🎨✅**AVG 색 스와치 추가(2026-08-28, 커밋 예정)** — AVG 카드에 클릭형 원형 색 스와치(좌클릭=색상선택창), 표시체크박스는 중립화(색 중복 제거). 우클릭 색변경도 유지. selfcheck 스와치 회귀가드 추가. ⚠️사용자가 스크린샷 보고 "별로다" → **디자인 재검토 대기**(재오픈 후 스샷 접근가능 위치로 재공유 예정).

### 🎧 오라리제이션 — ✅ 확정·release/v1.0 병합 완료 (2026-07-04, 사용자 "확정")
- [x] 측정 IR로 음악 A/B 청취. 사용자 승인 → `feature/auralization` release/v1.0 fast-forward 병합(커밋 1df5266) + v1.9 릴리스노트(9ec2115). 브랜치 삭제.

---

## 🐛 A. 고쳐야 할 버그 (코드) — ✅✅ 2026-06-22 전부 해결

- [x] **#3 Extra 소스 스펙트럼 캘리브 오적용** ✅✅ 수정+헤드리스검증(HW불필요). 곡선 calib → 소스 자기 `_spl_source_calib`(`:18154/18169`). `/tmp/calib_test.py`로 extra 오프셋=128(자기), primary 아님 PASS.
- [x] **#1 Farina 스윕 THD/SNR** ✅✅ HW실측(Scarlett, 0.2%/40dB). 커밋 `774079b`.
- [x] **#2 Farina `_fft_lbl` 크래시** ✅ 커밋 `774079b`.
- [x] **Farina 결과 IR 덮어쓰기(freeze)** ✅ 코드·헤드리스(실앱 IR 육안만 C에 남김). 커밋 `774079b`.

> 🔻 아래 2건은 **코드는 끝났고 HW 확인만** 남아 C로 이동:
> - **#67 TF 제너레이터 출력 채널 변경** — 코드수정 완료(`_sig_out_ch_changed`). → **C**
> - **6/22-4 Single TF 엔진 −1.8dB/코히0.82** — 조사·낮음, 블로커 아님(Single 강등후보). → **C**

## ✅ B. 오래된 TF 버그 (2026-06-05 메모) — **2026-06-22 코드 재확인: 3개 전부 해결 확인**

- [x] **Magnitude Y축 스케일 점프** — ✅해결. `fit_y`(`:8247`) 재작성: 코히런스 마스크(coh≥0.3)+5/95 백분위(주석에 2/98 과도범위 회피 명시)+최소12dB폭+3dB격자+−120/60 한계. **더블클릭 등 수동 트리거만**(매프레임 자동 아님)이라 측정 중 점프 없음. primary+extra 합치되 백분위로 묶임 → 96dB 폭주 안 남.
- [x] **동일 장치 멀티채널 Card1 불작동** — ✅해결. `_start`가 동일장치 pair를 `same_dev`/`dev_groups`로 묶어 `MultiChannelAudioThread`/공유엔진 단일스트림 처리(`:11775~11868`). 6/8 M4 실측 통과.
- [x] **Pair별 표시 ON/OFF 버튼** — ✅구현. `is_graph_visible()`(`:8904`)+pair별 `display` 토글이 렌더 전반(`_render_inner`/_on_frame/캡쳐)에 연결.

---

## ✅ C. HW 실측 검증 대기 (외장 인터페이스/M4 필요)

- [x] **레벨미터 구간/실시간 실측** 🔊 — ✅**M4 실측 확인(2026-06-26).** green/yellow/red 구간·실시간 반응 정상.
- [~] `[v1.8]` **Farina 스윕 4종 수정 + 스윕→핑크 전환 버그** — ✅**①②④ 완료(2026-07-02, headless), ③·전환버그는 코드상 이미 처리 → HW 재현으로 확인만.**
  - ✅**① 1s vs 2s** — 고정 분석길이(`_FARINA_ANALYSIS_S=0.25s`)로 T무관 동일 그리드·IR길이. test_farina PASS. `[SWEEP_FIXED_RES]`
  - ✅**② 임펄스 지저분** — 하드컷 → 1옥타브 raised-cosine 경계 테이퍼(`_band_taper`, 스윕대역은 평탄유지). IR 꼬리링잉 -38→-47dB. test PASS + before/after PNG. `[SWEEP_BAND_TAPER]`
  - ✅**④ IR 딜레이 정렬** — 이미 완료(c3bb517). ①② 후 IR +0.000ms 유지.
  - ✅**③ 캡쳐 안됨** — ✅✅HW 로그 검증(2026-07-02): `tf_capture_done added=1`(1s "Capture 1", 2s "2s") → total=2. 버그 아님.
  - ✅**스윕→핑크 전환** — ✅✅HW 로그 검증: `sig_start src=pink`→`OutputStream started`→`mtw_live coh_med=1.0`, 예외0. 버그 아님.
  - ✅✅**① + 레벨매칭 버그 HW 검증완료(2026-07-02)**: 1s·2s 5.3dB 오프셋의 진짜원인=레벨매칭이 무너진 HF(4-8k, 1s만)까지 median에 넣어 스케일 5dB 부풀림. 수정=`_wiener_match_scale`(피크 -30dB 이하 제외). 재측정: scale 1s24.5/2s24.3, 중역 125-2k Δ<1dB(오프셋 소멸). 추가로 고정창 pre/tail(좁은대역 truncate)도 수정. test_farina 14/14, selfcheck 32/32. 잔차=HF는 1s가 원래 2s만큼 해상못함(물리). 🔒#2/④ 유지. **미커밋.**
- [x] **외장 마이크 하이/로우 floor** — ✅**M4 실측 확인(2026-06-26).** 게인↓ 시 floor 같이 내려감 정상.
- [x] **TF 별도 창 STEP 1 — M4 2종** — ✅✅**검증완료(2026-06-26).** ①풀스크린 검은화면 없음 ②TF별도창+Spectrum 동시구동 무충돌(`hb run=True tf_cards=1`, xrun/error 0). STEP1 종료.
- [x] **TF 멀티카드 일괄 캡쳐** — ✅✅**검증완료(2026-06-26).** 카드1·2 라이브 중 Space → `tf_capture_done added=2`, 화면 "Capture 1"+"Capture 1·Card2". (단, 멀티카드 렌더 버그 수정 후라야 카드2 곡선·캡쳐가 떴음 → [[project_bug_mtw_multicard_render]])
- [ ] **(보너스) TF 제너레이터 채널버그 재현** — 4in4out 재현 + 로그 시작 장치목록 `in=?` 확인. *(선택, 블로커 아님)*
- [x] **#67 TF 제너레이터 출력 채널 변경** — ✅✅**해결+검증(2026-06-26).** 무끊김 채널변경 기능으로 대체 해결(standalone 콜백 가변참조). 재생 중 Out 채널 변경 즉시반영·끊김0(`sig_out_ch seamless=True`).
- [ ] **6/22-4 Single TF 엔진 −1.8dB/코히0.82 (루프백)** 🟢 *(조사·낮음, 블로커 아님)* — Adaptive=완벽. Single만 −1.8dB·코히0.82. 확정진단=`_on_frame`에 ref/meas 상호상관 lag `_diag` 1줄. Single 강등후보.
  - 🗓️ **사용자 결정 갱신(2026-06-26): Single 엔진 삭제는 v2.0에서 한다(확정).** MTW(Adaptive)는 6/26 M4 멀티카드·측정 내내 통과 확인. ⚠️렌더 심장부라 별도 집중 작업. 삭제 규모=`_tf_engine_mtw` 분기 6곳(L12320/12335/12354/12744/13678)을 MTW단독으로 + `eng_cb`콤보·`_engine_changed`(L13535)·Single/Adaptive i18n(L645/646/452)·상태 'engine'키 제거 + selfcheck. → **v2.0 로드맵 #3(MTW 기본승격)에 포함.**

---

## 🏗️ D. 빌드 / 배포

- [x] ✅✅ **v2.0 분해 후 빌드 검증 (Silicon, 2026-08-29)** — ⭐**단일파일→spectra/ 23모듈 분해가 PyInstaller 패키징을 깨뜨리지 않는지 = 2.0 최대 리스크 확인 완료.** `/usr/bin/python3 -m PyInstaller WSA2.spec --clean --noconfirm`(기본 Homebrew py3.14는 PyQt5 없음 → `/usr/bin/python3`(3.9) 사용) → `dist/SPECTRA.app`(149M) 빌드 성공(exit 0). **핵심 검증=레포와 완전 분리된 `/tmp`에서 실행**(레포 spectra/ 소스 없는 곳) → 번들앱 정상 부팅: ModuleNotFound 0·크래시0·하트비트17·"실행형태: 번들앱(설치본)"·TF엔진(MTW)/라이선스 정상. → **spectra/가 진짜 .app PYZ에 묶임 확정**(정적 from-import라 자동수집, .spec 수정 불필요). ⏳남은 빌드: **Intel**(x86_64 venv 재셋업, [[project_intel_build_toolchain]])·**Windows**(태그 push→Actions)는 실제 릴리스 때. 지금 dist/SPECTRA.app은 버전 1.9(2.0 bump는 릴리스 때).
- [x] ✅✅ **v1.8 3종 빌드 완료 (2026-07-02)** — 태그 `v1.8` push, 커밋 push 완료. 미서명(보류). 산출물:
  - macOS Silicon: `dist/SPECTRA_AppleSilicon.dmg` (52M, arm64, v1.8, CFBundleName=SPECTRA 확인)
  - macOS Intel: `dist/SPECTRA_Intel.dmg` (59M, x86_64 확인, v1.8) — 독립형 x86_64 python + arm64 래퍼 재셋업(메모리 [[project_intel_build_toolchain]])
  - Windows: `dist/windows/SPECTRA-Windows-x64/SPECTRA.exe` (85M, PE32+ x86-64, v1.8) — GitHub Actions run 28561410468 success
  - 기존 v1.7 빌드는 `rm -rf dist build`로 삭제 후 재빌드.
  - ⏳ **남은 배포 전 확인**: ①빌드된 .app 메뉴바/Dock "SPECTRA" 육안(CFBundleName=SPECTRA라 OK 예상)+`menubar.png` 촬영 ②Windows exe 실기 실행 ③서명·노타라이즈(보류) ④RELEASE_NOTES.html 웹용 동기화(앱은 .md 직접렌더라 무관).

- [ ] ⭐ **~~6/28 빌드 시작 시: 기존 빌드 전부 삭제 + 메인앱 파일명 SPECTRA로 변경~~ (✅v1.8빌드서 반영됨)** (사용자 지시 2026-06-25)
  - **① 기존 빌드 삭제:** `rm -rf dist build` (옛 WSA2 산출물 잔재 제거)
  - **② macOS 빌드 파일명 WSA2→SPECTRA** (Windows는 이미 `SPECTRA.exe`로 완성, 손댈 것 없음. 라이선스툴도 이미 SPECTRA 완료):
    - `WSA2.spec`: `name='WSA2'`(L53,75) → `'SPECTRA'`, `name='WSA2.app'`(L80) → `'SPECTRA.app'` (CFBundleName은 이미 SPECTRA)
    - `build_silicon.sh`: `dist/WSA2.app`(L29,33,45) → `dist/SPECTRA.app`, `dist/WSA2_AppleSilicon.dmg`(L33,36,44) → `dist/SPECTRA_AppleSilicon.dmg`
    - `build_intel.sh` (heredoc가 WSA2_Intel.spec 생성): `name='WSA2'`(L72)→`'SPECTRA'`, `name='WSA2_Intel'`(L78)→`'SPECTRA_Intel'`, `name='WSA2_Intel.app'`(L81)→`'SPECTRA_Intel.app'`; `dist/WSA2_Intel.app`(L102,105,117)→`dist/SPECTRA_Intel.app`, `dist/WSA2_Intel.dmg`(L105,108,116)→`dist/SPECTRA_Intel.dmg`
    - ⚠️ **`.spec 파일명(WSA2.spec)은 그대로 둘 것**(bump_version.sh L39·build 스크립트가 `PyInstaller WSA2.spec`로 참조). 바꾸는 건 produced된 .app/.dmg 출력명뿐.
    - make_dmg.sh는 입력경로를 인자로 받고 APPNAME='SPECTRA.app' 이미 맞음 → 예시주석만 정리(선택).
  - **③ 빌드 후 확인:** dist에 `SPECTRA.app`·`SPECTRA_Intel.app`·`SPECTRA_*.dmg`로 나오는지, 메뉴바/Finder/DMG 전부 SPECTRA인지.
- [ ] **DisplayName(.app)** — 빌드된 .app이 메뉴바/Dock에 "SPECTRA"로 뜨는지. 🔧**코드수정됨**: `CFBundleName`을 WSA2→SPECTRA로 변경(WSA2.spec·build_intel.sh heredoc·WSA2_Intel.spec). 메뉴바는 CFBundleName을 따르므로 이제 SPECTRA로 떠야 함 → **다음 빌드에서 실측 확인**. ⭐이때 `docs/img/menubar.png` 촬영(View/Help 메뉴 편 채 ⌘⇧4→Space) → MANUAL 스크린샷 11/11 완성.
- [ ] **윈도우 빌드 검증** — v태그 push→Actions 산출물 SPECTRA.exe·아이콘·속성 확인.
- [ ] **코드서명 + 노타라이즈** ⏸️보류 — Developer ID·노타리 프로필. 배포 전.
- [→v2.0] **소스코드 보호/난독화** — 아래 G(v2.0)로 이동.

## 🚀 G. v2.0 로드맵 (v1.7 빌드 후 착수) — ★청사진 전문 = 레포 `2.0_MODULE_PLAN.md`
> "기능 추가"가 아니라 **단일 파일(~19,800줄) 구조 재정비** 버전. 한 줄: **분해 → 보호 → 측정엔진 승격.** 상세=[[project_v2_roadmap]] / `2.0_MODULE_PLAN.md`.

- [~] **0순위(전제) 테스트 인프라** — `dsp/` 함수부터 pytest 골든값 고정. 분해 안전망. **지금 해도 손해 0**(즉시 회귀방지). ✅**핵심 DSP 골든 착수완료(2026-07-04, `tests/test_dsp.py` 15개)**: power_spectrum_db(ENBW·진폭스케일·floor)·_octave_bands·_band_taper·_wiener_match_scale·_ir_from_mag_phase(지연보존)·_hilbert_env·farina_analyze(단위통과)·freq_to_x/x_to_freq(줌 f_lo 왕복)·db_to_y·ms_to_m·fmt_delay·_fz_clamp. pytest 전체 31 passed(farina/mtw/loudness 포함). **남은 확장(v2.0 분해 때)**: 스무딩·K가중 세부·옥타브 마스크 등 나머지 dsp 커버 확대.
- [ ] **1순위 단일 파일 분해** — `wayaudo2.py` → `spectra/`(core/dsp/audio/ui/tabs/spl/popout) 패키지. 함정: 가변전역(`_theme`)을 `T()` 접근자로 통일 + 거대클래스 2개(MainWindow/TF)는 파일만 옮기고 내부 안 쪼갬 + selfcheck/.spec/bump 단일파일 하드코딩 같이 수정.
- [ ] **2순위 소스 보호(Nuitka)** — 분해 후. `core/license.py` 강하게. 결론: 라이선스는 이미 견고(Ed25519), 실비밀은 레거시 HMAC `_LIC_SECRET`뿐→은퇴 선행 권장. 상세=[[project_todo_code_protection]].
- [ ] **3순위 Adaptive(MTW) 기본 승격 + Single 엔진 삭제** — 옵션→기본(이미 기본 Adaptive). 6/28 MTW HW 통과 시 Single 완전 제거(분기 6곳·콤보·핸들러·i18n). HW A/B+CPU 검증 심장부작업.
- [ ] **4순위 프로젝트/세션 저장 포맷** — 캡쳐+캘리브+카드구성+뷰를 "측정 프로젝트" 하나로.

## 📄 E. 문서 — ✅ 2026-06-25 본문 작업 완료(집), 스크린샷·브라우저육안만 6/28

- [x] **README.md** — ✅버전 배지 1.5→1.7, v1.7 기능(적응형TF·정밀스윕·프리셋·다국어·SPL알람·Sine) 반영. 링크/이미지 정상.
- [x] **RELEASE_NOTES.html 동기화** — ✅v1.7(최신·now배지)+v1.6.1 추가, v1.5 최신배지 해제. MD와 정합(브랜드 압축본).
- [x] **LANDING.html 디테일** — ✅하이라이트 칩 v1.7 갱신(적응형엔진·정밀스윕·프리셋·한/영·SPL알람·Win). ⏸**다운로드/문의/가격 링크(`href="#"`)는 배포방향 미정이라 보류**.
- [x] **문서 오타/링크 검토** — ✅정적 검토 끝: HTML 3종 well-formed, 내부앵커 15/15 OK, 이미지 참조 OK(단 `menubar.png`만 미촬영=D). MANUAL v1.7 본문 동기화(아래) + 브라우저 육안만 6/28.
  - MANUAL 본문 v1.7 반영: 스펙트럼 툴바 표에서 **제거된 Avg 행 삭제+번호 재정렬(1~8)**, Stereo 히어로 **AVG/LIVE 토글→좌우 2분할 동시표기**(COMPLIANCE=AVG기준), **레이더 타겟 중심 눈금** 설명 추가.
- [x] **MANUAL.html 본문 v1.9 반영 완료(2026-07-06)** — 신기능 대거 추가: TF **멀티마이크 평균(Σ)**·**오라리제이션(🎧)**·**줌/팬**·**캡쳐 Import/평균**·소스별 Start·색변경·채널격자·dB수동고정·응답대칭·N2 툴바/탭 리디자인 설명. 툴바 범례(Spectrum/TF) v1.9로 재작성, 단축키·트랜스포트 갱신. HTML well-formed(태그 균형 OK). ⏳남은=스크린샷만.
- [ ] ⚠️ **스크린샷 재촬영 (v1.9 — 3탭 전부 N2로 바뀜)** — `docs/img/` 재촬영 필요: **spectrum.png**(N2 툴바·소스카드 Σ) · **tf.png**(N2 툴바·Σ평균·AVG카드) · **stereo.png**(N2 툴바) · **menubar.png**(미촬영). 재촬영 후 MANUAL 콜아웃 위치(%) 보정. (본문 텍스트는 완료.)

---

## 🔮 F. v1.7 기능 백로그 (검증 아님 — 참고용)
쇼모드 재개 · THD 측정(스펙트럼탭 커서) · MTW 멀티해상도 · 탭 별도창 분리 · TF 매그+위상 줌/팬 · TF 레벨바 Peak 표시 · RTA 측정카드 선택 · 팝업 둥근모서리 · 스펙트럼 dB축 정책 · 멀티모니터 팝업 드래그 정밀검증
→ 상세는 ISSUES.md G섹션.

---

## 📥 INBOX (새로 떠오른 것 막 적기)

### 🪟 2026-06-30 Windows 포팅 — 사용자 곧 실측할 것 (상세=[[project_session_2026_06_30_windows_port]])
- [ ] **실제 윈도우 노트북 실측**(Parallels 아닌 네이티브): ①3탭 동시 버벅임이 네이티브선 괜찮은지(VM 한계였는지 확인) ②콘솔 깜빡임 진짜 사라졌는지 ③전반 동작. → 노트북서도 3탭 버벅이면 Spectrum FFT도 워커로(다음 수술).
- [ ] **라우드니스 레이더 Windows 렌더 차이**: 중앙 히스토리·외곽 세그먼트가 줄무늬/틈으로 보임(맥은 매끈). `LoudnessRadarCanvas`(`:15340`). Qt 래스터 vs macOS 네이티브 + 갱신굶주림 복합. perf 풀린 뒤 재평가(세그먼트 오버랩/단일패스 윈도우 보정 후보).
- ✅**Windows 7종 수정 완료(커밋 release/v1.0)**: USB(WASAPI)·TF SR충돌·폰트Candara·다크타이틀바·TF워커병렬·3탭게이팅·콘솔깜빡임. Parallels+Scarlett 실측 통과(perf 제외). exe=`~/Desktop/SPECTRA_1.8beta_win.exe`.


- [x] 🪟✅✅**[버그·Windows] USB 인터페이스 안 잡힘/안 열림 — 수정+Parallels 실측 검증완료(2026-06-30, Scarlett 2i2)** — 빌드 SPECTRA.exe(run 28412370801)를 Parallels Windows11에서 실행, 로그 입증: Scarlett가 MME/DirectSound/WASAPI/WDM-KS **4벌 중복열거**(근본원인 확인) → `[DIAG] audio_hostapi tab=spectrum/tf wasapi_idx=2`로 **WASAPI 자동선택**, `eng_add dev=12`(MME 1 아니라 WASAPI 12로 라우팅)+`InputStream opened dev=12`+`hb chunks 63→2749 연속증가`로 스트림 오픈·연속측정 정상. 커밋 `991e6ea` push 완료. (VM 청크율 ~38/s=가상오디오 오버헤드, 무관.) 2026-06-30 정적검사+수정. **근본원인:** 코드 어디서도 PortAudio **호스트 API를 지정 안 함** → Windows에선 같은 USB 인터페이스가 MME/DirectSound/WASAPI/WDM-KS로 **중복 열거**되고 기본값이 MME(SR경직·다채널제한·31자 이름잘림)라 안 열림. **수정(macOS 무영향, 비-Windows 전부 None=종전동일):** ①신규 헬퍼 `_win_preferred_hostapi()`/`_dev_hostapi_ok()`/`_win_extra_settings()`(`wayaudo2.py` _diag 직후) ②장치열거 3곳(TF `_load_devices`·Spectrum 목록·`_start` 이름재조회)을 **WASAPI로 필터**(WASAPI 부재 시 None 폴백=전체표시) ③스트림 오픈 8곳에 `extra_settings=_win_extra_settings()`(WASAPI일 때만 `WasapiSettings(auto_convert=True)` → SR 불일치 자동변환, -9997 방지). ASIO는 장치당 단일스트림이라 공유엔진과 충돌→의도적 제외. **검증:** 헤드리스 단위테스트(맥 무변화+윈도우 모의 중복4→1+폴백) PASS, selfcheck 32/32. **Windows 로그 증거 = `[DIAG] audio_hostapi tab=… wasapi_idx=…`**. ⚠️실제 Windows+USB 인터페이스 실측 필요(맥엔 못 함). 미커밋.

- [x] ⚡✅✅**[성능] TF+Spectrum 동시 마이크 사용 시 버벅임 — 워커 스레드 병렬화로 해결(2026-06-30, 맥+Windows 실측)** — TF MTW 무거운 FFT를 단일 워커(ThreadPoolExecutor)로 분리 → Spectrum FFT(GUI)와 병렬(numpy GIL 해제). 맥: chunks ~47/s 안정·예외0. Windows(Parallels): 사용자 "지금 너무 좋아". 동기 폴백 유지(회귀0)·selfcheck 32/32. 커밋 release/v1.0. 상세 [[project_bug_spectrum_tf_contention]]. (Single 엔진은 아직 GUI=다음 후보, Adaptive 기본이라 무관)

- [ ] ⚡**[v1.8+ 성능·HW·구버전] TF 카드 켜면 스펙트럼 느려짐(간헐 멈춤처럼 보임)** — 2026-06-28 진단. 스펙트럼·TF 카드가 **같은 메인 스레드**에서 처리 → TF MTW 계산이 무거워 스펙트럼이 굶주림. 엔진은 청크 ~62/s 내보내는데(bs=512) 스펙트럼은 **~23/s만 처리**(TF 켜진 동안 약 1/3 속도). 부하 튀면 거의 멈춘 듯. **하드버그(완전멈춤) 아님=성능 경합.** eng_cb_dead=0(워치독 무관). **수정방향(나중)**: TF 처리 경량화 또는 스펙트럼 청크 처리/렌더를 경합서 분리(QueuedConnection 백로그 회피). 측정 심장부라 신중히. **HW테스트**: TF카드 Start+Spectrum Start 동시, 진단 `[DIAG] hb chunks=N`(증가속도)·`eng_add`로 판정. (조사용 진단 `eng_add`/`chunks` 카운터 코드에 남겨둠.)
  - **케이스2(더 심함): 스윕 측정 중 스펙트럼 아예 안 됨** — 스윕은 별도 `TFDuplexThread`(M4 in+out 풀듀플렉스)라 스펙트럼 엔진 입력과 **같은 M4에 2스트림 동시** = CoreAudio 경합으로 스펙트럼 정지. 수정=스윕도 입력을 공유 엔진서 받게(듀플렉스 격리는 유지). 상세=[[project_bug_spectrum_tf_contention]].

- 6/22 세션 완료(커밋 `8ead833`,`84283ae`): ①프리셋에 카드개수 저장(스펙트럼소스+TF카드, 덮어쓰기는 원래지원) ②추가소스 응답속도 불일치 수정(avg_buf 동기화 + OctaveCanvas.set_channel_oct에 primary와 동일 alpha ballistic — 옥타브뷰 전용 버그였음) ③스펙트럼 멀티소스 일괄캡쳐 ④TF Space=이름입력. 전부 selfcheck+실측 검증됨.
- **6/24 세션 완료(소프트웨어, 전부 라이브 검증됨)** — 커밋 `e9530ef`(빌드 pkg_resources 픽스) `7598daf`(스펙트럼 빠른상승·느린하강 ballistic 단일 엔벌로프+Response 재보정 Fastest~0.4s…Slowest~2.5s, SPEC_ATTACK/SPEC_FALL_MS = `[찾기: SPEC_TUNING]`) `61f5842`(죽은 Avg 컨트롤 제거+릴노트) `2790f86`(팝아웃 타이틀바 다크+브랜드헤더 가운데+버튼스타일) `4fcda02`(팝아웃 툴바 접기 토글+헤더 더블클릭 최대화+TF 라이트모드 타이틀바/Reference In 콤보 검정 수정). **HW 불필요 — 검증 끝.** → v1.7 남은 건 위 **C(HW 실측)·D(빌드)·E(문서)** 뿐.

