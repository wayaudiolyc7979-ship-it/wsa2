# WSA2 — 버그 / 요청 / 질문 트래커

> 사용법: 떠오를 때마다 아래 "받은 항목"에 날짜별로 한 줄씩 막 적어두면 됨.
> 형식 안 맞아도 OK. 가능하면 `[버그]/[요청]/[질문]` 태그 + (버그면) **재현방법** + **기대동작** 한 줄.
> Claude가 받아서 분류·우선순위·재현질문·수정까지 처리하고, 끝나면 "처리 완료"로 옮김.

---

## 📥 받은 항목 (처리 대기)

### 2026-06-13 (UI 폴리시 중 나온 것)
- `[요청/나중에]` TF 탭 측정카드(_MeasCard) **레이아웃 정리** — 사용자가 "카드쪽 이상해" 지적, 우측패널 먼저 하기로 미룸. 초록 Start 버튼은 이미 블루로 수정함. 남은 건 카드 내부 정렬/간격/요소 배치(M레벨바·Meas/Ch·Delay/Auto) 다듬기. 우선순위 낮음.

### 2026-06-13
- `[요청/나중에]` TF 탭에 옥타브/FFT(RTA) 뷰 추가 — Spectrum과 TF "통합" (Smaart식)
  - 사용자 의도: **Spectrum 탭은 그대로 두고**, TF 탭의 위상/매그니튜드 창에서 **옥타브 또는 FFT를 선택**해 함께 보기
  - 근거: TF 측정 입력이 이미 공유 오디오 엔진(`AudioEngine`, 장치당 단일 스트림)으로 들어옴 → 그 신호로 옥타브/FFT도 계산해 같은 창에 토글 표시 가능. Spectrum 코어 안 건드림
  - 참고: Loudness는 독립 엔진이어도 무방(사용자 확인)
  - 구현 시 고려: TF 측정 채널 신호 → octave/FFT 변환부 재활용(Spectrum의 `oct_cvs`/`fft_cvs` 로직), TF 뷰에 RTA 트레이스/뷰모드 토글, 같은 장치 SR 1개 제약
  - 우선순위: **낮음(나중에)**


---

## 🔧 처리 중

### 2026-06-12 → 구현 2026-06-13
- `[UX]` 마이크 캘리브레이션 — 다채널 캘리브 흐름이 헷갈림 → **B안 구현 완료, 실측 검증 대기**
  - 변경: `CalibDialog`를 채널 목록 테이블 형태로 재구성 (wayaudo2.py:2473~)
    - 다채널(>1)일 때 상단에 `Ch N / 현재 오프셋 / [선택]` 행 목록(스크롤). 미설정 채널은 `—`, 설정된 채널은 `+xx.x dB`(초록)
    - 행 [선택] → 부모 `in_ch_cb` 전환(스트림 재시작)되어 그 채널이 측정 대상이 됨. 측정박스 제목/오프셋 스핀이 선택 채널로 따라감
    - 측정/자동계산/직접입력 → 해당 채널 오프셋으로 기록. [OK] 시 변경된 모든 채널을 `장치:채널`로 일괄 저장
    - 취소 시 다이얼로그 도중 바뀐 채널을 원래 채널로 복원
  - 단채널 장치는 기존과 동일(테이블 숨김)
  - ⚠️ 검증 필요: 멀티채널 인터페이스(M4 등)에서 ①행 선택 시 스트림이 해당 채널로 전환되는지 ②측정값이 그 채널 기준인지 ③전환 직후 스무딩 안정화 시간 충분한지 ④일괄 저장/취소 복원 동작


---

## ✅ 처리 완료

(고치고 검증 끝난 항목 — 릴리스 노트로 재활용)
