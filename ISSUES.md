# WSA2 / SPECTRA — 통합 할일 트래커 (★마스터, 한 곳 관리)

> 🚧 **활성 개발 버전 = v1.6** (2026-06-14~). 지금부터 들어오는 버그·새 기능은 전부 v1.6. v1.5는 태그로 보존된 체크포인트(`git checkout v1.5`). 소스 `_APP_VERSION='1.6'`로 bump 완료(미커밋).
> **모든 할일·버그·다음버전·문서·배포가 여기 한 곳에.** 떠오르면 맨 아래 `INBOX`에 막 적어두면 Claude가 분류해 위로 올림.
> 체크박스 `[ ]→[x]` 로 진행 관리. 각 항목 끝 `(상세: slug)` = Claude 메모리의 상세 문서(코드 위치/구현방법). 다음 세션 시작 시 이 파일 먼저 확인.
> 형식: `[버그]/[요청]/[검증]/[빌드]` + 한 줄 설명. 우선순위 = 위에서 아래로.

---

## 🐛 2026-08-29 수정 (v2.0, 미커밋·HW 실측 대기)
- [ ] **[버그] 측정 마이크 장시간(1시간+) 뒤 조용히 멈춤** — 캡처 스레드 워치독이 "콜백 2초+ 정지"를 무조건 **물리적 제거(device removed)로 단정**하고 스레드를 종료 → 장치는 그대로인데(절전/App Nap/일시 글리치) `_on_tf_disconnect`가 "가짜 끊김(장치 개수 유지)"으로 무시 → **아무도 재시작 안 함 → 마이크 정지.** 시작-죽음(startup dead)엔 재오픈 복구가 있었으나 **running-stall엔 없던 미처리 케이스.** **2겹 수정:** ①`spectra/audio/watchdog.py` 신설 — 측정 중 `caffeinate -i`로 **idle 시스템 절전 차단**(트리거 원천 제거, refcount 다중스레드 안전). ②running-stall 시 disconnected 대신 **같은 장치 재오픈**(콜백 재개 시 예산 리셋, 연속 `MAX_DEAD_REOPENS=6`회 실패해야 진짜 제거 판정). 3경로(`MultiChannelAudioThread`·`TFSyncThread`·`TFDuplexThread`) + 진단 로그 `eng_cb_stall`/`tf_sync_cb_stall`/`tf_duplex_cb_stall`/`no_sleep_begin`. AST OK·selfcheck 44/44·caffeinate 실기동 검증. ⏳**HW 1시간+ 실측 대기** — 재발 시 로그 `[DIAG]`로 어느 워치독·절전 여부 확증. *(상세: project_bug_v2_mic_stall_long_run)*

## 🐛 2026-07-16 수정 (v1.9, 커밋 완료)
- [x] **[버그] Auto 딜레이 파인더 값 튐(v1.9 회귀)** — 딜레이 시간정렬(`_align_pair`) 도입 후, 누적 스펙트럼이 이미 현재 딜레이로 정렬돼 파인더가 "잔여"만 보고 그걸 **덮어써** 참값 못 감(로그 `2.65↔0.46 ms` 핑퐁). **수정: `d_ms = 현재딜레이 + 잔여`** (3경로: `_find_delay_for_pair`·`_find_delay_compute`·`AllDelayFinderDialog`). M4 물리ref **실측 확정**(4.47ms 고정·잔여0·임펄스 명중). verify_finder.py로 핑퐁 재현→수렴 검증, selfcheck 39/39. `[DELAY_FIND_ABS]` *(상세: project_v19_delay_time_align)*
- [x] **[버그] 내부(SigGen)·다른장치 ref 측정 위상/임펄스 어긋남** — 딜레이 정렬이 `_on_frame`(same-device duplex/sync)에만 걸리고 분리콜백 경로(Internal SigGen·diff-device primary=`_on_ref`+`_on_meas`)엔 빠져, `_render_primary_H`가 정수딜레이 제거 가정만 하다 위상 과다랩핑·IR 임펄스 2×딜레이. **수정: `_primary_upstream_aligned` 플래그로 미정렬 경로만 렌더 직전 정렬**(MTW=`_align_pair`, Single=`X·exp(-jωD)`), duplex 이중정렬 가드. 헤드리스 검증(verify_delay_align.py). ⏳실측=원래 스샷(맥스피커+맥마이크=diff-device)에서만 발동, M4는 duplex라 미발동. *(상세: project_v19_delay_time_align)*
- 진단(유지): 콜드오픈 콜백 미시작 워치독 로그 `sig_cb_dead`/`tf_duplex_cb_dead`/`tf_duplex_first_cb` — "인터페이스 출력 간헐 무음"(이번엔 사용자 라우팅 실수로 판명, 근본 미재현) 대비 저비용 로그.

---

## 🎧 A. 내일 바로 — 하드웨어 실측 (회사 M4/외장 인터페이스 필요)

- [x] **캘리브 멀티채널 검증** — ✅✅**실측 검증완료(2026-06-16, M4 4채널)**. 채널목록 4행·채널전환·채널별 측정/오프셋 정상. **검증 중 버그 2건 발견·수정:** ①**오프셋 150 클램프** — `offset_spin.setRange(-30,150)`이 측정마이크 오프셋(보통 120~170dB)을 150에 잘라 전 채널 +150으로 표시 → `(-30,200)`로 상향(`wayaudo2.py:3150`). ②**단독 측정 시 분석창/라이브카드 같이 켜짐** — `_open_calib`이 정지상태에서 `_start()`(메인경로)를 켜 분석창·카드가 살아남 → **캘리브 전용 경량 스트림**(`_calib_start_measure/_calib_on_chunk/_calib_stop_measure`)으로 레벨만 측정, 캔버스/카드/트랜스포트 미터치. 분석 Running 중이면 기존처럼 in_ch_cb 전환(라이브 곡선도 전환). **TF 탭엔 캘리브 미적용 확인(정상)** — TF 매그니튜드=meas÷ref 상대이득(dB)이라 절대SPL 약분, 캘리브는 Spectrum/SPL미터 전용. 미빌드. *(상세: project_calib_channel_table_verify)*
- [x] **SPL Meter 실시간** — ✅기능 검증완료(2026-06-15, 내장마이크). 칸별 값 실시간 갱신·Max홀드·리셋·Fast/Slow·Clock 정상. Smaart SPL미터 A/B: **C Slow 0.9dB·1kHz톤 0.4dB 일치**. ⚠️A-가중만 별도 미결(아래 버그 항목). *(상세: project_spl_meter_layout)*
- [x] **[검토] SPL 미터 무음 시 카드 멈춤** — ✅**실측 확인: 문제 없음(2026-06-16, M4)**. 무음에서도 Z/FS Peak가 floor 정상 표시·라이브 갱신. 게이트(`dba_db>-100`)가 실사용 조건에선 안 거슬림 → 수정 불필요(현행 유지). *(코드리뷰 #2 종결)*
- [x] **[검증] 스펙트럼 절대레벨 ENBW 정규화 (Smaart A/B)** — ✅✅**실측 검증완료(2026-06-15, 내장마이크 A/B)**. `power_spectrum_db()`(`wayaudo2.py:377`, Hanning 윈도우파워 Σw²+단측×2 → 밴드 파워합=실제 RMS/Parseval). **Smaart 나란히(같은 내장마이크, 둘 다 Cal Offset 128, 1/12, 16FIFO): ①형태 — 핑크노이즈·1kHz톤 컨투어 일치 ②절대값 — SPECTRA dBA/dBC 86.0 vs Smaart SPL A/C Slow 85.6 = 0.4dB 일치(측정불확실성 내). 캘리브 조정 불필요.** RTA 커서는 Smaart 밴드센터가 ISO고정(1.03kHz)이라 톤피크 빗나가 부정확 → 안정된 SPL미터로 비교가 결정적. ENBW 정규화 Smaart 대비 실측 정합 증명.
- [x] **인풋 카드 레벨미터 외장 검증** — ✅✅**실측 검증완료(2026-06-16, M4)**. 외장 인터페이스에서도 빌트인과 동일하게 채움/색/피크 정상 표시. `_MiniMeterBar` DB_MIN −84dBFS 그대로 OK, 재조정 불필요.
- [x] **TF 카드 레벨바 통일** — ✅✅**실측 검증완료(2026-06-17, Scarlett 2i2)**. TF탭 Start→ Reference(R)·Meas(M) 카드바 ①채움 이동 ②둥근 트랙+둥근 채움 룩 통일 ③peak tick ④초록(≤−9)→노랑(−9~−3)→빨강(>−3) 색전환 전부 정상(`_HorizBarVU`=`_MiniMeterBar`룩). **Smaart 비교 질의 결론:** TF 바=순수 RMS dBFS(`20log10(RMS)`, 캘리브무관). 레벨 값은 Smaart RMS와 일치해야 정상(±0.x dB) — 단 Smaart 기본은 Peak 표시라 RMS모드로 맞춰야 함, 풀스케일 사인=−3dBFS RMS(보정안함). 바 범위(−60~0)·색임계(−9/−3)는 SPECTRA 고유 디자인이라 Smaart와 픽셀일치 불필요. *(상세: project_v1_2_verify_checklist)*
- [x] **트랜스포트 블루/레드 틴트** — ✅✅**실측 검증완료(2026-06-17)**. 전 탭(Spectrum/TF/Stereo) Start=로고블루+▶play아이콘 / Stop=레드+■stop아이콘, 토글 시 색·아이콘 동시 전환, 초록 잔재 전무. **TF Meas 카드 개별 Start 포함** 확인. 통일 함수 `_apply_txn` (`wayaudo2.py:570`, green 분기 없음).
- [x] **라이선스 활성화 UI 육안** — ✅✅검증완료(2026-06-15). 새 키 발급→앱 LicenseDialog 활성화 OK.
- [x] **레거시 HMAC 키 활성화 확인** — ✅✅검증완료(2026-06-15). 듀얼검증 OK.
- [x] **TF 별도 창(팝아웃) — STEP 1 실측** — ✅✅**M4 실측 검증완료(2026-06-26).** ①풀스크린/Space에서 별도 창 검은화면 없이 정상(로그 `popout_titlebar fullsize=True`) ②**TF 측정+제너레이터 켠 채 Spectrum 동시 구동** — 로그 `hb run=True spl=-29.5 extra=1 tf_cards=1`로 동시 구동 실증, xrun·error·disconnect 0건(공유엔진 회귀 통과). **STEP 1 완전 종료.** (v1.6.5, 2026-06-17 구현·집검증완료: 분리·통째이동·측정 안끊김·독립창·위치복원·고아창 없음.) *(상세: project_smaart_view_layer_plan)*
- [x] **[버그] RTA 멈춤(장치변경·USB·채널변경)** — ✅✅**실측 검증완료(2026-06-18, Scarlett 2i2)**. 증상=RTA 막대 완전 정지. 멈춤 트리거가 계속 나옴(장치변경→USB뽑기→채널변경 Ch1↔Ch2, loopback 모드선 TFDuplexThread가 엔진 우회). **두더지잡기 대신 liveness watchdog로 구조적 해결**: 원인 불문 "청크 끊김"이 공통결과 → `_on_rta_chunk`가 `_rta_last_chunk` 기록, `_update_rta`(10fps)가 stale(1.2s+)·장치변경·채널변경·스트림소멸 4트리거로 자동 재구독. **사용자 실측 "채널변경 해결, USB뽑꽂·헤드폰 해결"**. 곁가지로 self-heal이 핫플러그 막던 회귀도 수정(`_any_audio_active`서 RTA 구독 제외). 로직테스트 6케이스 PASS. *(상세: project_smaart_view_layer_plan)*
- [ ] (보너스) **TF 제너레이터 채널버그 재현** — 4in4out으로 재현 + 로그의 시작 장치목록에서 그 인터페이스 `in=?` 확인(8채널 의문 해소).
- [x] (보너스) **USB idle 핫플러그 재현** — ✅✅**실측+로그 검증완료(2026-06-17, Scarlett 2i2)**. idle 상태 뽑/꽂 2회 반복 모두 자동 감지·복귀(수동 Refresh/재시작 불필요). **로그 증거**(세션 17:04:20): `리스너 등록 OK`(시작1.2s후) → 뽑기 `감지(idle)→재초기화` → 꽂기 `감지→PortAudio재초기화→Scarlett 2i2 목록 복귀`, 700ms 디바운스 단발 재초기화 정상. → D섹션 버그 항목과 동일 검증.

## 🖥️ B. 내일 바로 — 앱 재시작/육안/빌드 확인 (신호 불필요)

- [x] **About 화면** — ✅✅검증완료(2026-06-15). License 버튼→SPECTRA 마크+버전 1.6+머신ID, 다크/라이트 둘 다 가독성 OK, Copy Machine ID 동작 확인. *(상세: project_v1_5_verify_checklist)*
- [x] **엠프티 스테이트** — ✅✅검증완료(2026-06-15). Start 전/Stop 후 중앙 SPECTRA 웨이브 마크+"Press Start to begin". **검증 중 버그 발견·수정:** `_start()`가 `_idle_hint=False`로 끄는데 `_stop()`이 다시 안 켜서 한 번 Start하면 영영 안 뜨던 것 → `_stop()` 캔버스 clear 직후 `fft_cvs._idle_hint=True; oct_cvs._idle_hint=True` 복귀(`wayaudo2.py:14000`). 옥타브 확인, FFT 동일 메커니즘.
- [ ] **DisplayName (.app)** — 빌드된 .app이 메뉴바/Dock에 "SPECTRA"로 뜨는지(소스실행 말고 .app). ⭐**이때 메뉴얼 `menubar.png` 촬영**: .app 실행(앱 이름=SPECTRA) → View(또는 Help) 메뉴 펼친 채 `⌘⇧4 → Space`로 캡쳐 → `docs/img/menubar.png` 저장. (소스 python 실행 시 앱메뉴가 "Python"으로 떠서 지금 찍으면 안 됨.) → 이러면 MANUAL.html 스크린샷 11/11 완성.
- [x] **UI 개선 #1~5 육안** — ✅✅검증완료(2026-06-15). #1 네이티브 메뉴바·#2 Stereo 하단 메트릭바 토글·#3 캡쳐 토스트·#4 단축키 툴팁·#5 TF 엠프티 전부 정상. (#6 드롭도 ✅완료)
- [x] **팝아웃 풀스크린** — ✅✅검증완료(2026-06-15). 라우드니스/벡터스코프 팝아웃→풀스크린 Space 튐 없음. *(상세: project_popout_fullscreen_fix)*
- [x] **GUI 육안 묶음** — ✅검증완료(2026-06-15). 팝아웃 아이콘 / 툴바 3탭 동일 / 축 dB라벨 / 다이얼로그 통일 / 라이트·다크 가독성 육안 OK. *(상세: project_v1_2_verify_checklist)*

## 🛠️ C. v1.6 신규 기능 (코드 작업, 하드웨어 불필요 — 아무때나)

- [x] **[기능] 캡쳐 리캡쳐(제자리 다시 캡쳐)** — ✅✅**실앱 검증완료(2026-06-16)**. 기존 캡쳐의 **색/이름/그룹/가시성 유지하고 곡선 데이터만 현재 라이브로 덮어쓰기**. **트리거 = ①행 우클릭 "다시 캡쳐 (Recapture)" ②단축키 R**(선택 캡쳐, 없으면 마지막 / Space=새 캡쳐와 짝). 구현: 각 캔버스에 `recapture()`(FFT/Oct)·`recapture_live()`/`recapture_data()`(TF mag/phase/ir) 추가 — **같은 dict 객체를 유지해 메타 자동 보존**, 데이터 키만 교체. TF는 `_recapture_tf(idx)`가 `_tf_captures[idx]['source']`(primary/cardN)에 따라 3캔버스 동시 갱신. 드로어 시그널 `recapture_requested(mode,idx)` → `_on_drawer_recapture`(spec=fft_n 분기, tf 위임)·R은 `_recapture_selected`. 오프스크린 5캔버스 검증 PASS(제자리·메타보존·데이터갱신·범위밖 False). 미빌드.

- [x] **[기능] SPL 미터 측정 소스 선택** — ✅✅**실측 검증완료(2026-06-16, M4)**. SPL 미터가 primary 채널에만 하드코딩돼 있던 것 → **SPL 설정창(⚙)에 "측정 소스" 드롭다운** 추가, 입력 카드(primary+추가 소스) 중 선택. 선택 소스만 `push_levels`로 라우팅: primary는 `_spl_source_id==0`일 때만 push, 추가 소스는 `_process_extra_source`에서 선택 시 `_spl_inputs`(A/C가중+fs_peak, 소스SR기준 캐시 가중테이블)로 계산해 push. **소스별 calib**(device:ch 조회)도 창에 반영. 카드 삭제 시 primary로 자동 복귀. 오프스크린: `_spl_inputs`=primary경로 Δ0 일치, 셀렉터 매핑 PASS. ⚠️미영속(재시작 시 primary로) — 필요 시 추후 추가. 미빌드.
- [x] **[기능] SPL 임계 알람 — 독립 창** — ✅✅**실앱 검증완료(2026-06-19)**. 노랑→빨강 전이·깜빡임·View 메뉴 위치 OK. (소리 알람 옵션은 미구현·보류 — 현재 코드에 알람음 없음, 시각 전용. 추후 결정.) 미빌드. **SPL 미터와 무관한 완전 독립 창**(`SplAlarmWindow`, `wayaudo2.py` SplMeterWindow 위에 정의). **View 메뉴 → "SPL 알람 창"** 으로 직접 열림. 마스터 지표 1개를 한계와 비교해 **초록(여유)→노랑(한계 -N dB 접근)→빨강(초과, 0.4s 깜빡임 + "N초째")**. 창 크기에 맞춰 스케일되는 큰 중앙정렬(3구 신호등 글로우 + 거대 숫자 + /한계 + ▼여유/▲초과 + 하단 상태). **자체 오디오 엔진**(`_SplMetricEngine`: Fast/Slow EMA + LEQ적분 + 피크홀드, SplMeterWindow와 동일 계산식)을 소유해 **SPL 미터 안 열려 있어도 동작** — `_process_audio`가 spl_meter 급전과 별개로 `spl_alarm_win.push_levels(primary)` 직접 호출(다른모니터/구석 FOH 글랜스용). **자체 ⚙ 설정**(타이틀바, `SplAlarmConfigDialog`: 마스터 지표·한계·노랑 여유·LEQ time), `_settings['spl_alarm']`에 영속. 기본 LAeq/100dBA/-3dB. calib=primary(`_apply_calib_thresholds`에서 동기화), 테마 토글·앱 종료 정리 연결. 오프스크린: 엔진 laeq=98.40 정확 + 창 급전→tick→display 일치 + 3상태 렌더 PASS. **이력**: 처음엔 SPL미터 내장 바(`_SplAlarmBar`)+설정 종속 팝업으로 구현(반응형 겹침수정 포함)했으나, 사용자 요청 "아예 별도 창"으로 **독립 창 재설계**(내장 바/팝업체크박스 폐기, `git checkout 60c8321 -- wayaudo2.py`로 알람코드 리셋 후 재구현). **영어화+지표표기(2026-06-16, 사용자 요청)**: 창/메뉴/설정/상태문구 전부 영어(SPL Alarm·OK Safe·AMBER Ease off·OVER Ns·"X dB headroom"·"+X dB over"). 단위를 **선택 지표대로** 표기(`_UNIT`): laeq→"dB LAeq"·lceq→"dB LCeq"·dba→dBA·dbc→dBC·spl→dB SPL·peak/fs_peak 등, **LEQ 지표는 적분 분(min) 덧붙임**("/ 110 dB LAeq 10min"). 메뉴 "View → SPL Alarm". 3케이스 렌더 PASS. **글랜스 동작 추가(2026-06-16, 사용자 요청 — 미터·알람 둘 다)**: ①**always-on-top**(`Qt.WindowStaysOnTopHint`, 본화면 위 무조건 — 프레임리스 유지 위해 _apply_dark_titlebar 前에 OR) ②**마우스 떠나면 카드만**(`_glance_chrome_update`: 200ms 타이머에서 `geometry().contains(QCursor.pos())`로 판정해 `_dark_titlebar`/`_dark_grip` 표시·숨김 — 자식 위젯 enter/leave 영향 안 받는 전역커서 방식, 숨겨도 창 외곽크기 불변→카드만 꽉 참). 오프스크린: 플래그·호버 토글 PASS. ✅실앱 검증완료(2026-06-19). 더 큰 그림은 G의 SPL 컴플라이언스 로드맵. *(체크포인트 커밋 60c8321)*
- [x] **캡쳐 일괄 표시/숨김 토글** — ✅✅**실앱 검증완료(2026-06-15, A1~A4 통과)**. 전체 토글(SPECTRA 그라디언트 웨이브 아이콘, 제목 왼쪽)+그룹별 토글(× 왼쪽), 스마트 토글. 새 브랜드 아이콘 `_wave_toggle_icon`(ON=그라디언트 / OFF=회색+사선). 오프스크린 렌더+로직 테스트 통과. *(상세: project_todo_capture_bulk_visibility)*
- [x] **캡쳐 색상 구분 (라이브 회피 + 캡쳐끼리 구분)** — ✅✅**실앱 검증완료(2026-06-15)**. `_auto_capture_color`를 **farthest-point 팔레트**(`_capture_palette`)로 재작성: 라이브 8색을 시드로 색공간에서 최원점 선택 → 라이브와도(최소 RGB 76) 캡쳐끼리도(22개 최소 73, 기존 11) 최대 구분. 다크/라이트 밝기 하한. ~~hue±20만~~/~~점선~~ 폐기. ⚠️기존 캡쳐는 옛 색 유지(재캡쳐 또는 재배치 필요). *(상세: project_todo_capture_color_avoidance)*
- [x] **캡쳐 타겟 그룹 유연화** — ✅✅**실앱 검증완료(2026-06-15)**. 그룹에 갇혀 캡쳐되던 문제 해결. 그룹 헤더 **이름 클릭=새 캡쳐 타겟 지정(◉ 하이라이트)**, 재클릭=미지정 복귀, **화살표=접기 분리**, 탭바 아래 **"캡쳐 위치" 칩**(클릭 시 미지정). 새 시그널 `capture_target_changed`, 활성 타겟 `_target` dict(기존 `_pending_group` 흡수). 렌더+로직 테스트 통과. *(상세: 플랜 luminous-sauteeing-widget.md)*
- [x] **딜레이 파인더 단축키 개선** — ✅✅**실앱 검증완료(2026-06-15)**. TF 딜레이 파인더(L) 창에서 **L=다시 탐색(Find Again), Enter=딜레이 적용(Insert All)**. 버튼 autoDefault 끔 + 힌트 라벨 괄호표기 `(Enter)`/`(L)`. `AllDelayFinderDialog.keyPressEvent`. 키 라우팅 테스트 통과.
- [x] **스펙트럼 FFT 브랜딩 + 곡선 부드럽게 + 커서** — ✅구현완료(2026-06-15). FFT 곡선·채움 모두 SPECTRA 그라디언트(저역파랑→고역빨강, `_spectra_grad_pen`/`_brush`/`_SPECTRA_GRAD_STOPS`). 초록 채움 폐기→그라디언트 채움(alpha 40, dim 16). 클리핑 시 빨강 유지. **⚠️함정: `_front_id==0` front 재그리기가 초록 line_col로 덮던 것 발견→그것도 그라디언트화.** 시인성: 선 AA+float좌표(계단제거). **커서 십자선: 가로선을 마우스Y→곡선값 위치로(매그니튜드 방식, `_draw_live` 커서부).** 전체화면 버벅임: ①채널 없을 때 front 재그리기 스킵 ②**그라디언트 채움을 캐시 픽스맵+clip blit로**(픽셀별 그라디언트 계산 제거 — "소리 크면=면적 큼=더 버벅"의 근본원인, `_grad_fill_pixmap`). 전부 실렌더 검증. ③**글로우 밴드 채움**: 큰소리=채움면적이 화면 채움이 근본원인(로그상 오디오 xrun 0=순수 렌더 프레임드롭). 채움을 선 아래 고정높이(BAND 72px) 밴드로만 →면적이 음량과 무관하게 일정→전체화면/큰소리 가벼움. 사용자 선택(글로우밴드). ④글로우밴드로도 잔여 버벅임→**선 AA OFF**(float좌표라 계단현상 없음). 렌더 1.51→0.97ms(Retina선 차이 더 큼). 채움(밴드)+선 모두 AA 미사용. 잔여 시 다음=fps↓ 또는 MAX_POINTS↓.
- [x] **[성능] 첫 캡쳐 느림** — ✅✅검증완료(2026-06-15). farthest-point 색 팔레트가 첫 캡쳐 때 순수 파이썬 이중루프로 계산돼 수백ms~. numpy 벡터화(`_capture_palette`)로 **1.7ms**, 색 결과 동일(결정론).
- [x] **[UX] 캡쳐 패널 선택 표시 안 됨** — ✅✅검증완료(2026-06-15). 패널이 항상 마지막 캡쳐만 강조(`front_idx=len-1`). 드로어가 클릭한 캡쳐(`_sel`) 추적→그 행 강조(좌측 액센트 바+볼드+배경). 추가/삭제 시엔 마지막 자동. 그래프(canvas front)는 기존대로.
- [x] **[버그] 캡쳐 전체 숨김 시 라이브 곡선 흐리게 남음** — ✅수정(2026-06-15, 검증중 발견). 포커스(_front_idx)된 캡쳐가 숨겨져도 `_cap_focus=True`라 라이브가 dim 유지되던 것. `_cap_focus`를 "포커스 캡쳐가 visible일 때만"으로 가드. FFT/oct/TF(mag/phase/ir) 6곳 전부. 로직 테스트 통과.
- [x] **[버그] 스펙트럼 소스 카드 해제해도 곡선 표시됨** — ✅수정(2026-06-15). 첫 실행 시 카드 가시성 해제 상태인데 `set_channel_data`가 새 채널을 항상 visible=True로 만들어 곡선이 나타나던 것. 캔버스에 `_ch_visible` 영속 맵 추가(데이터 도착 전에도 유지), `set_channel_visible()`로 통일, 카드 빌드 시 동기화. FFT+oct 둘 다. 4케이스 테스트 통과.
- [x] **라이트 모드 캡쳐 패널 검정 잔재 수정** — ✅구현완료(2026-06-14). 라이트에서 헤더 버튼(wave토글/Avg/Export/+Grp)·행 체크박스·R버튼·구분선이 다크로 남던 것 테마화. `_CaptureDrawer._restyle_chrome()` 신설(헤더버튼) + `_make_row` 체크박스/구분선 테마분기(다크 동일) + `_apply_theme`가 토글 시 `_restyle_chrome()`+`_redraw()` 호출 + `#capScroll` 배경 테마화. 다크 회귀 없음(렌더 확인).

- [x] **[결정: 수용] 전체화면 상단 메뉴바가 헤더 가림** — macOS 전체화면 hover 시 메뉴바/타이틀바 reveal이 SPECTRA 헤더(NSFullSizeContentView) 덮음(OS 표준, 마우스 뗄 때만 잠깐). 상단 여백 시도→노치맥 검은 띠 커서 더 어색→되돌림. **사용자 결정 A: 현재대로(띠 없음, brief 표준 cover 수용)**. 추후 원하면 풀스크린 메뉴바 always-visible 네이티브 방식 재검토.
- [x] **LEQ 시간 미터 + 리셋** — ✅✅구현+실앱 검증완료(2026-06-15). SPECTRA 그라디언트 진행 바(`_GradTimeBar`, 전폭 그라디언트를 진행도만큼 노출)+리셋 버튼(↻=적분버퍼 비움)을 **LAeq/LCeq 카드 회색 바탕 안**에 배치(`_SplPanel.reset_time_requested`/`set_time_progress`). dBA/dBC 카드엔 없음. 진행도=쌓인시간/`_leq_secs`. **숫자(경과시간) 표시 제거**. 창은 컴팩트 최소 크기로 열림(`_open_w/_open_h`). 렌더 테스트 통과(laeq/lceq만 바 표시, 30s→50%). 리셋 아이콘=둥근 화살표(`_draw_reload_arrow` 공용 헬퍼, 거의 꽉 찬 원+삼각 화살촉)로 통일 — 카드 버튼은 위젯에 직접 그리는 `_ReloadBtn`(정사각 18×18, QIcon 스케일 찌그러짐 방지), 타이틀바 Reset Max(`_ResetMaxBtn`)도 동일 글리프로 일관화(2026-06-15).
- [x] **[버그] 소스 이름변경 입력칸이 카드 밖으로 넘침** — ✅수정완료(2026-06-15). `begin_inline_rename`이 폭을 `max(label_w+60,100)`로만 잡아 카드(host) 오른쪽 경계를 무시 → 파란 입력칸 삐져나옴. `w=min(w, host.width()-tl.x()-6)`로 클램프(6px 우측 여백). Spectrum/TF 인라인 리네임 공통. 오프스크린 폭 110~200 전 구간 카드 내부 유지 확인.
- [x] **[버그] 팝아웃 창 흰색 타이틀바** — ✅수정완료(2026-06-15). 벡터스코프/라우드니스 레이더 팝아웃(`_make_popout_win`)이 네이티브 흰 타이틀바였음 → `_apply_dark_titlebar(win, resizable=True)` 적용(프레임리스+다크바+✕+우하단 리사이즈 그립). 닫기=`win.close()`→기존 closeEvent로 도킹 복귀 유지. 렌더 검증(frameless·_dark_titlebar·grip·제목 'Vectorscope' 확인).
- [x] **SPL 미터 지표 확장(Smaart 세트)** — ✅✅구현+실앱 검증완료(2026-06-15). `_METRICS`에 spl_slow/dba_fast/dbc_fast/spl_fast/peak/peak_c/fs_peak 추가(+기존 dba/dbc 라벨 'SPL A/C Slow'로). 스펙트럼 처리부(`_process_audio`)가 순간 calibrated Z(=raw_dbfs+calib)/A(raw_dba)/C(raw_dbc) + 풀스케일 디지털 피크(fs_peak=20log10 max|buf|)를 `spl_meter_win.push_levels()`로 전달. SPL Meter가 dt기반 EMA로 Fast(τ125ms)/Slow(τ1s) 계산 + 피크 홀드(6dB/s 감쇠), LEQ버퍼는 Slow A/C 사용(기존 laeq/lceq 유지). peak=디지털피크+calib, peak_c=C가중 최대홀드(근사), fs_peak=dBFS(캘리브 무관, -6/-1 임계). `_SplPanel`: peak/peak_c/fs_peak는 Max행 숨김. 합성피드 검증(12지표 값·다이얼로그 12옵션·렌더 OK). 사용자선택=전체세트. ⚠️Peak/Peak C는 시간영역 가중 참피크 아닌 근사 — 하드웨어 실측 시 재확인.
- [x] **SPL 미터 시계 카드(선택형)** — ✅✅구현+실앱 검증완료(2026-06-15). `_METRICS`에 `'clock':('Clock','#5AC8FA')` 추가 → 설정 다이얼로그(`SplLayoutDialog`)가 `_METRICS` 자동 나열하므로 칸별 선택지로 바로 노출. `_SplPanel`: clock이면 Max행(dot/max_lbl) 숨김 + `set_clock(text)`로 값라벨에 시각, `reset()`은 clock 스킵. `_update_display`: 매 틱(200ms) `time.strftime('%H:%M')`(24h)로 clock 패널 갱신(오디오 없어도 동작), dB 값 루프에선 clock continue. 렌더 검증: clock 카드 '11:47' 표시·Max숨김, 다이얼로그에 'Clock' 옵션 확인.
- [x] **SPL 카드 Smaart식 스케일링** — ✅✅구현+실앱 검증완료(2026-06-15). 좁고길게/넓게 모두 카드가 폭 꽉 채움+너비 따라 숫자 커짐+세로중앙+클리핑 없음 육안확인. 리사이즈 동작을 Smaart SPL Meters처럼: **카드가 셀을 꽉 채우고(양옆 여백 0) + 숫자는 너비 기준으로 크게 스케일 + 세로 중앙 정렬**. (1차 "카드 비율 고정(레터박스)"은 양옆 검은 여백이 어색해 폐기.) `_SplPanel`: `paintEvent`는 셀 전체(`self.rect()`)에 카드 렌더, 레이아웃에 value 위·아래 `addStretch`로 세로 가운데, `_relayout`은 `scale=min(w/_BASE_W, h/_fit_h)`(너비 주동인 + 높이 상한 클램프, `_fit_h` 일반92·leq116). `_inner_rect`/`_aspect` 제거. 열림 크기 `_open_w=150*cols`/`_open_h=150*rows`(좁고 길게). 렌더 검증: 좁고길게/넓게/넓고낮게 모두 폭 꽉 채움+너비 따라 숫자 커짐, 클리핑 없음. 사용자 선택=너비 기준(Smaart식).
- [x] **릴리즈 노트 메뉴** — ✅✅**실앱 검증완료(2026-06-15)**. Help 메뉴에 "릴리즈 노트 (Release Notes)" 추가 → 클릭 시 브랜드 창(다크+그라디언트 라인+QTextBrowser)에 RELEASE_NOTES.md 렌더. 경량 md→html(`_md_to_html`: ##/-/​**​/`code`/링크정리). 빌드 3종 spec에 RELEASE_NOTES.md datas 추가. 렌더 확인.
- [x] **팝업 다이얼로그 브랜딩** — ✅✅**실앱 검증완료(2026-06-15)**. 시스템 QMessageBox(흰 타이틀바+시스템 아이콘) → SPECTRA 브랜드 다이얼로그(`_brand_msg`/`_BrandBox` 드롭인): 다크+상단 그라디언트 라인+라인 아이콘(info=파랑 / warn=주황△ / question=?)+브랜드 버튼(위험=빨강 `ss_btn_danger`). info/warning ~25곳 sed 일괄 치환 + Delete All confirm. 실렌더 확인. 사용자 선택=B(라인 아이콘).
- [x] **[버그] 다이얼로그 이중 타이틀바** — ✅✅검증완료(2026-06-15). `_TFAverageDialog`·`LicenseDialog`가 `_apply_dark_titlebar`(프레임리스) 뒤에 `setWindowFlags` 재호출→네이티브 프레임 되살아나 타이틀바 2개. setWindowFlags 제거(고정크기는 setFixedSize로). TF Average는 그라디언트 라인+힌트+테마 체크박스로 재디자인.
- [x] **[기능] 제네레이터 Sine(사인파) 추가** — ✅구현완료(2026-06-15, 실앱검증 대기). TF 제네레이터에 Pink/White/**Sine**/Sweep. Sine 버튼 클릭→`SineConfigDialog`(주파수 10~24000Hz)→Apply 시 버튼에 주파수 표기('Sine 1k'). 재생 버퍼는 **정수 사이클 사인**(`_n_sine=sr*10`, k=round(f·n/sr) 사이클 → 루프 이음새 클릭 0, 오프스크린 검증: 주파수 오차 0Hz·seam~1e-13·진폭1.0). 레벨은 기존 Level 컨트롤 공용. 버튼 상호배제·Cancel복원·File로드 동기화 전부 반영. Sweep 전용 1-shot 캡처 로직은 미접촉(Sine은 Pink/White처럼 루프 재생 경로). dBA 극저역 격리테스트(50/100Hz 톤)에도 바로 활용 가능. 상태변수 `_sine_freq`.
- [x] **캡쳐 우클릭 Delete All** — ✅✅**실앱 검증완료(2026-06-15)**. 캡쳐 패널 행/빈공간 우클릭 → **Delete All**(영어), 확인 다이얼로그 후 현재 탭 전체 삭제. 드로어 공유라 **Spectrum/TF 동일**. 시그널 `delete_all_requested`, 핸들러 `_on_drawer_delete_all`(spec=fft+oct, tf=mag/phase/ir+_tf_captures, Δ기준 해제). 드로어 emit 테스트 통과.

## 🐛 D. v1.6 버그 수정 (코드) — 🏢 내일 사무실에서 (하드웨어 검증 필요)

- [~] ⚠️**[버그·검증중 발견 2026-06-26] Adaptive(MTW) 엔진에서 멀티카드(extra) 분석/곡선 안 뜸** — ✅**근본수정+카드2 라이브검증완료(M4), 3+카드 라이브 재검증만 남음**. **근본원인:** `_render_inner`가 MTW 엔진 분기(`wayaudo2.py:12766`)에서 `_render_mtw`(primary 전용) 호출 후 **즉시 `return`** → 그 아래 extra 카드 렌더 루프가 **실행조차 안 됨**. extra 카드 데이터·누적(`_on_mc_chunk`→`_accumulate_extra`→`_extra_pair_acc`)은 정상인데 **그리는 단계만 누락**(엔진=기본 Adaptive라 멀티카드 전부 영향. Single 엔진이면 정상이라 이전 검증서 안 잡힘). **수정:** extra 렌더 루프를 `_render_extra_pairs(freqs,t_ms)` 메서드로 분리, **MTW 분기(조기 return 전)·레거시 경로 양쪽에서 호출**. extra 카드는 엔진 무관 자체 FFT 크로스스펙트럼이라 안전. **검증(로그 교차):** 수정 전 `[DIAG] tf_card_render` 0줄 → 수정 후 `i=0 accn=80 disp=True gvis=True` 매초 기록, 화면 카드2 곡선 표시 확인. selfcheck TF 5/5 PASS. **➡️ 카드 3개+ N개 일반성 라이브 재검증은 사용자 결정(2026-06-26)으로 다음 버전 보류**(렌더는 `for i,acc in enumerate(_extra_pair_acc)`라 카드수 무관 구조 — 코드상 일반성 확보됨, 스트레스 실측만 이월). 계측 `[DIAG] tf_card_setup`/`tf_mc_route`/`tf_card_render`(브랜치 `chore/v17-hw-verify-diag`). *(상세: [[project_v17_hw_verify_prep_branch]] [[project_todo_tf_multicard_capture_verify]])*
- [~] **[버그] TF 제너레이터 출력 채널 변경 미반영** — ✅**코드수정완료(2026-06-17, HW검증 대기)**. 두 출력채널 콤보(`sig_out_ch_cb`/`sig_out_ch2_cb`)를 save-only 람다 → 새 핸들러 `_sig_out_ch_changed`(저장+재생중이면 `_stop_sig_gen`→`_start_sig_gen` 재시작)에 연결. `_restoring_devices` 중엔 저장만. ⚠️M4 등 다채널 인터페이스에서 재생 중 Out 3+4 변경 즉시반영 실측 필요. *(상세: project_bug_tf_sigout_channel_no_reconfig)*
- [x] **[버그] USB idle 핫플러그 안 잡힘** — ✅✅**idle 케이스 실측+로그 검증완료(2026-06-17, Scarlett 2i2)**. 근본=CoreAudio device-change 리스너 없음 → `_CoreAudioDeviceWatcher`(ctypes로 `AudioObjectAddPropertyListener`, kAudioHardwarePropertyDevices) 추가, 시작 1.2s 후 등록. `changed` 시그널→700ms 디바운스→idle이면 `reinit_audio_devices`. **검증:** idle 뽑/꽂 2회 모두 자동 감지·목록 복귀, 로그(`리스너 등록 OK`/`감지(idle)→재초기화`/Scarlett 복귀) 확인. 미빌드. *(상세: project_bug_usb_idle_hotplug_coreaudio)*
- [x] ⚠️**[버그·검증중 발견] 측정 중 USB 끊김 → 복구 안 됨(교착)** — ✅✅**실측+로그 검증완료(2026-06-17, Scarlett 2i2)**. TF 측정 중 뽑기→`제거 감지(removed=True)→끊김복구강제`→목록제거→재연결 폴링→재연결 시 목록복귀+측정재개+**소리복구** 전 과정 로그 확인. 폴링 타임아웃(30초) 후 꽂아도 **CoreAudio 리스너 idle경로가 백업으로 복구**(이중 안전장치 실증). 2차 코드: 2026-06-17 HW검증 중 발견(Scarlett 2i2, TF 탭). **재현:** TF 측정 중 USB 뽑고 Start/제너레이터 재시도→다시 연결해도 소리 안 남(복구 안 됨). **근본=교착+잘못된 신호:** 끊김 워치독(`MultiChannelAudioThread._run` 2초 콜백갭)도, 1차 수정의 콜백age 체크도 **둘 다 "콜백 생존"에 의존**하는데, **Scarlett 드라이버는 USB 뽑혀도 InputStream 콜백을 무음(0)으로 계속 흘림**(로그 17:39 `스트림 생존, 보류` 확인) → 콜백 신호로는 끊김을 영영 못 잡음. **2차 수정(올바른 신호=장치 개수):** ⓐ`_CoreAudioDeviceWatcher.device_count()` 추가 — `AudioObjectGetPropertyDataSize(kAudioHardwarePropertyDevices)`/4 로 HAL 장치수 직접 질의(PortAudio 캐시·콜백 무관, 뽑히면 즉시 감소). 실측 4개=PortAudio와 일치. ⓑ시작 1.5s 후 기준선 `_ca_dev_count` 기록. ⓒ`_ca_apply_device_change`: 매 이벤트마다 개수 비교, **활성+개수감소(removed) 또는 콜백사망이면 끊김 복구 강제**(`reinit_audio_devices`+`_begin_replug_watch`), 개수증가(헤드폰 등 추가)·동일+생존은 보류(오판방지). 1차의 `_last_cb_mono`/`freshest_callback_age`는 보조신호로 유지. 오프스크린: device_count 실동작 + 분기 6케이스(측정중제거→복구/추가→보류/idle 등) PASS. ⚠️**HW 재검증:** TF 측정 중 뽑기→재연결 시 자동복구+소리복귀, 로그 `사용 중 장치 제거 감지(removed=True) → 끊김 복구 강제` 확인. 회귀: 측정 중 헤드폰 연결 시 측정 안 끊기는지. *(상세: project_bug_usb_idle_hotplug_coreaudio)*
- [x] **[해결·버그아님] dBA가 Smaart比 낮음 — SPECTRA가 정확한 쪽으로 확정** — ✅✅**단위테스트로 종결(2026-06-15)**. 실제 코드 함수(`power_spectrum_db`+`a_weight_db`+`_process_audio`의 dBA 합산 `Σ10^((db_raw+aw)/10)`)를 그대로 떼서 순수 톤 주입 → **dBA−Z가 IEC A-가중 이론값과 전 주파수 0.04dB 이내 일치**(50Hz: −30.23 vs −30.27, 100Hz: −19.13 vs −19.14, 250Hz: −8.68, 1kHz: 0.00). **SPECTRA dBA = IEC 표준 그 자체, 버그 없음.** ⇒ 내장마이크 A/B에서 LF 지배 신호일 때 Smaart가 A를 ~7dB 높게 읽은 건 SPECTRA 결함 아님(추정: Smaart 시간영역 A-가중 IIR의 극저역 오차 / 노트북 스피커 음향오염=순수톤 아님). dBC−dBA도 SPECTRA가 이론값에 더 가까움. **참고: Z 절대레벨·C가중·1kHz톤·미드지배 A는 모두 Smaart와 0~0.4dB 일치(이미 검증).** 추후 원하면 캘리브 측정마이크+기준 SPL미터로 재확인 가능하나 계산은 증명 완료. *(상세: [[project_spl_meter_layout]])* — 드라이버 보고값(`max_input_channels`) 추정. A의 재현+로그로 먼저 판별 후 결정.

## 📄 E. 문서

- [~] **MANUAL.html v1.7 리뉴얼** — ✅**전면 재작성 완료(2026-06-21)**: 15섹션, 전 기능 + 버튼별 ①②③ 콜아웃, 신규(공통/창분리/SPL알람/TF엔진·RTA·Units/라우드니스 시안C/재캡쳐/프리셋·언어토글). 스크린샷 **10/11 완료**(stereo·tf·spl_alarm·capture_drawer는 Claude 자체렌더 또는 사용자 실촬영, popout_split·spectrum·calib·spl·spl_settings·tf_delta 반영). **남은 1장 = `menubar.png` → ⚠️아래 빌드 항목에서 촬영.** *(상세: project_todo_manual_screenshots)*
- [ ] **README.md 작성** — 깃허브 대문(소개·기능·설치·빌드·문서링크), 민감정보 주의. *(상세: project_todo_readme)*
- [ ] **LANDING.html 디테일** — 앱 미리보기 교체·다운로드/문의 링크·가격(배포방향 정해지면). *(상세: project_todo_landing_details)*
- [ ] **문서 내용/오타 검토** — MANUAL/LANDING/RELEASE_NOTES 맥 브라우저로 열어 문구·디자인·링크 확인.

## 📦 F. 배포 / 빌드 / 보안

- [x] **`RELEASE_NOTES.html` v1.8·v1.9 섹션 동기화** — ✅완료(2026-07-04). 브랜드용 HTML에 v1.8·v1.9 섹션 추가, 최신 배지 v1.9로. **앞으로는 버전 작업할 때마다 md+html 둘 다 즉시 동기**([[feedback_release_notes_all_docs_per_version]]) — "빌드 때 한번에" 방침 폐지. ⚠️단 `bump_version.sh 1.9`로 `_APP_VERSION` 1.8→1.9 bump는 여전히 "빌드하자" 트리거 때.

- [x] 🪟✅✅ **[버그·Windows] USB 인터페이스 안 잡힘/안 열림 — 수정+Parallels 실측 검증완료(2026-06-30, Scarlett 2i2)** — 근본=PortAudio **호스트 API 미지정** → Windows 기본 MME로 USB 4벌 중복열거·SR경직. **수정(macOS 무영향):** WASAPI 우선 필터 + `WasapiSettings(auto_convert=True)`. 빌드 SPECTRA.exe를 Parallels Win11서 실행→로그 `audio_hostapi wasapi_idx=2`·`eng_add dev=12(WASAPI)`·연속 chunks로 정상 입증. 커밋 `991e6ea` push. 상세=[[project_bug_windows_usb_hostapi]].
- [ ] **윈도우 빌드 검증** — v태그 push→GitHub Actions(windows-latest) 산출물이 **SPECTRA.exe**·아이콘·속성(1.5.0.0/WAYAUDIO)인지. *(상세: project_verify_iphone_session)*
- [ ] **코드서명 + 노타라이즈** (⏸️"나중에" 보류) — Developer ID 인증서·노타리 프로필. 배포 전 처리. *(상세: project_v1_5_verify_checklist C)*
- [ ] **소스코드 보호/난독화** — Nuitka(전면 컴파일) 또는 PyArmor 택1. `_LIC_SECRET` 노출방지 중요. 빌드 3종+Actions 교체 필요. *(상세: project_todo_code_protection)*

## ✅ H. v1.8 (2026-07-02 빌드·출시 완료 — Farina 스윕 중심)

> v1.8 3종(Silicon/Intel/Windows) 빌드·배포 완료. 아래는 릴리스 내역(완료). **남은 HW 필드 재검증은 섹션 I(v1.9)로 이월.**

- [x] **[버그 3종·2026-07-02 수정→v1.8 출시(be0eec7)] 스윕/핑크/딜레이 상태머신** — 커밋·빌드 반영(마커 소스 확인). HW 필드 재검증 3종은 v1.9 섹션 I로 이월:
  - ✅**Bug3 딜레이 재적용 안 됨** — 원인: `_on_sweep_captured`가 공유 `delay_ms`를 0으로 덮으며 **숨은 툴바 스핀만** 리셋→보이는 primary 카드 스핀이 옛 값 stale 표시→desync + 재탐색 `setValue`가 같은 값이면 no-op(신호 미발화)로 delay_ms=0에 갇힘. 수정: 카드 스핀도 0 동기화(`[SWEEP_DELAY_SYNC]`) + 딜레이 파인더 강제 적용(`[DELAY_FORCE_APPLY]`, `_apply_find_pair_result`/`_apply_find_result`).
  - ✅**Bug1 카드 Start 안 눌러도 스윕 분석됨** + ✅**Bug2 스윕→핑크 간헐 측정안됨** — 원인: 스윕이 카드 Start(`_display_on`) 게이트를 우회(duplex 직접 캡쳐), 카드 Stopped인데 분석·표시 / 그 상태로 핑크 전환 시 `_start_active_pairs`가 카드 미활성이라 `_start()` 안 함(간헐=이전 카드상태 의존). **사용자 결정: "Play 시 카드 자동 Start"**. 수정: `_toggle_sig_gen`가 활성 카드 없으면 primary 자동 `set_running(True)` + `_start_active_pairs`에 스윕 duplex(`_sc_armed`) 중이면 `_start()` 스킵 가드(같은 장치 2스트림 충돌 방지). `[PLAY_AUTOSTART_CARD]`. 계측 `[DIAG] start_pairs`.
  - selfcheck 32/32·test_farina 14/14 통과. HW 필드 재검증 시나리오 3종 → **섹션 I(v1.9) 이월 항목** 참조.


- [x] **[UI·2026-06-30 ✅구현+실앱검증] SPL 알람 창 디자인 정합** — ✅✅**실앱 검증완료(2026-06-30).** ①**창 배경 검정→회색(bg2)** — SPL 미터 창과 통일(`SplAlarmWindow` `:5008/5104`). ②**LAeq/LCeq 적분 진행 바 + 리셋(↻)** — 미터 LEQ 카드와 동일 언어로 **카드 내부 하단**에 SPECTRA 그라디언트 바 + `_draw_reload_arrow` 글리프를 `_SplAlarmDisplay.paintEvent`에서 직접 그림(텍스트 영역 ch=h*0.85로 축소해 하단 스트립 확보), 리셋 클릭=히트테스트→`reset_requested`. `_SplMetricEngine`에 `reset_leq()`/`leq_progress()` 추가. LEQ 지표일 때만 표시(`set_show_timebar`). selfcheck 알람 체크에 LEQ 변형 추가·PASS. 미커밋→커밋예정. *(처음 외부 위젯 행으로 했다가 "카드 밖이라 어색" 피드백 → 카드 내부 페인트로 재구현)*

- [x] **[기능·2026-06-29 ✅구현+실앱검증] 팝아웃 창 캡쳐 패널 처리** — ✅✅**실앱 검증완료(2026-06-29, 로그+스크린샷).** ①**TF 팝아웃**: 공유 캡쳐 드로어를 TF 본체 왼쪽으로 동반(reparent) → 멀티모니터에서 TF 캡쳐를 팝아웃 창에서 직접 관리. 드로어 1개(공유)라 TF 팝아웃 소유 중엔 메인 탭전환이 안 건드리게 가드(`_drawer_owner='tf_popout'`)+메인 Spectrum 드로어 버튼 비활성, 도킹 시 메인 복귀. ②**Spectrum 팝아웃**: 드로어 안 따라감 + 툴바 좌측 `_drawer_btn` 숨김(`hide()`/도킹 시 `show()`). ③**Stereo**: 무변경(원래 드로어·아이콘 없음). 검증: 로그 `tf_popout_drawer action=popout/dock`·`popout_titlebar win=_TFPopoutWindow/_SpectrumPopoutWindow`, 에러 0건. 계측 `[DIAG] tf_popout_drawer`. 코드 `_popout_tf/_dock_tf/_popout_spec/_dock_spec`+`_tab_changed` 가드. 미커밋→커밋예정.
- [x] **[UI·2026-06-29 ✅구현+검증] SPL 미터 LEQ 카드 이름 정리** — ✅`_METRICS` laeq/lceq 라벨 `'dB LAeq'/'dB LCeq'` → **`'LAeq'/'LCeq'`**(`:5285-6`)로 줄여 옆 카드(SPL A Slow 등)와 형식 통일. selfcheck 하드코딩 제목도 `_METRICS` 참조로 동기화. SPL 알람창 단위표기('dB LAeq 10min')는 단위 접미사라 유지. selfcheck PASS.

- [x] **🔬 코히런스 블루 채움 밴드 실측** — ✅✅**HW 실측 검증완료(2026-06-28).** D안(블루 반투명 채움 밴드, `_coha*0.28` `:8780`) 실제 코히런스 데이터로 정상 표시 확인. *(상세: [[project_v18_tf_panel_distinction]])*

- [x] **[UI·2026-06-27 ✅구현완료] TF 3분석 패널 구분 (CARD 방식)** — ✅세 패널(IR/Phase/Mag+Coh)을 **떠 있는 둥근 카드**로 분리: **카드=순수 블랙(SPECTRA 정체성 유지)+거터=우측 패널 회색 `T('bg2')`=`#1C1C1E`+하어라인 테두리** 둥글게(`_paint_tf_card`, 그리드캐시 3곳 `:7990/8550/9542`), 스플리터 handle=거터색 통일. (처음 밝은카드 `#0e1116`은 "블랙 정체성 사라진다" 피드백 → V2 블랙카드로 정정.) **패널별 컬러 제목**(IR=청록`#2DD4BF`/Phase=보라`#A78BFA`/Mag=파랑`#4DA3FF`, `_TF_CARD_COL`). 라이트/다크 테마 토큰. selfcheck 29/29 PASS(_tf_phase/_tf_mag/_tf_ir 카드 반영). ⚠️처음 라벨중심 4안(spine/chip/band/ghost) 거절→"창 분리" 의도 파악→영역분리 3안(card/frame/window) 제시→**CARD 선택**. 미커밋. *(상세: [[project_v18_tf_panel_distinction]])*
- [x] **[UI·2026-06-27 ✅구현완료] TF 코히런스 기준선 정리 (E안: 전부 제거)** — ✅Magnitude+Coherence 패널의 코히런스 **정적 기준선(1.0/0.5/0.0 점선)·우측 % 라벨 전부 제거**(`TFMagCanvas` 그리드 그리는 블록 `:8600~`). 곡선 + `γ²` 식별 라벨은 유지. 정확한 코히런스 값은 **커서 리드아웃**(`... 85%`, `:8759`)으로 읽음 → 정적 스케일은 커서 있으면 중복이라 삭제. (시안 A~E 목업 제시 → 사용자 **E(전부 제거) 선택**, "커서에 % 뜨면 되지 않냐" 확인 후 확정.) selfcheck `_tf_mag` 추가·PASS. 미커밋. *(이전 상세: [[project_v18_tf_panel_distinction]])*
- [x] **[UI·2026-06-27 ✅구현완료] SPL 미터 버튼 아이콘 변경 (사람들이 기능 모름)** — ✅`extlink`(외부링크 화살표) → **"dB" 텍스트**(SPL=데시벨). ⚠️처음엔 레벨미터 세로바로 했다가 **LEVEL 섹션 헤더 막대 아이콘과 중복**되어 사용자 지적 → 목업 5종(게이지/dB/창+막대/숫자/되돌리기) 제시 후 **B(dB) 선택**. `_SplMeterBtn` paintEvent, 눌림 시 흰색 대비. selfcheck PASS. 미커밋. — LEVEL 패널 헤더의 SPL 미터 열기 버튼이 **Lucide `extlink`(외부링크 화살표)**라 "새 창 열기"로만 보이고 SPL 미터인지 인지 안 됨. 위치=`_SplMeterBtn`(`wayaudo2.py:6355`), 아이콘 그리는 곳 `:6386`. **방향**: 기능이 보이는 아이콘으로 교체 — 바로 아래 `_SplAlarmBtn`(`:6390`)이 미니 신호등을 **커스텀 드로우**하는 패턴 재활용. 후보 ①**레벨미터 세로바**(VU식 막대 = 오디오 표준, 추천) ②게이지/바늘(아날로그 SPL미터 느낌, Lucide `gauge` 신규추가) ③소형 "SPL"/"dB" 텍스트. 현 `_LUCIDE_ICONS`엔 gauge/meter/bar 없음 → 신규 path 추가 또는 alarm처럼 paintEvent 커스텀. selfcheck 렌더 확인. *(상세: [[project_v18_spl_meter_icon]])*
- [x] **[UI·2026-06-27 ✅구현완료] 라우드니스 COMPLIANCE 원형 배지 테두리 거침(계단식)** — ✅QLabel+CSS `border-radius` → **`_ComplianceBadge(QWidget)` QPainter 안티앨리어싱 원**으로 교체(틴트채움+부드러운 링+가운데 글리프). `_build_compliance_card`/`_style_comp_banner`/테마재적용 수정, selfcheck PASS(3배확대 렌더 매끄러움 확인). 미커밋. *(상세: [[project_v18_compliance_badge_aa]])* — 빨강/색상 원형 배지의 링이 들쭉날쭉·점선처럼 보여 부자연스러움. **근본원인**: 배지가 `QLabel` + CSS `border-radius:34px` + `border:2px solid`로 그려짐(`_style_comp_banner` `wayaudo2.py:15467`, 생성 `_build_compliance_card:15449` 68×68). **Qt 스타일시트 border-radius는 안티앨리어싱이 약해 원 테두리 계단현상**. **수정 방향**: CSS 원 대신 **QPainter 커스텀 paintEvent로 안티앨리어싱 원**(틴트 채움 + 부드러운 컬러 링 + 가운데 글리프) 직접 그리기 — SPL 알람 신호등(`_SplAlarmBtn:6390`)·기타 배지 커스텀드로우 패턴 재활용. 상태색(col)·글리프(—/✓/✕)는 그대로, 라이트/다크 토큰 유지. selfcheck 렌더 확인. *(상세: [[project_v18_compliance_badge_aa]])*
- [x] **[버그·2026-06-26 발견→✅✅해결확인] 스윕 측정 후 핑크(연속 노이즈) 작동 안 함** — ✅✅**HW 로그 검증완료(2026-07-02, Scarlett 2i2).** 6/26 이후 리팩터(`_stop_sig_gen` 듀플렉스 완전종료 `:14833` + `_start_sig_gen` 진입 즉시 `_sweep_freeze=False` `:14569`·`_pink_buf` 재생성 `:14594`)로 **부수 해결됨.** 로그(세션 085134): 2s 스윕 측정 직후 `sig_start src=pink` → `OutputStream started out=0` → `InputStream opened` → `mtw_live coh_med=1.0`(핑크 라이브 분석 정상), 예외·xrun 0건. **버그 아님.**
- [x] **Farina 스윕 4종 수정** — ✅✅**전종 완료+HW 로그 검증(2026-07-02)·v1.8 출시(be0eec7).** 2026-06-26 진단, 2026-07-02 구현+headless+HW 검증:
  - ✅**① 1s vs 2s 분석 다름** — `farina_analyze`에 **고정 분석길이**(`_FARINA_ANALYSIS_S=0.25s`, `nwin`을 T무관 고정) 도입. 1s/2s/4s가 동일 주파수그리드(Δf)·동일 IR길이. `[SWEEP_FIXED_RES]`. test_farina 신규 3어설션 PASS.
  - ✅**② 임펄스 Gibbs 링잉** — `H[...]=0` 하드컷 → **1옥타브 raised-cosine 경계 테이퍼**(`_band_taper`, 스윕대역 [f1,f2]은 평탄 유지·대역밖만 롤오프). 밴드 IR 꼬리링잉 -38→-47dB. `[SWEEP_BAND_TAPER]`. test PASS + before/after IR PNG 확인.
  - ✅**④ IR 딜레이 정렬** — 이미 완료(커밋 c3bb517, `_on_sweep_captured` `delay_ms=0`). ①② 후에도 IR 임펄스 +0.000ms 유지 확인.
  - ✅**③ 캡쳐 안됨** — ✅✅**HW 로그 검증완료(2026-07-02).** 스윕 auto-stop 후 캡쳐 정상: `tf_capture_done base="Capture 1" added=1`(1s), `base="2s" added=1`(2s) → total=2. **버그 아님.**
  - ✅✅**① + ⑤ 레벨매칭 HW 검증완료(2026-07-02, 내부루프백)** — 1s·2s가 **5.3dB 어긋나던** 진짜 원인 규명+수정. **원인**(로그 `sweep_level` 계측): 레벨매칭 스케일=median(Wiener÷Farina)인데 1s는 4-8k에서 **윈도우 Farina(`_far`)가 노이즈플로어로 무너지지만 전체배열 Wiener(`_Hw`)는 안 무너져** 비율이 폭등→중앙값 스케일 5dB 부풀림(1s=29.4 vs 2s=24.3dB). 짧을수록 일찍 무너져 길이의존. **수정**: `_wiener_match_scale()` — 피크 대비 -30dB 이하 무너진 bin 제외하고 스케일 계산. **검증**: 재측정 로그 scale 1s=24.5/2s=24.3(일치), CSV 중역 125-2k Δ<1dB(전 +5.3dB 소멸). test_farina `level_match_robust_to_hf_crash`. 잔차=LF SNR변동+HF는 1s가 원래 2s만큼 고역해상 못함(물리, 긴스윕 쓰면 됨).
  - ✅**② IR 경계 테이퍼** + ✅**추가수정: 고정창 pre/tail**(`_FARINA_PRE_S/TAIL_S`, 좁은대역서 2s 꼬리 truncate하던 버그도 수정, test `duration_independent_response`).
  - 🔒**안전성**: #2 레벨/④ IR 정렬 유지. selfcheck 32/32, **test_farina 14/14**. **커밋·빌드 반영(be0eec7)**. *(상세: `_on_sweep_captured`, `farina_analyze`/`_wiener_match_scale` `:7899`)*

## 🔮 I. v1.9 예정 (다음 버전 — 사용자 확정 2026-07-03)

- [x] `[검증·v1.8→1.9 이월]` **스윕/핑크/딜레이 상태머신 HW 필드 재검증** — ✅✅**HW 실측 완료(2026-07-04, 사용자 확인 "다 해봤어").** v1.8(be0eec7) 출시본의 스윕/상태머신 6종 수정 실하드웨어 동작 확인 종료. 시나리오 3종(①Start 안 누르고 스윕 Play→카드 자동 ▶ ②스윕→핑크 전환 매번 측정 ③마이크 이동 재탐색 딜레이 적용) 전부 정상. 자동테스트 selfcheck 32/32·test_farina 14/14도 통과. *(원 항목: 섹션 H)*

- [x] `[UI폴리시/v1.9]` **UI 폴리시 스윕 (2026-07-04, 목업 비교→구현→실위젯 검증)** — 위젯 13개 시각점검 결과 전반 프로수준, 실제 빈틈 3건만 수정: ①**Group Delay 세로축 눈금 균일화** — `-2~30, [-2,0,2,5,...]`(아래 몰림) → `-5~30, 5ms 균일 [-5,0,5,...,30]`(목업 3안 중 A안, `:8494/8521/8571/8612`) ②**TF Magnitude/Phase 줌·팬 발견성 툴팁** — v1.9 신기능이 발견성 0이었음 → 두 캔버스 `__init__`에 멀티라인 툴팁(휠/드래그/Shift팬/더블클릭/⌘±) ③**오라리제이션 정지 버튼 툴팁**(아이콘전용 버튼 128개 중 유일한 툴팁 누락) → `재생 정지`. selfcheck 33/33·i18n 260. 미커밋→커밋. *(목업 `scratchpad/mock_gd_ticks.py`)*

- [x] `[UI리디자인/v1.9]` **세 탭 상단 툴바(메뉴바) 전면 N2 리디자인 (2026-07-05)** — "귀엽/화려/밋밋" 반복 거부 끝에 **N2 'Functionalist'**(Dieter Rams/Teenage Engineering/계측장비 계열) 확정·구현. **디자인**: 테두리리스 컨트롤 + hover 시 배경 노출 + Lucide 직관 아이콘 + 활성만 단일 블루 액센트(#4E7DF0) + 토글=LED 점 + 모노스페이스 숫자 + 그룹 세로 하어라인 + 하단 하늘색 라인 유지. **구현**: 신규 드롭인 위젯 `_N2Select`(RoundComboBox 호환)·`_N2Button`·`_N2Toggle`·`_N2IconBtn`·`_N2Segmented`(`:~7900`) + Lucide 아이콘 14종 추가(`_LUCIDE_ICONS`). Spectrum(`sp0`)·TF(`tb`)·Stereo(`sp2`) 툴바 재구성 — **기존 로직/시그널 100% 보존**(드롭인). `_apply_txn`·`_update_spec_db_btn`·`_update_tf_db_btn`·`_apply_theme` N2 가드. 아이콘: dB=세로눈금축·SR/Phase=파형·+Spectro=패널2단·Peak=천장화살표·Range=상하·Speed/Resp=게이지·Capture=카메라·Engine=cpu·Smooth=곡선·IR=펄스·Units=자·Auralize=헤드폰·Target=과녁. **검증**: 3탭 실물 캡처(크래시 없음)·selfcheck 36/36(신규 `_n2_toolbar` 체크)·앱 정상기동. 상세=[[project_v19_toolbar_n2_redesign]].

- [x] `[UI리디자인/v1.9]` **상단 탭 바 N2 통일 + 아이콘 겹침 해소 (2026-07-05)** — 탭(Spectrum/TF/Stereo)을 회색 트랙 제거 → **`_N2Tab`(아이콘+이름, 활성=글자폭 중앙 밑줄 "B안" + 레터스페이싱)**. **가로줄 정리**: 탭바 하단 보더 제거 + 툴바 하단 액센트선 투명화 → 헤더 무지개선+활성탭 밑줄 2줄만. **아이콘 겹침 감사·해소**(동시노출·다른기능만): TF탭 `spline`↔Smooth 겹침 → **TF탭=`git-compare`(기준↔측정 비교)**, Stereo탭 `gauge`↔Speed/Response 겹침 → **Stereo탭=`volume2`(음량)**. Spectrum탭=`audio-lines`(View와 같은 스펙트럼 도메인이라 허용). `_apply_tab_styles`·`_apply_theme` N2탭 가드. **검증**: 3탭 실물 캡처·selfcheck 37/37(신규 `_n2_tabbar`). 사용자 목업 다수 비교 후 확정(귀엽/화려/밋밋/밑줄과다 반복 조율). 상세=[[project_v19_toolbar_n2_redesign]].

- [x] `[UI통일/v1.9]` **스펙트럼 INPUT 카드 조작을 TF 측정 카드와 동일하게 통일 (2026-07-05)** — 어제 TF 측정 카드 UX(bda5186)에서 인라인 ✕ 삭제→우클릭 메뉴로 통일한 것을 스펙트럼 INPUT 카드(`_SpecCard`)에도 적용: ①헤더의 인라인 **✕ 삭제 버튼 제거**(non-primary에만 있던 것) → primary·추가 카드 헤더 완전 동일 ②`contextMenuEvent` 신설 — **우클릭 메뉴 이름 변경(항상)/삭제(non-primary만)**, `remove_requested` 시그널로 삭제 ③카드 툴팁 `우클릭: 이름 변경 · 삭제`. 이름 편집은 기존대로 더블클릭·우클릭 둘 다. **검증**: 오프스크린 렌더로 두 카드 헤더 통일 확인, selfcheck에 회귀체크(`_spec_card_header`) 신설 → **35/35 PASS**. `_SpecCard` `:6697`(툴팁 `:6740`, `contextMenuEvent` `:6815`).

- [x] `[신기능/v1.9]` **🎚️ TF 라이브 멀티마이크 실시간 평균 — 공간평균 (2026-07-06, SDD 6태스크 구현·리뷰 완료)** — 마이크를 객석 여러 위치에 두고 라이브로 돌릴 때 그 구역의 **실시간 평균 곡선(AVG)** 표시. **크기(파워RMS) 전용**(사용자 "크기만 필요해" → 복소/딜레이정렬 UI 제거, 코어 `_multimic_average`는 complex 보존·미노출) + **평균만** 토글. 카드별 `avg` 포함 토글. **UI=툴바 Σ 버튼 → 팝업**(우측패널 상시노출 → "찾아서 켜는" 구조로 변경). ※정렬 의미 교정(개발 중 발견): 카드 H가 누적서 이미 시간정렬 → 복소 코어는 align=True=정렬 그대로, align=False=`exp(-jωτ)`로 콤복원(현재 UI 미사용). **구현**(브랜치 develop, `0693e1e..bca61e5`): `_multimic_average`(코어, lockstep 필터, <2→None) → 3캔버스 `set_tf_average` 굵은 오버레이 → AVERAGE UI+상태 → `_MeasCard.in_average` → `_render_average` 훅(`_render_extra_pairs` 말미, Single·MTW 공통; primary=저장H_disp, extra=재구성H_disp) → 세션저장/restyle/selfcheck. **검증**: selfcheck 39/39, 헤드리스 단위+offscreen 통합 테스트, 각 태스크 opus/sonnet 리뷰(정렬방향·페인트가드·라운드트립 검증), 최종 whole-branch 리뷰(블로커=크기모드+평균만 phase/IR 빈화면 → `_apply_avg_only` AVG보유캔버스만 숨김 픽스, primary in_average 저장, diag 스팸 제거). 설계=`docs/superpowers/specs/2026-07-05-...`, 계획=`docs/superpowers/plans/2026-07-05-...`. ⏳**남은 것=HW 실측만**(실마이크 2개+ 서로 다른 위치: 크기모드=위치평균 응답, 복소+정렬=콤필터 제거 확인 — 내부SigGen+맥마이크론 배선만 확인가능). 상세=[[project_v19_multimic_live_average]].

- [x] `[버그·UI/v1.9]` **우클릭 컨텍스트 메뉴 흰색(흰 박스) → 다크 통일 (2026-07-05)** — **진짜 원인**: 사용자 macOS가 **라이트 모드**였고(`AppleInterfaceStyle` 미존재), 앱은 다크 외형을 **창별(NSWindow `setAppearance`)로만** 강제 → 별도 NSWindow로 뜨는 **우클릭 QMenu가 시스템 라이트 외형을 따라 흰색**으로 떴음(네이티브 렌더라 QSS/palette 무시). 진단=임시 top-level Show 스파이로 `QMenu 87x33`(≈174×66px, 카드 채널 근처) 확인. **수정 2단**: ①`_global_popup_qss()`(QMenu·QToolTip 셀렉터 한정) `app.setStyleSheet` 전역 적용(`dcd4bb7`) ②**핵심**=`_apply_app_dark_appearance()` 신설 — `NSApplication` 전체 appearance를 DarkAqua로 강제(`_apply_theme`서 호출) → 시스템 라이트여도 네이티브 컨텍스트 메뉴/팝업 다크(`72d6f4f`). **+캡쳐 리스트**: 테두리 제거로 각졌던 리스트를 4모서리 둥근 카드로 인셋(패널 외곽 둥근 + 리스트 둥근 둘 다). **검증**: selfcheck 37/37, `app_appearance ok=True` 로그, 빈/채운 드로어 렌더.

- [x] `[버그·UI/v1.9]` **라이트 모드에서 TF 패널 N2 컨트롤 깨짐 → 테마 재스타일 (2026-07-05)** — 이번 세션 N2 작업들이 다크 색을 하드코딩(생성시 `T('text')`=흰색 등)하고 테마 토글 시 재적용 안 돼, 라이트 모드에서 흰 글씨가 흰 배경에 안 보임(타입 "Pink"·캡스 헤더 등)·카드 스테일. **수정**: `_restyle_sig_gen_theme()` 신설 — 타입 셀(테두리·텍스트·아이콘·chevron)·OUT 콤보 `_flat_border`·Reference 카드/콤보·측정 카드 `restyle()`·캡스 헤더 라벨(`objectName='n2GroupHdr'`) 전부 테마색 재적용, `_apply_theme`서 호출. `_n2_group_header` 하어라인 테마 중립색. 스테레오 `_st_dev_lbl_ss`/`_lu_btn_ss` 테마 대응(투명 flat/활성 회색필). 스펙트럼은 `_apply_theme`가 `_rebuild_ch_cards`로 재빌드해 원래 정상. **검증**: 라이트 TF/스펙트럼/스테레오 실물렌더 확인, selfcheck 37/37.

- [x] `[버그·기능/v1.9]` **다채널 인터페이스 채널 선택 불가 → 채널 그리드 피커 + 드롭다운 스크롤 (2026-07-05)** — 제보버그: 64ch 인터페이스(Red 16Line 등)에서 채널 드롭다운이 화면 밖으로 넘쳐 스크롤/드래그 안 됨(`DropdownPopup`이 스크롤영역 없이 세로로 다 쌓음). 사용자 "획기적 방법" 요청 → **채널 그리드 피커**(`_ChannelGridPopup`: 8열 격자, 64ch도 8×8 한 화면, 번호만 표시, 클릭 선택). `RoundComboBox._grid_popup` 플래그(항목>5) → 그리드, 채널 콤보 전부 설정(spectrum `in_ch_cb`/`_ch_cb`, TF `ref_ch_cb`/`meas_ch_cb`/`sig_out_ch_cb`/`ch2`). +`DropdownPopup` QScrollArea 추가 + `showPopup` 높이 화면60% 클램프(장치 목록 안전망). **검증**: 64ch 그리드 실물렌더 8×8 확인, selfcheck 37/37. HW(다채널) 실측은 사용자.

- [x] `[UI리디자인/v1.9]` **TF Measurement 카드 N2 (direction B = 스펙트럼 통일, 2026-07-05)** — 사용자 목업 2안(색테두리 vs 중립+스와치) 중 **B(중립 테두리+색 스와치+색점Start)** 선택. `_MeasCard`: ①`_apply_card_style` 중립 테두리(#34343B)+선택 색틴트(스펙트럼 `_SpecCard`와 동일) ②헤더 `dot(●)`+텍스트 Start버튼 → **색점=Start LED**(`_led_power_pm`, 측정중=채움+글로우/정지=흐린 링, `_on_start_stop`), 텍스트 `_start_btn`은 숨김 상태보관 ③dBFS 모노 ④`_cb_style`/`_delay_spin_ss`/`_auto_btn_ss` flat(투명+테두리), meas_cb/ch `_flat_cell`. Reference 섹션도 중립 테두리+파란 정체성 유지+flat 콤보+모노. **검증**: TF 패널 실물렌더·selfcheck 37/37·사용자 승인("지금 좋아"). 라이브 start/stop HW 확인 남음.

- [x] `[UI리디자인/v1.9]` **TF Signal Generator N2 리디자인 (direction C + 통합, 2026-07-05)** — TF 오른쪽 패널 신호발생기를 N2로. 사용자와 방향 3안(플랫/서브틀보더/세그먼트트랙) 목업 비교 → **C(세그먼트트랙 계열)** 선택. ①그룹박스 제목/박스테두리 제거 → **캡스 헤더(`_n2_group_header`: 액센트바+대문자+하어라인)** ②컨트롤=서브틀 보더 셀(파란 그라디언트/필 폐지), Level 값 흰 모노 ③Play=회색 채움 필+재생/정지 아이콘(`_style_sig_play`, `_apply_txn` 10여곳 교체) ④**타입(Pink/White/Sine/Sweep/File) 드롭다운 통합**(`_sig_type_menu` QMenu, 기존 `_CheckBtn`은 상태보관용 숨김·isChecked 로직 보존, `_update_sig_type_label`) + **Level 옆 한 줄 배치**(5줄→3줄) ⑤OUT 드롭다운 **flat 셀 모드**(`RoundComboBox._flat_cell`: Fusion 회색배경 제거→투명+테두리) ⑥`_CheckBtn` 세그먼트 활성필=스타일시트 `:checked{background}`(네이티브 체크렌더가 텍스트 좁게 감싸던 것 회피). 겹침(최소폭>패널폭) 조정. **검증**: TF패널 실물렌더·selfcheck 37/37. **⏳측정 카드는 다음**(같은 결). 미커밋→커밋.

- [x] `[UI리디자인/v1.9]` **상단 헤더 바 + Grp N2화 (2026-07-05)** — 상단 브랜드 헤더 컨트롤(Standby·Light·언어·Preset·저장·삭제·Calibration)이 파란 그라디언트 필/테두리로 남아있던 것 → **N2(테두리리스+hover 배경+중립 아이콘)**로 통일(`_apply_theme` 헤더 블록). 캡쳐 드로어 `+ Grp` 버튼도 파란 액센트-틴트 필 → 흰 글씨+hover(사용자 "파란불 싫어"). Preset 드롭다운도 테두리리스.

- [x] `[버그·기능/v1.9]` **스펙트럼 카드 색상 변경 버그 수정 + 카드별 색 변경 (2026-07-05)** — 버그: Color 버튼이 전역 `_custom_color`(그래프 그라디언트)만 바꾸고 **primary 카드 스와치는 `T('spec_line')` 고정**이라 안 바뀜, 추가 카드(`_MC_COLORS`)엔 아예 미연결. 수정: ①primary 카드 색=`bar_top()` 실제 그래프색 연동 ②`_SpecCard.set_color()` 신설(체크박스·Start점·번호·선택틴트 갱신) ③`_open_color_picker(card_id=None)`=front 카드(툴바 Color) 또는 지정카드 색 변경 ④카드 우클릭 **"색 변경…"** 메뉴(`color_requested` 시그널) ⑤추가카드 색 저장·복원(spec_sources 'color'). +UI: 색점=Start를 왼쪽 체크박스에 붙임(간격 축소). **검증**: `set_color` 렌더(빨강/노랑 갱신)·selfcheck 37/37·라이브 색변경 사용자 승인("완전 잘돼").

- [x] `[요청·기능/v1.9]` **스펙트럼 카드별 Start (측정 소스 개별 on/off) — LED 파워 점 (2026-07-05)** — 사용자 요청. 각 INPUT 카드(primary+추가)를 개별 측정 시작/정지(공유 AudioEngine 장치별 구독을 카드마다 열고 닫음). **UI**: 카드 헤더 dB값 옆 LED 파워 점(꺼짐=희미한 링/라이브=파란 글로우, `_led_power_pm`, 옵션11 확정). **코어 리팩토링**: `_card_start/_card_stop/_card_toggle`+`_open_primary_sub`/`_close_primary_sub`/`_spec_teardown`/`_refresh_running_ui`. 전역(툴바Start·상태·엠프티·SPL)=첫소스ON/마지막OFF, `_spec_running()`=아무카드나 실행중. **전역 툴바 Start 버튼은 제거**(카드 LED로 대체, 사용자 요청) — 객체는 상태/테마 참조용 숨김 유지, **S키**로 `_toggle`(전부 시작/정지) 유지. ✓(표시)와 Start(측정) 별개. 새 카드=정지로 추가. dev/ch 변경은 그 카드 실행중일 때만 재구독. **검증(라이브 HW)**: 로그 `[DIAG] spec_card_start/stop` 교차확인 — 카드별 독립토글·마지막정지 any_run=False+teardown·first정확·eng_add n_subs=3 공유·chunks 연속·예외0. selfcheck 37/37. 커밋 `2e90181`.

- [x] `[버그·UI/v1.9]` **N2 글꼴 불일치(SF Pro) → Optima 통일 + 카드 파란 포커스 링 제거 (2026-07-05)** — 사용자 "전체 글꼴 통일 안 됨" + 카드 파란 선 지적. ①**글꼴**: `_n2_val_font`/`_n2_caps_font`/`_N2Tab` 라벨/`_uw_font`/페인트 글리프가 `QFont()`를 **패밀리 없이** 생성 → `setFont` 시 상속된 앱 폰트(Optima)를 덮어쓰고 **시스템 기본(SF Pro)**으로 떨어짐 → N2 툴바·탭이름·컨트롤만 SF Pro, 나머지 Optima라 어긋남. 전부 `QFont(FONT_FAMILY)` 명시로 통일(숫자값 Menlo 모노 유지). ②**파란 포커스 링**: 카드 체크박스·장치/채널 콤보에 `setFocusPolicy(NoFocus)`. **검증**: selfcheck 37/37, 탭바·툴바·패널 렌더 Optima 통일 확인. 커밋 `35827c5`. ⚠️교훈: setFont용 QFont는 항상 패밀리 명시.

- [x] `[UI리디자인/v1.9]` **오른쪽 패널·마이크 카드·캡쳐 드로어·툴바 배치 N2 정합 (2026-07-05)** — 툴바/탭 N2화(위 두 항목)에 이어 나머지 UI를 같은 결로 통일. ①**오른쪽 패널(LEVEL/INFO/INPUT)**: 개별 섹션 박스 제거 → **외곽 프레임 하나(`rpFrame`)** + 섹션 헤더 밑 하어라인(`_sec_hairline`) + INFO 값 모노(`_n2_mono_font`). LEVEL 섹션은 사용자 지시로 미변경. ②**마이크 카드(`_SpecCard`)** "E안"(meter-forward): 박스형 테두리 완화(중립 `#34343B`, 선택=밝은 중립+색틴트), dB 값 **큰 모노**, 미터 12px, 카드 가로폭 확대. ③**배경 통일**: 패널/스크롤/카드 배경을 툴바와 동일 `#1C1C1E`(bg2)로. ④**캡쳐 드로어**: 탭=언더라인(메인탭바식, **글자폭 일자 밑줄 QFrame** — border-bottom 곡선렌더 회피, `_dtab_uls`) + `+Grp`·Capture Target 칩 액센트-틴트 + 리스트 배경 통일 + **스크롤 테두리 제거(외곽 둥근모서리 복원)**. ⑤**캡쳐 행 리디자인 "C안"**(배경 위 글씨): 박스 체크박스·구분선 폐지 → 이름=곡선색(선택행만 흰 볼드), **색 점=표시/숨김 토글**(안티앨리어싱 페인트 원 `_cap_dot_pm`, 채움=표시/링=숨김), 선택행=폭전체 틴트+좌측 액센트 바. ⑥**아이콘 보강**: `palette`·`clock`·`panel-left`·`panel-bottom` 등록(참조됐으나 미등록→빈아이콘 버그 수정). ⑦**Log/Lin 스케일 FFT 뷰에서만 노출**. ⑧**툴바 순서 3탭 일관 재배치** — 원칙: *파라미터는 종류별 인접, 동작 버튼은 파라미터 밖 한 묶음*. Spectrum=`Start │ View +Spectro SR │ DB RANGE │ PEAK HOLD SPEED │ Reset Capture │ Color`, TF=`ENG FFT RESP SMTH │ dB │ IR UNIT PHASE │ Find Capture Δ Stable 🎧`, Stereo=이미 부합(변경없음). **검증**: selfcheck 37/37, 3탭·드로어 실물 렌더 확인, 앱 무오류 기동. `_make_row` `:7686`, 툴바 `sp0`/`tb` 재구성.

- [x] `[버그·DSP/v1.9]` **TF 딜레이 시간영역 정렬 (Smaart식) — magnitude가 딜레이 적용시 올바른 레벨로 스냅** — ✅**primary 실측검증 완료(2026-07-04, 사용자 "이거야")·커밋 ff88cb8** / ⏳**추가카드는 HW 검증 대기(2026-07-06 월, 인터페이스 필요)**. **문제**: 기존 딜레이는 `H_disp = H_raw·exp(jωτ)` **표시 위상만 회전**하는 가짜 보정 → ref/meas를 ΔT=0로 FFT해서 **magnitude·coherence는 어긋난 채**(특히 MTW 고역 짧은 윈도우 디코릴레이션). 위상만 딱 정렬되고 매그는 딜레이와 무관하게 느린 평균으로만 흘렀음. Smaart는 FFT 이전 시간영역서 측정신호를 시프트(정렬). **수정**: ①`_align_pair(ref,meas,D)` — 딜레이 D샘플만큼 ref 지연(앞에 0, 길이유지)해 정렬, 콜백(`_on_frame`)서 FFT 이전 적용. ②표시는 잔여(sub-sample) 위상만 회전(`_render_primary_H`). ③IR은 `np.roll(h, D)`로 임펄스를 실제 도착위치 복원. ④딜레이 변경시 평균 재시드(`_on_delay_changed`→`_reset_avg`)로 즉시 스냅. ⑤**추가카드도 적용**(`_extra_ffts` 헬퍼, 4경로: 같은장치·mc·diff-device meas / `_on_extra_delay_changed`서 그 카드 acc만 재시드). delay=0이면 완전 no-op. **검증**: 합성 통합테스트 mag오차 2.5dB→0.01dB·위상 17.6°→0.1°·IR피크 정확위치, selfcheck 33/33, primary 실측 승인. **⚠️추가카드 라이브 검증 남음(월요일 인터페이스)**: 물리 REF 채널 + 측정 마이크 2개+로 primary+extra 동시측정 → 각 카드 딜레이 적용시 magnitude 스냅·IR 위치·delay=0 동일성. **별개 발견(기록만)**: 추가카드는 **내부 SigGen ref로는 안 돎**(레벨 안뜸) — 내부 ref가 primary(`_on_ref`/`_int_ref_buf`)에만 연결, extra는 물리 ref 채널(`_extra_ref_fft`) 필요. 맥북 내장(SigGen+모노마이크)선 추가카드 테스트 불가 → 인터페이스 필수. 원하면 내부ref→추가카드 연결도 별도 개선 가능(사용자: 지금은 보류). *(합성검증 인라인)*

- [~] `[버그·DSP/v1.9]` **TF 응답(크기 곡선) 대칭화 (Smaart식)** — ⏳**HW 검증 대기(2026-07-06 월, 인터페이스+실스피커 필요)**. **문제**: 딜레이와 무관하게 **레벨을 줄였을 때 크기 곡선이 원래 값으로 너무 느리게 내려옴**(Fast/Normal 무관, 사용자 "너무 늦게 내려온다"). 원인=비대칭 밸리스틱 — 상승은 빠르고 하강은 fast-release 알파(`_TF_RELEASE_ALPHA`)로 별도 처리하다 실제론 느린 EMA만 지배. **수정**: 상승·하강 **완전 대칭 EMA**(fast-release 로직/`_TF_RELEASE_ALPHA` 제거) — MTW 푸시(`self._sxy/_sxx/_syy`)·Single 렌더(`_cross_acc/_auto_acc_x/_auto_acc_y`) 양 엔진 모두 `b=1-a; acc=b·acc+a·new`. Response 시정수 짧게 재조정 `TF_AVG_SEC=[0.5,1,2,4,8]`(Fast0.5·Quick1·**Normal2**·Smooth4·Stable8초), 기본 Normal=2초(`averaging_sec=2.0`, `avg_cb` 인덱스). **검증**: pytest 36·selfcheck 34/34 통과. **⚠️실측 남음**: 내부SigGen+맥마이크론 위/아래 대칭·2초 체감 검증 불가(사용자 판단) → 인터페이스+실스피커로 레벨 올렸다내렸다 하며 상승·하강 같은 속도·2초 적절성 확인 후 값 튜닝/확정. 상세 메모리=`project_v19_response_symmetry`.

- [x] `[버그·UI/v1.9]` **짧은 패널 축 라벨 겹침 + TF 제목 클릭영역·크기 (2026-07-04, 실측 확인)** — 사용자 실사용 중 발견 3건 수정·커밋(`2460330`+`25079f6`). ①**축 눈금 라벨 겹침**: 패널을 짧게 줄이면 세로축 라벨이 고정 간격이라 서로 뭉치고(Mag 3dB마다 +15…-15, Phase 30°마다 150°…-150°) IR 바닥 미터축이 카드 프레임에 겹침. **수정=그리드 선은 유지, 텍스트만 최소 픽셀간격(폰트높이+2) 그리디 솎아냄**(Mag `_build_cache`·Phase `_build_cache`·IR lin/dB 루프 + 스펙트럼 FFT/Octave `_lg/_last_ly`) + **IR/Phase PAD_B 20/24→28**(Mag과 통일, ±1.0·미터축이 둥근 프레임 위로). 140/90px 코너 확대 렌더로 확인. ②**제목 클릭영역 어긋남**: 앞서 PAD_T 10→16으로 제목 baseline이 y=29로 내려갔는데 핫스팟(`_TFTitleHotspot`)이 250×26 그대로라 제목 아래절반을 못 덮어 좌상단 모서리만 메뉴가 떴음 → **핫스팟 285×34로 확대**(오프스크린 childAt로 제목 전체 핫스팟 검증). ③**제목 3pt 확대**: 전용 토큰 `CF_TF_TITLE=CF_MODE+3`(=11) 신설, IR/Phase/Mag 제목에 적용(옥타브 제목·IR 딜레이 라벨은 CF_MODE 유지). selfcheck 34/34.

- [ ] `[요청/v1.9]` **실시간 공간 평균화 (마이크 여러 개 동시 TF 측정 → 평균 곡선)** — 여러 측정 마이크(측정 카드)를 **동시에** 띄워놓고 라이브로 평균낸 하나의 매그/위상 곡선을 보는 기능. ⭐**기존 것과 구별**: 지금 있는 `_TFAverageDialog`(`:10941`)+`_do_tf_average`(`:13871`)는 **캡처된 정적 곡선**을 사후에 평균내는 것 → 신규는 **라이브 다중 카드 실시간 평균**. **데이터 소스**: primary 측정(`_process_audio` 경로) + extra 카드들(`_extra_pair_acc`, `_render_extra_pairs`)에 이미 카드별 H(크로스스펙트럼)가 각각 누적됨 → 이걸 매 렌더프레임에 **합쳐 평균**. **핵심 설계 결정(다음 세션 논의)**: ①**평균 방식** — 복소 벡터 평균(위상 보존, 같은 음원 여러 위치일 때 표준) vs 매그니튜드 파워 평균(위상 무시, 공간 RMS). Smaart 공간평균은 보통 코히런스 가중 복소 평균. ②**어느 카드를 평균에 넣을지 선택 UI**(체크박스/토글) ③**평균 곡선 표시 방식**(별도 색 곡선 하나 + 개별 카드는 흐리게, 또는 평균만) ④코히런스 가중 여부. **구현 포인트**: 렌더는 이미 `for i,acc in enumerate(_extra_pair_acc)` 구조라 카드 순회는 있음 → 순회 중 H 배열들을 모아 평균 배열 계산 후 별도 곡선으로 그리는 레이어 추가. producer/consumer 30fps 타이머 패턴 준수(콜백에서 평균 계산 금지, `_pending`에 저장). ⚠️카드마다 딜레이/정렬 다르면 위상 평균이 무의미 → 정렬(딜레이 보정) 후 평균해야. 라이브 엔진(MTW)·extra 카드 경로 양쪽 검증 필요.

- [x] `[요청/v1.9]` **dB(세로)축 수동 값 입력 고정** — ✅**구현완료(2026-07-04). 최종 UI=툴바 컨트롤** (on-graph 아이콘 3종 위치 시도했으나 제목메뉴 핫스팟·피크배지·둥근모서리·dB숫자와 다 충돌 → 폐기. 툴바 `dB [Auto/±범위]` 버튼으로 확정, Engine/FFT/Response/Smooth 옆). 대화상자에 [자동] 버튼(해제). 아래는 초기 로직 상세: 그래프 **우클릭 → dB 범위 입력…**(상·하단 타이핑, 뒤집힘 자동교정·최소3dB) → 그 범위로 락(우상단 자물쇠 배지). 메뉴에 자동맞춤·고정토글도. **더블클릭=자동맞춤+락해제(기존 유지)**. 고정 중 `fit_y`/`_auto_fit_y_to_data` 조기 return. **스펙트럼(MainWindow `_db_lock`+FFT/Octave 캔버스)·TF(TFMagCanvas `_db_lock`) 공통**, 설정 저장(spec_db_lock/top/bot·tf_db_lock/top/bot)으로 재시작 유지. 공용 `_ask_db_range` 대화상자·`_db_axis_context_menu`·`_draw_lock_badge`. selfcheck 34/34(dB락 assert 포함)·설정 라운드트립·실앱 무오류. 미커밋→커밋. *(목업 `scratchpad/mock_db_axis.py`·`mock_db_dialog.py`)* — 현재 스펙트럼/TF 세로축은 **더블클릭 시 `_auto_fit_y_to_data`(`:14912`)로 1회 자동맞춤 후 고정** + 핸들 드래그 수동조정. 요청=**정확한 상·하한 dB 값을 직접 입력해 고정**(예: 상단 130 / 하단 30 타이핑 → 그 범위로 락, 이후 자동맞춤·데이터변화에도 안 바뀜). **적용 대상**: 스펙트럼 + TF 매그니튜드(둘 다). **UI 방향(다음 세션 논의)**: ①축 옆/설정에 상한·하한 숫자 입력칸(spin) ②더블클릭 메뉴에 "값 입력…" 항목 ③락 상태 표시. **관련 기존 항목**: G섹션 `[검토/v1.7] 스펙트럼 dB 축 범위 정책`(133행, 라이브 자동확장 논의 → "넉넉한 고정 창" 방향)과 직결 — 수동 고정이 그 요구의 정답일 수 있음. `[차별화/v1.7+] TF 매그+위상 주파수축 줌/팬`(가로축)과도 축 조작 UI 일관성 맞출 것. **구현 포인트**: dB축 범위 상태변수(현재 자동맞춤/드래그가 쓰는 min/max)에 "수동 락" 플래그 추가 → 락이면 `_auto_fit_y_to_data`·자동확장 스킵. 스펙트럼(`MainWindow`)과 TF(`TFMagCanvas`)가 각자 축을 가져 **양쪽에 구현** 필요. 값 저장(settings)해서 앱 재시작에도 유지 검토.

- [x] `[UI/v1.9]` **TF 창 Reference·Measurement 레벨 미터 정렬 (가로폭·세로 위치 통일)** — ✅**구현완료(2026-07-04, B안: Reference도 카드).** 목업 2안(A=R바 들여쓰기 / B=Reference도 카드) 제시 → 사용자 **B 선택**. Reference 섹션(`:11456`)을 측정 카드와 **동일한 `measCard` 카드 박스**(테두리 1px rgba(accent,120)·radius·bg panel·padding 2px, 내부여백 `(6,5,6,6)`)로 감싸고 헤더도 `● Reference`+dB(accent색)로 카드형 통일 → R/M 바가 **동일 컨테이너·동일 내부여백**에 놓여 구조적으로 좌우 정렬(하드코딩 픽셀 아님). **검증(목업 실측)**: R바 x=[32,262]·M바 x=[32,262] **Δleft=0/Δright=0**, selfcheck 32/32 PASS. 카드 파란 테두리로 Reference/Measurement 구분 유지. ⚠️엣지케이스: 카드 많아 스크롤바(6px) 뜨면 M바만 6px 밀림(Ref는 스크롤영역 밖) — 일반(카드 몇 개) 상황은 완벽 정렬, 필요시 스크롤바 거터 예약으로 보강. 미커밋→커밋. *(목업 `scratchpad/mock_meter_align.py`)* — TF 창의 **Reference VU 바**(`_ref_vu_bar`, `_HorizBarVU`, 공유 Reference 섹션 `ref_sl` `:11471`, 앞 라벨 `R` width 10)와 **측정 마이크 VU 바**(`_m_bar`, 각 `_MeasCard` 내부 `:9363`, 앞 라벨 `M` width 10)가 **서로 다른 컨테이너**(Ref=상단 공유 섹션 / M=카드 안)에 있어 **가로폭·세로 위치가 안 맞음** → 나란히 정렬해서 같은 폭·같은 좌우 기준선으로. **방향(다음 세션 논의)**: 라벨 폭·바 마진·컨테이너 좌우 패딩을 R/M 동일하게 맞추거나, 미터 행을 공통 정렬 그리드에 올림. `_HorizBarVU`는 이미 `addWidget(bar,1)` 스트레치라 폭은 부모 컨테이너 폭·마진에 좌우됨 → 두 부모 레이아웃의 `ContentsMargins`/좌우 요소 폭을 일치시키는 게 핵심. ⚠️Ref는 카드 1개(공유), M은 카드 N개라 카드 들여쓰기/테두리 유무 차이도 정렬에 영향 → 실제 렌더로 눈으로 맞춰야(목업 비교 후 확정). 미적 변경이라 [[feedback_propose_ui_options_before_implementing]] 적용(구현 전 목업 제시).

- [x] `[버그·성능/v1.9]` **캡처 많이 쌓이면 여전히 버벅임** — ✅✅**근본수정 완료(2026-07-04, 증분 컴포지트).** **프로파일로 원인 확정**: `_build_cap_img`가 캡처 1개 바뀔 때마다 **전체 N개를 재빌드**(~1ms/캡처, 100개=100ms·200개=200ms). bg 스레드지만 `QPointF` 구축이 GIL을 잡아 메인 UI 스레드 정지 → 캡처 추가(`add_capture`)·클릭선택(`bring_to_front`)마다 프리즈. **수정=2층 캐시**: `_cap_pix`를 "보이는 캡쳐 전부 dimmed" 베이스로 재해석 + front 강조는 paintEvent 오버레이(`_draw_cap_curve(emph=True)`, 재빌드 없이 1곡선). **추가=`_append_cap_incr`로 새 곡선 1개만 베이스에 얹어 O(1)**, 선택변경=베이스 유지. 캐시키에 캡쳐 개수 포함 → bg 재빌드 중 추가돼도 다음 프레임 자동복구(레이스 방지). **5개 캔버스 전부 적용**(FFT/Octave/TFPhase/TFMag/TFIR — 머신러리 동일, `_build_cap_img`만 좌표 상이). **검증(벤치마크)**: 증분 add=0.2~1.6ms(N무관, 기존 O(N) 22~137ms), bring_to_front=0.002ms(베이스 유지), selfcheck 32/32, 5캔버스 시각 정합(dimmed+front강조)·레이스 복구·실앱 실행 무예외. 잔여: 축변경/삭제/가시성토글은 여전히 전체 rebuild(빈도 낮음, dB축 고정 항목이 축변경 줄임). 미커밋→커밋. *(프로파일러 `scratchpad/prof_captures.py`·벤치 `bench_all_canvases.py`)* — 캡처가 많아지면 렌더 끊김. **이미 최적화 되어있는 부분**: 캡처 곡선은 백그라운드 스레드에서 픽스맵으로 캐싱됨(`_trigger_cap_build`→`_bg_cap_build`→`_build_cap_img` `:2905~2960`, 세 캔버스 각자 보유 — FFTCanvas/OctaveCanvas/TFMagCanvas), 캐시키=`(W,H,db_max,db_min,scale_log,ny,front,live_on_top)`. **→ 그런데도 버벅이면 후보(다음 세션 systematic-debugging + 실데이터 프로파일링)**: ①**캐시 무효화 빈도** — 라이브 중 db_max/db_min 자동맞춤·축 변동으로 캐시키가 자주 바뀌면 매번 O(캡처수×점수) 재빌드. (☝️v1.9 dB축 수동고정 항목이 이걸 줄여줄 수도) ②**QPolygonF 생성 비용** — `_build_cap_img`가 `[QPointF(x,y) for ...]` 파이썬 리스트컴프로 캡처마다 점 배열 생성(`:2945`) → 캡처수 늘면 파이썬 루프 선형증가, np→QPolygonF 벡터화(setPoints/버퍼) 검토. ③**캡처 드로어 리스트 UI** — 캡처 행 위젯이 많아질 때 레이아웃/페인트 비용(리스트 가상화·행 캐시 검토). ④build은 bg지만 `_cap_built.emit()`→메인스레드 `QPixmap.fromImage`+`update()`가 큰 이미지에서 무거울 수 있음. **⚠️먼저 재현·프로파일**: 캡처 N개(20/50/100)에서 어느 지점이 느린지 `[DIAG]`/타이밍 계측 후 원인 확정하고 손댈 것. *(관련: [[project_perf_draw_live_grad_deferred]] — "버벅인다" 하면 라이브 그라디언트 펜/브러시 캐시부터 점검)*

- [x] `[요청/v1.9]` **캡처 임포트 (익스포트한 CSV를 다시 불러 캡처 화면에 표시)** — ✅**구현완료(2026-07-04, TF, IR 포함).** 드로어에 **Import 버튼(upload 아이콘, Export 옆, TF모드)** 신규 + `TransferFunctionWindow._import_tf_captures()`(QFileDialog→CSV 파싱→label별 그룹핑→Mag/Phase/Coherence 복원 + `_tf_captures`/3캔버스 주입, `_auto_capture_color` 자동배정). **⭐IR도 복원**: 어제 "CSV에 IR 없어 불가"였는데 **크기+위상 → 복소 H=mag∠phase → 역FFT**로 재구성(`_ir_from_mag_phase`, 로그격자를 real/imag로 균일격자 복소보간 후 irfft). **검증**: 라운드트립(지연 오차 0ms·형상 상관 0.90) + 실 CSV(`tf_captures_*.csv`) 파싱→3캔버스 주입→렌더(Mag/Phase 정상, IR 0ms 임펄스+감쇠 확인), selfcheck 32/32, 실앱 무예외 실행. 한계: 로그격자라 고역 성긴 반사는 뭉툭해짐(내보낸 데이터 정보량 한계). 미커밋→커밋. **실앱 버튼 클릭 검증만 사용자 대기**(로직 전부 검증됨). *(테스트 `scratchpad/test_import.py`·`ir_reconstruct_test.py`)* — 지금은 캡처를 **내보내기만** 됨(`_export_tf_captures` `:13819`, 드로어 Export 버튼 `:6970`/`_on_drawer_export` `:19977`). 요청=**내보낸 파일을 다시 불러와 캡처로 복원·표시**. **익스포트 포맷(재활용)**: `tf_captures_<stamp>.csv`, 컬럼=`capture, freq_hz, mag_db, phase_deg, coherence`, 여러 캡처를 `capture`(label) 컬럼으로 구분(`:13842`) + 별도 Mag/Phase/IR PNG. **구현 방향(다음 세션 논의)**: ①CSV 파싱→label별 그룹핑→캡처 dict 재구성(mag_cvs=`{f,mag,coh}`, phase_cvs=`{f,ph_wrap}`, `_tf_captures` 메타=label) → `add_capture`/캔버스 `_captures`에 주입 + 드로어 리스트 행 추가 ②색상 자동배정(라이브/기존 캡처와 안 겹치게, [[project_todo_capture_color_avoidance]] 참고) ③드로어에 **Import/불러오기 버튼** 신규(Export 옆). ⚠️**한계**: IR은 CSV에 없음(PNG만) → 임포트 캡처는 **Mag/Phase/Coh만 복원, IR 곡선 없음**(불러온 캡처는 IR 패널 미표시 또는 빈값 처리 결정 필요). ⚠️현재 익스포트=TF 전용 → 임포트도 TF부터. 스펙트럼 캡처 익스포트/임포트는 별건(현재 스펙트럼 캡처 익스포트 경로 확인 후 결정). 파일 선택은 `QFileDialog`(익스포트 폴더선택 패턴 재활용).

## 💤 G. 나중에 / 우선순위 낮음

- [x] `[v2.0 재개]` **FOH 글랜스 쇼 모드** — ✅**재개 완료(2026-08-29, `d7aab6e`).** 진입점(View 메뉴 'Show Mode (Full Screen)' + ⌘⇧F) 복구, 헤드라인 평활을 **프레임율 무관 시간기반 EMA**로 다듬음(튜닝 지점=`ShowModeWindow._HEADLINE_TAU=0.7초`, spectra/ui/spl.py). 풀스크린 거대 SPL(한계 대비 초록→노랑→빨강 신호색) + 라이브 옥타브 스펙트럼 + PEAK/LEQ/HEADROOM 칩, SPL 알람 설정 라이브 연동. showmode 렌더 눈으로 확인. **⏳실측**: 실오디오로 헤드라인 체감 속도(_HEADLINE_TAU) 확인 후 필요시 한 줄 튜닝. **남은 다듬기(선택)**: 자체 ⚙(지표·한계 독립), 멀티모니터 배치. (원구현: e65aa8e·4acb4a8·4d9bd29)
- [x] `[차별화·Smaart/v2.0]` **코히런스 블랭킹** — ✅**완료(2026-08-29, `9631621`+`947910d`).** TF Magnitude 크기곡선을 코히런스(신뢰도)에 비례해 진하게/흐리게(**연속 페이드**, 옵션 B — 목업 4종 숨김/페이드/회색/고스트 중 사용자 선택). 고코히=불투명, 저코히=흐림(끊김 없음). `TFMagCanvas._draw_curve_coh`(알파를 코히로 매핑, 11단계 양자화+세그먼트 배칭으로 라이브 perf 유지, 튜닝상수 `_COH_FADE_LO/HI/MIN`). primary+extra 카드 곡선 적용, AVG 오버레이는 진하게 유지. **우클릭 메뉴 '코히런스 블랭킹' 토글 + `tf_coh_blank` 설정 저장/복원(기본 ON).** selfcheck ON/OFF 렌더 추가(42/42), 육안 확인. ⏳실측: 실측정에서 페이드 강도(_COH_FADE_*) 체감 후 필요시 튜닝.
- [ ] `[차별화/v1.7+]` **SPL 컴플라이언스 풀세트** — 신호등 알람 바(✅완료, C섹션)에 이어 ①**디스크 로깅**(LAeq/LCeq/LAFmax/LCpeak 시간스탬프 CSV, 캡쳐 Export writer 재활용) ②**SPL 히스토리 그래프**(시간축 dB, 라우드니스 레이더의 분/시 스케일판) ③**리포트**(세션 요약 PDF/PNG+CSV, 한계 준수/초과횟수). 공연장 소음규제(DIN 15905-5 등) 준수 증빙 → "라이브 튜닝"에서 "컴플라이언스 측정기"로 시장 확장. 엔진(dBA/dBC/LEQ/Peak)·CSV/PNG Export·색임계는 이미 있음 → "기록·표시·출력 껍데기"만 신규. Class 1/2 법적정확도는 캘리브 마이크+검증 별도.
- [x] `[차별화·Smaart/v2.0]` **THD 측정** — ✅**완료(2026-08-29, `4757f70`).** 스펙트럼 커서 리드아웃에 `THD n%` 한 줄(옵션 A, Smaart식). **우클릭 'Show THD' 토글로 켜야 표시**(평소엔 깔끔, 사용자 요청). `dsp/weighting.thd_from_spectrum`(기본파 커서스냅→배음 k·f0 피크→sqrt(Σ배음²)/기본파, 골든테스트). FFT·RTA 양쪽, `spec_show_thd` 저장/복원. selfcheck 43/43. ⏳실측: 실사인+인터페이스로 정확도 체감. **(구 메모)** THD 측정 (사용자 검토중 2026-06-16) — 순음(사인) 입력 시 출력의 배음(2·3·4…차) 합÷기본파 = 총고조파왜곡. **위치 = 스펙트럼 탭(RTA, 단일채널)** — TF 매그니튜드 아님(TF는 선형 이득만, 왜곡은 코히런스↓로만 보임). **Smaart 방식 표기**: RTA 상단 **커서 리드아웃에 `THD: n%` 한 줄**(커서를 기본파 봉우리에 올리면 그 주파수 기준 실시간 계산), 조건=Show THD 켜기 + 1/12옥타브 이상 해상도. SPECTRA에 이미 ①사인 제너레이터 ②상단 커서 리드아웃(`▲ 62Hz 75.2dBSPL`)이 있어 거기에 THD 한 줄 덧붙이면 동일. 옵션으로 배음별 dB 패널·포함차수 선택 추가 가능(시안 렌더함). ⚠️제너레이터가 현재 TF탭에 있어 스펙트럼탭과 공유/외부소스 필요. %↔dB 둘다(−47dB≈0.44%). **사용자 v1.7 확정(2026-06-20)** — v1.6.1 쇼모드 작업 마무리하며 THD는 v1.7 할일로 넘김.
- [ ] `[차별화/v1.7+]` **MTW 멀티해상도 측정** — 라이브 연속측정 해상도를 Smaart급으로(저역 긴창/고역 짧은창 동시→전대역 균일 points/octave). **모드 토글로 추가**(단일FFT 폴백 유지). ⚠️측정 심장부 작업+하드웨어 A/B 검증 필수, CPU 부담 체크. **지금은 안 함**(서명·보호·버그·정확도증명이 우선). 정밀측정은 이미 스윕 모드로 풀해상도 됨. 사용자/매출 생기고 프로 라이브 정면경쟁 시점에.
- [ ] `[차별화/v1.7+]` **탭을 별도 창으로 분리** (사용자 검토→v1.7 보류 2026-06-17) — 3탭(Spectrum/TF/Stereo) 중 하나를 독립 창으로(듀얼모니터). **방식 A=버튼 분리(쉬움) / B=드래그 떼어내기(Safari식, 높음)**. ⭐**핵심 난관=드래그가 아니라 구조**: 한 탭이 main_stack(그래프)+sub_stack(전용 툴바)+우측 사이드바(LEVEL/INFO/INPUT) 세 곳에 흩어져 있어 "한 덩어리"가 아님 → Spectrum/Stereo는 자체완결형으로 **리팩토링 필요**(A·B 공통). **예외=TF는 이미 `TransferFunctionWindow(embedded=False)` 독립 창 클래스라 쉬움.** 추천 단계: ①A+TF만(50~100줄, 가성비 최고) ②나머지 리팩토링+드래그는 추후. 오디오는 공유엔진(다중구독)이라 분리해도 측정 유지. ⚠️macOS 프레임리스+Space 실측 부담(풀스크린 자식창 이슈와 동류).
- [x] `[요청/v1.9]` **TF 매그+위상 주파수축 줌/팬** — ✅**구현완료(2026-07-04).** 프로툴(REW/Smaart) 리서치 후 조작맵 확정. **공유 믹스인 `_TFFreqZoomMixin`**(매그·위상 상속)이 줌상태 `f_lo/f_hi` + 좌표매핑 `_fx`/`_xf` + 이벤트 보유. **조작**: 휠/핀치=커서기준 확대·축소, 그냥드래그=박스줌(러버밴드), Shift+드래그/트랙패드 두손가락=팬, Cmd±=단계(×1.4), 더블클릭/Cmd0=20~20k 리셋. **연동**: 창 `_sync_freq_zoom`(`_fzoom_cb`)으로 매그·위상·코히런스 같은 f_lo/f_hi. 곡선·그리드·커서·캡처·보조그리드 전부 줌 따라감(확대 시 `_fz_marks`로 라벨 조밀). 좌표 ~15곳 self._fx/_xf 전환(스펙트럼 `ny=20000` 무영향), `freq_to_x`/`x_to_freq`/`draw_freq_minor_grid`에 f_lo 파라미터 추가(기본20). 최소스팬 클램프(≈1/7옥타브). IR은 시간축이라 제외. **검증**: 헤드리스 줌매핑(100→왼끝/500→오른끝)·클램프·매그↔위상 연동·박스/팬/휠 로직 + Phase·Mag 줌 렌더 + selfcheck 33/33(줌체크 추가) + 실앱 무예외. 계측 `[DIAG] tf_fzoom lo= hi=`. 미커밋→커밋. **실앱 실조작(스크롤/드래그) 육안만 사용자 대기.** *(테스트 `scratchpad/test_fzoom.py`)*
- [x] `[차별화·Smaart/v2.0]` **TF 레벨바 Peak 표시** — ✅**완료(2026-08-29, `de290a8`+`b6ccd89`).** TF Ref/Meas 레벨바(_HorizBarVU)에 채움=RMS(평균)+흰 틱=Peak(순간최대) 동시표시(목업 3종 RMS만/Peak만/RMS+틱 중 사용자 'C' 선택). 진짜 샘플피크(_pk)는 이미 set_rms(rms,peak)로 유입 → 표시만. **Peak-hold(1.4s)+느린하강(6dB/s)**로 틱 튐 완화(_PK_HOLD_S/_PK_DECAY, _HorizBarVU 전용). 라이트/다크 대응. selfcheck 레벨미터 틱분리 검증. **(구 검토메모)** TF 레벨바 Peak 표시 옵션 (Smaart 정합) — 현재 TF Reference/Meas 바는 **RMS dBFS**(`20log10(RMS)`, `_HorizBarVU` `wayaudo2.py:7759`). Smaart I/O 미터 기본은 **Peak** 표시라 같은 신호도 숫자가 다름(사인 ~3dB, 핑크노이즈 10dB+). **검토: Peak 모드 추가**(RMS↔Peak 토글 또는 Peak+RMS 동시) → Smaart와 숫자 직접 일치. 디지털 클립 감지(피크 0dBFS 근접)에도 Peak가 더 적합. 색임계(−9/−3)는 Peak 기준이면 재조정 필요. 레벨 값 정합성 검증 시 Smaart를 RMS모드로 맞췄던 불편 해소. *(2026-06-17 사용자 요청)*
- [ ] `[요청]` TF 탭에 옥타브/FFT(RTA) 뷰 추가 — Spectrum 그대로 두고 TF 위상/매그 창에서 선택 토글(Smaart식). ✅구현+버벅임 해결(2026-06-18, producer/consumer 분리).
- [ ] `[요청/v1.7]` **RTA 측정 마이크(카드) 선택** (사용자 요청 2026-06-18) — 현재 RTA는 TF **1번(primary) 측정 카드**의 장치/채널(`meas_cb`/`meas_ch_cb`, `wayaudo2.py:10039`)만 구독(`_rta_subscribe` 11330). TF에 측정 카드 3개면 2·3번은 RTA에 못 띄움. **요청=카드 선택형**: RTA 칸에서 어느 측정 카드를 볼지 고르게(예: RTA 제목 `▾` 메뉴에 활성 measurement 카드 목록 → 선택 시 그 카드의 dev/ch로 재구독). 구현 포인트: ①현재 활성 meas 카드들(primary `meas_cb` + extra cards `_spec_extra` 대응 TF판/`_on_extra_meas` 소스) 열거 ②선택값 상태변수(`_rta_card_idx` 등) ③`_rta_subscribe`가 primary 대신 선택 카드의 dev/ch 사용 ④`_update_rta`의 device-change 재구독도 선택 카드 기준으로. Spectrum 탭 마이크와는 무관(RTA=TF 측정 입력).
- [ ] `[v1.7 정밀검증]` **멀티모니터 풀스크린 팝업 드래그 동작** — 1.6에서 1차 해결(`_float_sync_attach`: 풀스크린+같은화면일 때만 자식부착, moveEvent로 화면 넘나들면 자동 분리/재부착, 창모드=항상 독립). 사용자 "일단 해결, 1.7에서 디테일 체크". ⚠️실외부모니터에서 ①창모드 외부이동 ②외부에 먼저두고 풀스크린(확실) ③풀스크린 위 미터를 외부로 드래그(best-effort, macOS 자식창 드래그 화면밖 허용 여부 의존) 세 경우 + 재부착 매끄러움 정밀 확인. *(상세: [[project_macos_float_over_fullscreen]])*
- [x] `[해결]` **플로팅 창 풀스크린 동작 — 자식 창 전환(2026-06-17, 사용자 선택)** — 독립 창(parent=None)+collectionBehavior로는 메인 풀스크린일 때 ①풀스크린 크기로 뜨거나 ②별도 Space(검은화면)로 새는 문제 지속. 사용자 "풀스크린 위에 확실히" 선택 → **SPL미터·알람을 메인의 자식 창**(`super().__init__(parent, Qt.Window)`)으로 전환 → macOS 자식 창이 부모를 풀스크린까지 추종, 같은 Space 위에 정상 크기로 뜸. 워크어라운드(`_set_float_above_fullscreen`·`_force_float_normal`) 제거. **트레이드오프(수용)**: 메인 최소화 시 미터/알람도 함께 숨겨짐(독립 생존 포기). 벡터스코프/라우드니스 팝아웃은 아직 독립(_show_popout_normal) — 필요시 동일 전환 검토. *(상세: [[project_popout_fullscreen_fix]])*
- [ ] `[UI/v1.7+]` **팝업창 둥근 모서리(매끄럽게)** — 사용자 요청(2026-06-17). 시도·실패 기록: ①`setMask`(폴리곤/QBitmap 둘 다)=1비트라 안티앨리어싱 불가 → 코너 계단형(깨져 보임). ②`WA_TranslucentBackground`+코너 clear 오버레이=Qt가 **모든 stylesheet 배경을 안 그려** 콘텐츠가 통째로 비침(더 깨짐). **결론: 매끄럽게 하려면 각 팝업의 배경을 stylesheet가 아니라 위젯 paintEvent로 불투명 라운드 직접 그리도록 전면 리팩토링 필요**(SPL미터/알람/팝아웃/다이얼로그 전부). 큰 작업이라 보류, 현재는 각진 모서리 유지. **⭐권장 방향(2026-06-17)**: 프레임리스로 직접 둥글게 말고, **메인창처럼 네이티브 프레임 유지 + 다크 appearance**(titlebarAppearsTransparent + fullSizeContentView + NSWindow dark) 방식으로 팝업 타이틀바 재구현 → OS가 둥근 모서리·그림자 공짜로·매끄럽게. 대가: ✕ 대신 macOS 신호등 버튼, ⚙·자물쇠 커스텀버튼은 콘텐츠 영역으로 이동. 메인창이 이미 이 방식이라 됨(네이티브=OS가 그림 vs 프레임리스=앱이 그림).
- [x] `[요청]` 딜레이 m 거리환산 표기 — ms↔m(음속 343). ✅✅**구현+실앱 검증완료(2026-06-22)**. **1단계 통합**: 흩어진 ms 표기 6곳을 단일 포맷터 `fmt_delay()`+`ms_to_m`/`m_to_ms`+전역 `_SOUND_SPEED`/`_DELAY_UNIT`(`wayaudo2.py:386~`)로 통합. IR 마커/커서/시간축, All Delay Finder, Delay Finder 음속 한 소스. **2단계 토글 UI**: TF 툴바 IR 콤보 옆 **Units 콤보**(`ms`/`ms·m`/`m`, `unit_cb`)→`_unit_changed`가 `_DELAY_UNIT` 바꾸고 `_refresh_delay_unit`(ir_cvs.update + 카드 m라벨). 측정 카드 딜레이칸 옆 **= N.NN m 라벨**(`_MeasCard._update_m_lbl`, m/both일 때만). 기본 ms라 출력 byte-identical, 구문+로직테스트 PASS. ✅**실앱 육안 확인(2026-06-19)**: Units 콤보 ms/ms·m/m 전환 시 IR 시간축(m모드 -3.4m…3.4m 환산정확)·카드 딜레이칸 옆 `= N.NN m` 라벨 정상. ✅**측정 켠 상태 IR 마커/커서 `ms·m` 둘다표기도 실앱 확인완료(2026-06-22)** — 잔검증 종결. (보류: View메뉴 미러—MainWindow↔TF 커플링이라 보류. 그룹딜레이 제외.) *(상세: project_todo_delay_unit_m)*
- [ ] `[검토/v1.7]` **스펙트럼 dB 축 범위 정책** — 현재=캘리브 시 신호 피크+12dB로 **1회만** 자동맞춤 후 고정(`_auto_fit_y_to_data` `wayaudo2.py:14912`). 이후 더 큰 소리(예: 120 dBC)가 와도 자동확장 안 돼 윗선에서 잘림 → 핸들 수동 드래그 필요. 라이브 표준은 "넉넉한 고정 창"(천장 충분히 높게, 큰 소리 안 잘리게). **1차 시도(라이브 상향 자동확장: 윗선 3dB 근접 시 천장↑, 아래로는 안 줄임)했다가 사용자가 "원래대로, 다른 사람 의견 듣고 1.7에 결정"으로 되돌림.** 천장은 캘리브 천장(0 dBFS=측정가능 최대 SPL)에 종속 — 120 측정하려면 캘리브 자체가 그만큼 높아야 함(코드무관). 의견 청취 후 정책 확정.

---

## ✅ 완료 (최근, 릴리스 노트 재활용)

- **2026-06-26 M4 HW 검증 세션 (브랜치 `chore/v17-hw-verify-diag`, 미커밋)**:
  - 🐛✅ **[버그] Adaptive(MTW) 엔진 멀티카드 분석/곡선 안 뜸** — `_render_inner`가 MTW 분기서 조기 return → extra 카드 렌더 루프 미실행. `_render_extra_pairs()` 분리로 MTW/레거시 양쪽 호출. 카드2 라이브검증✅(`tf_card_render accn=80`). *(상세: [[project_bug_mtw_multicard_render]])*
  - ✅ **멀티카드 일괄캡쳐**(`added=2`)·**TF 별도창 STEP1**(풀스크린+Spectrum 동시구동 무충돌) 실측 종료.
  - 🆕✅ **[기능] 무끊김 출력 채널 변경** — standalone 출력을 디바이스 전체채널로 열고 콜백이 가변참조(`_sig_out_ch_ref`)로 채널 선택 → 같은 장치 채널변경 시 스트림 재시작 없이 참조만 갱신(끊김 0). 실측 OK(`sig_out_ch seamless=True`).
  - 🆕✅ **[기능] TF 카드 레벨미터 = 카드 Start/Stop 연동(제너레이터 무관)** — (프리 입력 모니터는 만들었다 회귀·복잡성으로 **전부 원복**.) `_on_meas_start`가 입력 스트림을 제너레이터 켜졌을 때만 열던 것 → `_delayed_restart()` 게이트밖으로 빼 제너레이터 OFF에도 카드 Start=미터ON. **핵심: 분석 입력 force_latency를 제너레이터 출력장치는 재생상태 무관 항상 high 고정**(low↔high 플립플롭이 ①제너레이터 끄면 미터 stall ②카드먼저+제너레이터 켜면 제너레이터 멈춤 의 원인). 실측 **"이대로 확정"**.
  - 🐛✅ **[버그·검증중 발견] 채널 변경 시 가짜 USB 끊김** — TF 입력 채널 변경→제너레이터 재시작으로 M4 스트림 churn→콜백 1.5s+ 멈춤→스트림 disconnect 신호가 **장치개수 확인 없이** "USB disconnect(spectrum/TF)"로 오발동→불필요 전체 재초기화(~9초 끊김). **수정:** `_on_device_disconnected`·`_on_tf_disconnect`에 **HAL 장치개수 가드**(개수 안 줄면 무시=churn 오판, 실제 제거는 개수감소로 통과+CoreAudio 리스너 백업). 실측: 채널변경 stale 0·가짜reinit 0, **실제 USB 뽑기는 여전히 정상 감지·복구**(회귀 없음).
  - ✅ **[검증] #1 SPL 임계 알람** 3상태 전이(OK/AMBER/OVER, limit기준 엣지) 로그 확인. **#2 채널버그** RTA 채널변경 생존(ch_chg=True, stale 0) 확인.
  - 🆕✅ **[UI] TF 카드 미터 흰색 peak tick 제거** + **추가 카드 커서 리드아웃을 1번 카드와 동일하게**(coherence γ² 계산 + 교차데이터 저장 → mag뷰 Hz/dB/°/%, phase뷰 Hz/dB/°) + **Spectrum 입력미터 peak tick**(바=RMS, tick=true peak; Smaart 기본 Peak라 RMS보다 높게 보임). 실측 확정. *(상세: [[project_v17_free_input_monitor_seamless_outch]])*
- **2026-06-17 실앱 검증완료(전부 ✅)**: SPL 알람 창·always-on-top 자물쇠(깜빡임0)·마우스떠나면 카드만·단축키 치트시트(?)·라우드니스 True Peak·타겟 즉시반영/Δ/LU·벡터스코프↔레이더 원크기·탭 이중네모 제거·TF 분석창 하이라이트·커서 음이름(센트 제거). **+ ⭐풀스크린 SPL미터/알람 "완벽해결"(사용자 확인)** — 최종해법: show 前 `_set_fullscreen_auxiliary()`(FullScreenPrimary 제거+**FullScreenAuxiliary** 지정=풀스크린 위 정상크기 보조창, 자동 풀스크린화 차단) + `_attach_as_child`(네이티브 addChildWindow=같은 Space) + opacity0로 열고 보정후 표시 + 850ms 안전장치(크게뜨면 줄이고 무조건 표시). 검은풀스크린/Space점프/번쩍임 전부 해결. **A섹션 검증 전부 끝.** 전부 미빌드.
- 2026-06-14: git push(84커밋+태그7), 라이선스 Ed25519 라운드트립 6/6, 개인키 백업, Help버튼·드래그드롭#6 육안, Silicon 빌드+DMG 브랜딩, 진단 로깅+로그보내기, **재초기화 후 장치목록 로깅 추가**.
- 2026-06-13: 캘리브 채널테이블 B안 구현, SPL Meter 행×열, 캡쳐 일괄토글 설계.

---

## 📥 INBOX (분류 대기 — 막 적기)

(여기에 떠오르는 거 한 줄씩. Claude가 위 카테고리로 올림.)

### 🗓️ 2026-06-22 Scarlett 2i2 HW 검증 세션 — 발견 모음 (하나씩 처리 예정)

**✅ 검증 통과 (HW 실측):**
- TF **Adaptive(MTW) 엔진** = 검증 끝. 엔진 런타임 전환 OK, 라이브 곡선·딜레이파인더 정상. **전기 루프백에서 Adaptive 평탄편차 0.005dB·코히런스 1.0000·위상0°**(CSV 정량). 신뢰대역(100Hz–8k)에서 Single과 0.2–0.4dB 일치. (검증 CSV: `~/Desktop/tf_captures_2026062*.csv`)

**🐛 발견 버그 (우선순위 순):**

1. **[버그·✅✅HW 실측 검증완료·미커밋] Farina 스윕 THD/SNR 깨짐 (THD 325% / SNR −28dB)** — ✅✅**헤드리스 재현+수정+Scarlett 2i2 실측 검증완료(2026-06-22)**. **진짜 근본원인 2개(둘 다 수정):** ⓐ duplex가 스윕을 루프 재생하고 캡처가 임의 위상에서 시작 → 캡처 ref가 ESS의 회전본이라 `_ess_inverse`(index 기준 envelope 재계산)와 불일치. ⓑ **(진짜 주범)** `latency='high'` duplex라 **입력(meas)이 출력(ref)보다 I/O 왕복지연 132~218ms(매번 가변) 늦는데**, `farina_analyze`가 선형피크 창을 g_ref(zero-delay) 위치 **50ms tail**에 고정 → 실제 측정피크가 창 밖으로 나가 lin_peak이 노이즈만 잡아 THD 폭발. (진단 로그로 `start_pos`는 처음부터 정확(`ref_align_cos=1.0`), `peak_off=6316~10418`로 창밖임을 실측 확인.) **수정 2곳:** ①duplex `cb`가 캡처 첫샘플 재생버퍼 위상 `start_pos` 기록→`sweep_captured(ref,meas,start_pos)`(signal 3인자), `_on_sweep_captured`가 x=재생성 clean ESS + `meas=np.roll(meas, start_pos)`로 위상정렬. ②`farina_analyze`가 선형피크 n0를 **측정 g의 실제 argmax(reference 위치 이후 최강점)**로 잡아 I/O 지연 흡수, reference 창은 자기 피크 기준(`wayaudo2.py:7390~`). **검증:** 헤드리스 sim(I/O지연 40~10451샘플 전부 THD 1.5~2.1% 정상, 기존 test_farina 8/8 PASS) + **Scarlett 2i2 전기루프백 4회 연속 THD 0.19~0.21%·SNR 39.6~40.0dB 안정**(이전 318~1914%/−39dB). 미커밋.

2. **[버그·🟡수정적용·미커밋] Farina 스윕 `_fft_lbl` 크래시** — `_on_sweep_captured`(`:12550`/`:12574`)가 **DelayFinderDialog 소속** `_fft_lbl`(`:9922`)을 잘못 참조 → 스윕 시 무조건 AttributeError 크래시 → Wiener 폴백(−27dB 엉터리). **수정: `avg_lbl`로 변경 + 완료 결과 팝업(`_BrandBox.information`, THD/SNR 표시) 추가 — 현재 작업트리에 적용됨(미커밋).** Farina DSP 테스트 8/8 정상(라벨만 문제였음). ℹ️ 부작용 규명: 기존엔 이 크래시로 `_stop_sig_gen()`에 도달 못 해 스윕이 무한루프 → "연속 스윕"처럼 보였던 것. **사용자 결정: one-shot 정밀 측정 유지**(연속 아님).

3. **[버그·✅✅수정+헤드리스검증완료] Extra 소스 스펙트럼 캘리브 오적용** — ✅✅**수정+검증(2026-06-22, HW불필요)**. `_process_extra_source` 곡선이 `self.calib_offset`(primary) 쓰던 것 → `src_calib = self._spl_source_calib(card_id)`(소스 자기 device:ch 조회, mutex 밖 계산)로 교체(`:18154`/`:18169`). **검증(`/tmp/calib_test.py`, 실 MainWindow 메서드를 mock self로 직접 호출)**: extra 소스(자기 calib 128) 곡선 적용 오프셋=**128.00**(primary 100 아님)=PASS, `_spl_source_calib` 룩업도 primary=100/extra=128 정확. **세로 레벨 어긋남 해결, 모양 무관.** 끝단까지 확인 — 실앱 육안 불요.

4. **[조사·🟢낮음] Single TF 엔진 −1.8dB / 코히0.82 (루프백)** — Adaptive=완벽인데 Single만 전대역 균일 −1.8dB·코히 0.82. 위상 평탄(0°)이라 **고정 시간오프셋 아님** → 랜덤 디코릴레이션/비원자 ref·meas 짝짓기 의심(Single `_render_inner` 경로 vs MTW 원자버퍼 `:12078`). |H|≈γ 서명. 확정 진단=`_on_frame`에 ref/meas 상호상관 lag `_diag` 1줄. **Single은 제거/Lite 강등 후보라 블로커 아님.**

**🟢 [조사·HW재확인예정] 외장 마이크 하이/로우 floor (2026-06-22 사용자 증상 정정)**
- **정정된 증상**: "두 마이크가 측정이 다르다"가 아니라 — **스펙트럼 하이/로우 끝쪽에서 외장(Scarlett)은 어떤 레벨 아래로 곡선이 더 안 내려가고 floor에 깔리는데, 내장(MacBook)은 그 아래로 계속 내려가 표시됨.**
- **코드 검증(2026-06-22)**: primary(`_process_audio:17973`)/extra(`_process_extra_source:18146`) DSP 바이트 동일 — `power_spectrum_db`(1e-20=−200dB에서만 floor)·EMA pow smoothing·avg_buf 평균·calib 더하기 전부 동일. 스펙트럼 곡선엔 게이트/floor 없음. 디스플레이 클램프(`db_min`)도 두 곡선 공통. **→ 앱이 외장만 막는 코드 floor는 없음(곡선 계산 대칭 확인).**
- **추정(미확정·HW검증대기)**: 외장이 멈추는 floor가 `db_min`보다 위 = 그 인터페이스+측정마이크의 **실제 broadband 노이즈 플로어(프리앰프 히스+저레벨 광대역음)**. 내장이 더 내려가는 건 소형 MEMS의 HF 롤오프+LF 하이패스/DSP 가공. 즉 floor에서 멈추는 외장이 더 정직, 내장이 가공.
- **HW 확인법(사용자 다음에 직접 테스트하기로)**: ①외장 입력 게인↓ 시 floor 같이 내려가면 = 프리앰프 노이즈 증거. ②같은 음원 미드대역 비교 시 일치(이미 0.4dB), 차이는 끝쪽만. ③필요시 평균화↑로 floor 부드럽게(실노이즈라 한계). **사용자 결정: 수정 전 HW로 직접 재현·확인 후 판단.**

**⚠️ 현재 작업트리 상태:** #1(Farina THD)·#2(크래시)·IR freeze = **커밋됨(`774079b`)**. #3(Extra 소스 캘리브) = **수정완료·미커밋(작업트리)**. SMAART PDF·`_make_smaart_pdf.py`는 미커밋(별개).
