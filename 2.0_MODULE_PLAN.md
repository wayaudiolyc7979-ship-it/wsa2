# SPECTRA 2.0 — 로드맵 & 모듈 분해 청사진

> 원본: 2026-06-25 아침 세션에서 논의한 v2.0 방향. 그때 "1.7부터 집중하자"로 접으며 저장이 빠졌던 것을 이 문서로 영구 반영.
> ⚠️ 아래 라인 번호는 2026-06-25 기준(약 19,800줄). 이후 편집으로 ±수십 줄 어긋날 수 있으니, 작업 시 클래스/함수명으로 재확인할 것.
> 상세 메모리: `project_v2_roadmap`, `project_todo_code_protection`, `project_v2_direct_decision`.

---

## ✅ 진행 현황 (2026-08-29 갱신)

- **Phase A (DSP 골든 pytest) — ✅ 완료.** `tests/test_dsp_golden.py` 20개 통과.
- **Phase 1/B (단일 파일 분해) — ✅ 완료(MainWindow 포함 완전 분해).** `wayaudo2.py` **23,380 → 748줄(97% 격리)**, `spectra/` **23모듈**. 순환참조 0, 전 모듈 임포트 OK, 매 이관 verify.sh(골든20·selfcheck41·실앱기동 크래시0·미해결상수/미import/undefined 전부 NONE) 통과.
  - 실제 구조: `spectra/dsp/{weighting,tf,loudness,farina}.py` · `core/{i18n,logging_diag,config,license}.py` · `audio/engine.py` · `ui/{tokens,draw,colors,icons,widgets,dialogs,spl,canvas_spectrum,canvas_tf,canvas_stereo,stereo_page,tf_window,capture_drawer,main_window}.py`.
  - **wayaudo2.py 잔존 748줄 = 순수 런처**(진입점 `__main__` + `_APP_VERSION` + 스플래시 + 시작진단 + 미사용 dead code 3종).
  - **MainWindow도 `spectra/ui/main_window.py`로 이관.** `_APP_VERSION`은 bump_version.sh 정규식 대상이라 wayaudo에 유지하되, main_window의 About/푸터용으로 `main_window.APP_VERSION`에 import 시점 주입(순환 회피).
  - **빌드 spec:** `__init__.py` 5개 존재 + 전부 static import → PyInstaller가 spectra/ 자동수집(디스크 27모듈 전부 임포트그래프 포함 확인). `.spec` 수정 불필요 추정. selfcheck.py는 spectra 백필로 대응 완료. bump_version.sh는 정규식이라 _APP_VERSION이 wayaudo에 있는 한 OK. **실제 빌드 검증은 사용자 "빌드하자" 신호 때.**
- **Phase 2/C (Nuitka 보호 + MTW 승격), Phase 3/D (측정엔진·세션저장) — 미착수.**

---

## v2.0의 본질 = "기능 추가"가 아니라 "구조를 다시 세우는" 버전

v1.7까지는 단일 파일에 기능을 쌓아왔다. v2.0은 그 토대를 재정비한다. 핵심 한 줄: **분해 → 보호 → 측정엔진 승격**. 나머지(탭분리·둥근모서리·세션저장)는 분해가 끝나면 자연스럽게 쉬워진다.

### 우선순위
- **0순위 (전제조건) — 테스트 인프라.** `selfcheck.py`는 훌륭하나 정식 회귀 스위트가 없음. 분해를 안전하게 하려면 **DSP 핵심부터 pytest 골든값 고정**. 분해를 나중에 하더라도 지금 만들어두면 손해 0(즉시 회귀 방지 이득).
- **1순위 — 단일 파일 분해 (이게 진짜 2.0의 이유).** `wayaudo2.py` ~19,800줄. 팝아웃/탭분리/둥근모서리 같은 작업이 "한 덩어리라서" 매번 막힘. 테스트·코드보호·협업 전부 여기서 걸림. 중간 버전에서 하기엔 회귀 위험이 너무 커서 **2.0이 아니면 못 함**.
- **2순위 — 소스코드 보호 (Nuitka).** 모듈 분해 후에 해야 빌드 spec·Actions 교체가 깔끔. `core/license.py`만 따로 강하게 보호. 상세=`project_todo_code_protection`.
- **3순위 — 측정 코어 Adaptive(MTW) 기본 승격.** 지금은 옵션(폴백). 멀티해상도를 기본 측정 엔진으로 올리면 Smaart와 정면 차별점. 단 HW A/B + CPU 부담 검증이 심장부 작업 → 1·2순위 후.
- **4순위 — 프로젝트/세션 저장 포맷.** 캡쳐+캘리브+카드구성+뷰를 "측정 프로젝트" 하나로 저장/불러오기. "어제 세션 그대로 열기".

---

# 모듈 분해 청사진

## 0. 현재 구조의 핵심 사실 (분해 설계의 전제)

19,800줄이 사실 **이미 7개의 자연스러운 덩어리**로 나뉘어 있다. 문제는 파일이 하나라는 것뿐이고 클래스 경계는 의외로 깨끗하다. 다만 **두 결합 지점**이 난이도를 결정한다.

### ⚠️ 결합 지점 1 — 가변 전역 상태 (진짜 함정)
```
_theme       ~66회 참조   ← 런타임 다크/라이트 토글로 변함
_LANG         ~7회        ← 언어 토글
_DELAY_UNIT   ~9회        ← ms/m 토글
_SOUND_SPEED  ~7회
global        ~8곳에서 재할당
```
모듈을 쪼개면 `from config import _theme`는 **값을 복사**해 토글이 안 퍼진다. → **반드시 함수 접근자**(`T(key)`, `_tx(s)`, `cfg.delay_unit()`)로만 접근하게 통일. `T()`는 이미 접근자라 대부분 안전, **직접 `_theme` 참조하는 소수만 `T()`로 교체**. (1순위 작업)

### ⚠️ 결합 지점 2 — 거대 클래스 2개
```
MainWindow              ~3,900줄
TransferFunctionWindow  ~3,500줄
```
이 둘이 전체의 ~38%. 모듈로 옮기는 건 쉽지만 **클래스 내부를 더 쪼개는 건 2.0에서도 하지 말 것**(메서드 간 `self.` 결합 촘촘, 회귀 지옥). 파일만 분리하고 내부는 그대로.

---

## 1. 목표 패키지 트리

```
spectra/
├── app.py                  # main 진입 + 부팅 시퀀스
├── core/
│   ├── config.py           # ★상태 단일 소스: _theme/_LANG/_DELAY_UNIT/THEMES + T()/_tx()
│   │                       #   설정·캡쳐 load/save, 경로, _APP_VERSION
│   ├── i18n.py             # _TR_KO, _tx, _set_lang
│   ├── logging_diag.py     # 로깅·_diag·_crash_handler·_cleanup_logs
│   └── license.py          # verify_license, load/save, machine_id
├── dsp/                     # ★순수 함수 — 테스트 앵커, Qt 의존 0
│   ├── weighting.py         # a_weight/c_weight/power_spectrum_db/octave
│   ├── tf.py                # _tf_smooth/_smooth_real/MTWEngine
│   ├── farina.py            # ESS/inverse/fft_convolve/farina_analyze
│   └── loudness.py          # _biquad/_KWeightFilter/LoudnessMeter
├── audio/
│   ├── engine.py            # AudioEngine/_DeviceStream/Subscription
│   ├── sources.py           # AudioThread/_Engine*Source/TFSyncThread
│   ├── duplex.py            # TFDuplexThread
│   └── device_watch.py      # _CoreAudioDeviceWatcher
├── ui/
│   ├── tokens.py            # FS_*/RADIUS_*/ss_* 헬퍼·폰트
│   ├── chrome.py            # 타이틀바·brand 로고·grad·icon·splash
│   ├── widgets.py           # VUMeter/RoundComboBox/팝업/카드/세그먼트
│   ├── dialogs.py           # _BrandBox/Calib/Color/Shortcuts 등
│   └── draw.py              # freq_to_x/db_to_y/info_box/grid/palette
├── tabs/
│   ├── spectrum/
│   │   ├── canvases.py      # FFT/Octave/Spectrogram
│   │   └── window.py        # MainWindow
│   ├── transfer/
│   │   ├── canvases.py      # TFMag/TFPhase/TFIR
│   │   ├── cards.py         # _MeasCard/_VUProxy/_MiniVU
│   │   ├── dialogs.py       # Sweep/Sine/Delay/AllDelay finder
│   │   └── window.py        # TransferFunctionWindow + key filters
│   └── stereo/
│       ├── canvases.py      # Vectorscope/Radar/History/GradientNumber
│       └── page.py          # StereoLoudnessPage/StereoAudioThread
├── spl/                     # SPL미터·LEQ·알람·ShowMode (탭 공유)
│   └── meter.py             # Leq/SplPanel/SplMetric/Alarm/ShowMode/SplMeter
└── popout.py                # _TFPopout/_Spectrum/_Stereo Popout
```

---

## 2. 의존성 방향 (단방향 강제 — 순환 금지)

```
app → tabs → {ui, spl, audio, dsp, core}
              ui  → {core}
              spl → {ui, dsp, audio, core}
            audio → {core}
              dsp → (의존 없음, 순수)   ← ★테스트 황금지대
             core → (PyQt 최소만)
```

**순환 위험 1곳**: `MainWindow`가 `TransferFunctionWindow`/`StereoLoudnessPage`를 임베드(탭)하고, 팝아웃이 셋을 다 참조. → `popout.py`를 최상위에 두고 탭들은 서로 import 안 하게 **MainWindow가 조립**(현재도 사실상 그럼). 탭 간 직접 참조 0 유지 시 순환 없음.

---

## 3. 안전한 마이그레이션 순서 (회귀 최소)

> 핵심 원칙: **한 번에 한 패키지, 매 단계 `python3 selfcheck.py` + `import ast` 게이트 통과 후 커밋.** 동작 0 변경, 순수 이동만.

| 단계 | 내용 | 위험 | 선행 |
|---|---|---|---|
| **0. 테스트 골든값 고정** | `dsp/` 함수(power_spectrum_db, a_weight, MTW, farina, loudness)에 입력→출력 골든값 pytest | — | **필수 1순위** |
| **1. `_theme`/전역 함수화** | 직접 `_theme` 참조 ~66곳 → `T()` 경유 통일, 가변 전역을 `core/config.py`로 | 중 | 0 |
| **2. `dsp/` 추출** | Qt 의존 0이라 가장 안전, 골든 테스트가 지켜줌 | 낮 | 0,1 |
| **3. `core/` + `audio/` 추출** | 엔진 경계 깨끗(이미 클래스 분리) | 낮 | 2 |
| **4. `ui/` 추출** | tokens→chrome→draw→widgets→dialogs (의존 깊이 순) | 중 | 3 |
| **5. `spl/` + 탭 canvases 추출** | selfcheck 캔버스 렌더로 커버 | 중 | 4 |
| **6. 거대 클래스 2개 + app** | MainWindow / TF window를 **통째로** 이동(내부 안 쪼갬) | 중 | 5 |

**롤백 안전망**: 각 단계 독립 커밋. selfcheck PNG를 단계마다 `Read`로 육안 대조 → 시각 회귀 즉시 감지.

---

## 4. 따라오는 보너스 (분해 끝나면 거저 풀림)
- **탭 별도 창 분리** — `tabs/*/window.py`로 자체완결되면 자동 해결
- **소스 보호** — 모듈 단위라 Nuitka spec 깔끔, `core/license.py`만 따로 강하게 보호
- **테스트** — `dsp/` 격리되면 정식 pytest 스위트가 처음으로 가능
- **Windows/Intel 빌드 spec** — 단일 파일 가정 제거, 패키지 자동 수집
- **레포 루트 재배치** — 지금은 빌드 spec/스크립트가 브랜드자산·스크립트를 **루트 bare 경로**로 참조해서 `assets/`·`build/` 폴더로 못 옮김(릴리스 전 위험). spec을 어차피 다시 쓰는 v2.0에 **함께** 정리: `assets/`(splash·icon·dmg_background·entitlements), `build/`(spec·sh), `tools/`(license). 2026-06-25에 스크래치/디자인 실험본만 `docs/concepts/`로 1차 정리해둠.

## 5. 비용·리스크 (솔직히)
- **분량**: 순수 이동이라 "새 코드"는 거의 없지만 import 정리 + `_theme` 함수화가 손이 많이 감. **단계 0~2가 전체 위험의 80%**, 나머지는 기계적.
- **숨은 함정**: `selfcheck.py`·빌드 `.spec`·`bump_version.sh`가 **단일 파일을 하드코딩** → 같이 고쳐야 함(분해 마지막에 일괄).
- **절대 하지 말 것**: MainWindow/TF 클래스 **내부**를 쪼개려는 욕심. 파일만 옮기고 멈출 것.

---

## 추천 선행 작업 (지금 해도 손해 0)
**단계 0(dsp 골든 테스트)** 는 분해를 나중에 하더라도 지금 만들어두면 즉시 회귀 방지 이득. 분해의 안전망이자 v2.0 착수 전 가장 가성비 좋은 준비.
