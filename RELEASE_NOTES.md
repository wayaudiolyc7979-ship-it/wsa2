# WSA2 — 릴리즈 노트

> 정식 릴리즈는 **v1.0** 부터 시작 예정. 현재는 **베타** 단계.
> - `v0.9-beta` : 초기 버전
> - `v1.0-beta` : 멀티-장치 카드 + 런타임 장치 변경 등
> - `v1.1-beta` : TF 코히어런스 표시 정리 (**현재** 버전)

---

## v1.1-beta (2026-06-07) — 구현 내용

### Transfer Function (TF) — 코히어런스 표시 정리
- **코히어런스를 Magnitude 그래프 전용으로 통합** — Phase 쪽 코히어런스 **완전 제거**(트레이스·블랭킹·커서 % 표시)
- 툴바 **`Coh:` 슬라이더 + `Mag` 블랭킹 토글 제거** — UI 단순화
- Magnitude γ² 트레이스 개선: **밴드 높이 50%**, 기준선 **1.0 / 0.5 / 0.0**(위=100%, 아래=0%), 시각 스무딩 완화(5→3탭) → Smaart식 **디테일** 표시
- Magnitude 자동 Y스케일은 코히어런스 ≥0.5 구간 마스킹 유지(노이즈 구간 제외)

> ⚠️ 검증 미완: 코히어런스 **값 자체**가 정상 범위인지 외장 인터페이스로 실측 필요(아래 TODO G).

---

## v1.0-beta (2026-06-07) — 구현 내용

### Spectrum
- **멀티-장치 입력 카드** (AudioEngine 기반): 카드마다 장치+채널 드롭다운, FFT/옥타브 색별 오버레이
- 카드 **이름 변경**(번호 더블클릭), **번호 안정화**(삭제해도 유지·추가 시 빈번호 채움)
- 카드 **표시 토글**(체크박스), **클릭 시 front**(맨 앞), 카드 우측 테두리·레이아웃 통일
- 옥타브 추가소스 **solid 막대**, **옥타브 주파수 라벨 밴드 정렬**(라벨↔막대 위치 일치)
- **FFT 멀티마이크 버벅임 해소**(라이브 곡선 안티앨리어싱 OFF — Retina 부하 10배↓)
- 'M' 라벨 제거 + 레벨미터 확대

### Transfer Function (TF)
- 카드 **이름 변경·번호 안정화·그래프 표시 ON/OFF 토글**(분석은 유지, 곡선만 숨김)
- 카드 추가 시 **macOS 전체화면 Space 전환 버그 수정**(부모 없는 위젯 setVisible 제거)
- 추가 측정 카드 **영속화**(장치/채널/딜레이/이름/번호 저장)

### 오디오 엔진 / 장치
- **런타임 장치(입력/출력/채널) 변경 안전 재구성** — 재시작 없이 전환
- 오디오 스레드 `stop()` **abort teardown**(장치 즉시 해제 → 멈춤/잠김 방지)
- **마지막 사용 인터페이스 기억**(시작 시 복원, 클로버 버그 수정)

### UI 공통
- 카드 통일/테두리, 구분선·점선 정리, 콤보 팝업 화면밖 클램프, 옥타브 minor 그리드

---

## 해야 할 일 (TODO) — 월요일 외장 인터페이스 실측 검증

> 아래는 **코드 구현은 됐으나 실장비(외장 인터페이스) 검증이 남은** 항목.
> 실행: `python3 wayaudo2.py`

### A. 오디오 — 런타임 장치 변경 (★실측 필수)
- [ ] Spectrum: 장치 A Start → B로 변경 → **재시작 없이** 정상, A 복귀·채널변경 반복, 멈춤/잠김 없음
- [ ] TF: 분석 중 Reference/Measurement 입력장치·채널·출력장치 변경 → **즉시 새 장치로 재개**
- [ ] 같은 장치 in/out duplex 상태 변경, 빠른 연속 변경(디바운스) 누수 없음
- [ ] 로그에 `wait timeout` 경고 없음 / Stream open·close 짝 맞음 / USB disconnect 회귀 없음

### B. 마지막 장치 기억 (★실측 필수)
- [ ] 외장을 Meas/Ref/Out으로 선택 → 종료→재실행 → 그대로 복원 (내장도)
- [ ] 재실행 직후 settings.json 의 tf_*_device·채널이 사용자값 그대로(기본값으로 안 바뀜)
- [ ] 외장 미연결 재실행 → 폴백돼도 저장된 장치값 유지(폴백값으로 안 날아감)

### C. Spectrum 멀티-장치 카드 (★외장 본검증)
- [ ] 추가 카드에서 다른 인터페이스 선택 → FFT 색별 커브 동시 오버레이, 장치별 SR 달라도 고역 정상
- [ ] 같은 장치 다른 채널 카드 2개 → 엔진 스트림 1개 공유(로그 확인), 충돌/멈춤 없음
- [ ] 카드 구성 종료→재실행 복원, 가시성 토글·레벨미터·장치별 calib

### D. TF 멀티-카드 측정 (★외장 본검증)
- [ ] 모든 마이크 같은 외장 인터페이스 + Reference를 **실제 입력채널**(루프백/레퍼런스 마이크) 설정 →
      카드1·2·3 동시 측정 정상 (※ Internal SigGen 레퍼런스로는 카드1만 측정 — 정상 한계)
- [ ] 그래프 표시 체크박스: 일부 숨겨도 분석 유지·나머지 정상

### E. TF 캡쳐 개선 (2026-06-06 구현, 실측 미완)
- [ ] 멀티카드 동시 캡쳐 / 빠른 캡쳐(스페이스 연타) / Reference·Δ / 안정화 캡쳐(⏳) / Export(CSV+PNG)
- [ ] 앱 재시작 시 captures.json 복원, 카드별 IR 표시, 카드 클릭→곡선 front

### F. 디자인/레이아웃 육안 확인
- [ ] TF·Spectrum·Loudness 디자인 토큰 통일, 툴바 가로 스크롤(창 1100까지 축소)

### G. TF 코히어런스 표시 + 값 검증 (2026-06-07 구현, ★실측 필수)
- [ ] **코히어런스 값 점검**: 깨끗한 측정에서 마우스를 **1kHz 근처**에 올려 `%` 확인 → 중역 **80%+** 정상.
      현재 화면상 주황선이 "0.0"선 근처(~5%)에 평평 → 값이 낮으면 **계산/측정 설정**(Ref/Meas 짝·딜레이 정렬·평균) 조사
- [ ] γ² 트레이스 육안: 밴드 50% + 기준선(1.0/0.5/0.0)에서 디테일하게 움직이는지, Magnitude 가독성 방해 없는지
- [ ] Phase 그래프에 코히어런스 흔적 없는지 / 툴바에 Coh 슬라이더·Mag 토글 없는지 확인

---

## 보류 / 결정 필요
- **Δ(델타) 버튼 유지 여부**: 직접 써본 뒤 유지/제거/단순화 결정. (제거 시 toolbar `delta_btn`+드로어 `R`+핸들러 정리)
- **TF 내부 레퍼런스 멀티카드 공유**(선택 확장): 같은 장치 한정으로 Internal SigGen으로도 멀티 카드 측정 가능하게 — 실측 후 필요시
- **공유 엔진 옵션2 다음 단계**: TF 입력도 engine 마이그레이션(탭 간 동일장치 충돌 완전 해결), Stereo 마이그레이션

---

## 버전 되돌리기
- 베타 0.9로: `git checkout v0.9-beta`
- 베타 1.0로: `git checkout v1.0-beta`
- 현재(베타 1.1)로: `git checkout v1.1-beta`
