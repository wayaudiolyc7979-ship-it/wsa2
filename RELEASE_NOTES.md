# SPECTRA — 릴리스 노트

> **See your sound.** · Spectrum Analyzer · by WAYAUDIO

브랜드 버전(HTML)은 [`RELEASE_NOTES.html`](RELEASE_NOTES.html), 베타 시절 개발 로그는 [`docs/beta_dev_log.md`](docs/beta_dev_log.md) 참고.

---

## v2.0.2 — 정확도·안정성 보강 (빌드전 심층리뷰)  *(2026.09.06)*
> v2.0.1 배포 이후 **전체 코드 심층 재점검**으로 오래된 정확도·안정성 문제를 위험도순으로 바로잡은 유지보수 릴리스입니다. 동작·화면은 그대로.

### 🐛 버그 픽스
- **라우드니스(LUFS) 표준 추가 정합 — 게이팅·샘플레이트** — 통합 라우드니스(Integrated)의 **게이팅 구간을 국제표준(BS.1770, 400ms·75% 겹침)에 정확히** 맞췄고, **44.1kHz 및 고배율(96/192kHz) 샘플레이트에서 K-가중 필터가 어긋나던 것**도 바로잡았습니다. 특히 44.1kHz(CD 레이트)에서 저음 가중이 잘못돼 값이 틀리던 것을 해결 — 이제 모든 샘플레이트에서 규격에 맞는 라우드니스가 나옵니다
- **캡처 파일도 원자적 저장** — 설정에 이어 **저장된 캡처(스펙트럼·TF 곡선)도** 저장 도중 앱이 꺼져도 파일이 깨지지 않도록(원자적 교체) 보강. 예기치 않은 종료로 저장해둔 캡처가 통째로 사라질 위험 제거
- **장시간 측정 자동복구 보강** — 마이크가 오래 뒤 멈추는 문제의 **남은 경로 하나**를 추가로 막았습니다. 장치는 계속 인식되는데 내부적으로만 콜백이 완전히 죽어(재시도 소진) 방치되던 드문 경우에도, 이제 같은 장치로 자동 재초기화·재시작합니다(Spectrum·Transfer Function 공통)
- **탭 동시 사용 시작 안정성** — 한 인터페이스를 다른 탭이 이미 다른 샘플레이트로 쓰고 있을 때 Spectrum·Stereo 탭 시작이 실패하던 것을, **먼저 연 쪽 샘플레이트에 자동으로 맞춰** 함께 켜지도록 개선
- **Transfer Function 종료 후 Spectrum 응답성 회복** — TF를 함께 쓰면 지연(latency)이 올라가는데, TF를 끈 뒤에도 Spectrum이 세션 내내 느린 지연에 머물던 것을 **다시 빠른 지연으로 되돌리도록** 수정
- **USB 재연결 감지 중복 정리** — 인터페이스를 뽑았을 때 재연결 감지 폴링이 두 갈래로 겹쳐 재초기화가 두 배로 돌던 것을 정리(감지 창도 정상화)
- **Stereo 벡터스코프 부드러움** — 벡터스코프가 오디오 콜백 속도에 끌려 버벅이던 것을, 다른 화면들과 같은 고정 30fps로 그리도록 통일
- **정지한 측정 카드의 유령 한 프레임 제거** — 추가 측정 카드를 끄는 순간 그 곡선이 한 프레임 깜빡 남던 것 정리
- **한국어 UI 완성도 보강** — 한글 모드에서 영어로 남아있던 문구 30여 곳(오라제이션 다이얼로그·Stereo Loudness 상태 표시·일부 툴팁)을 번역해 채웠습니다(Auralization은 "오라제이션"으로 표기)

### ⚡ 오래 켜둬도 가벼워졌습니다 (장시간 성능)
공연·방송처럼 **몇 시간씩 켜두면 점점 무거워지던** 원인들을 찾아 걷어냈습니다. "사운드체크 땐 멀쩡했는데 밤엔 버벅인다"가 사라집니다.
- **라우드니스 측정이 시간에 비례해 무거워지던 문제 해결** — 적분 데이터를 계속 쌓아두고 매번 전부 다시 훑던 방식을 바꿔, **8시간 구동 기준 계산 비용을 약 29배 줄였습니다**(메모리도 18MB→2MB). 측정값은 그대로입니다
- **SPL LEQ 표시가 시간이 갈수록 느려지던 문제 해결** — 필요한 구간만 읽도록 바꿔 **약 69배 빨라졌습니다**(3시간 구동 기준). SPL 미터와 알람 창을 함께 띄워도 화면이 매끄럽습니다
- **캡처가 쌓이면 캡처할 때마다 멈칫하던 문제 해결** — 저장을 백그라운드로 옮겨, 캡처가 수십 개여도 화면이 멈추지 않습니다(예전엔 50개에서 약 2초, 100개에서 약 4초 정지)
- **측정 CPU 사용량 상시 감소** — 라우드니스 필터 연산을 최적화해 **상시 CPU 점유를 약 20%→2%로** 낮췄습니다
- **그래프 그리기 최적화** — 옥타브 막대를 매 프레임 두 번 그리던 낭비 제거(1/24 옥타브에서 그리기 비용 42% 절감), Transfer Function 곡선 생성 5배 가속, 벡터스코프·SPL 패널 중복 작업 제거. 화면은 완전히 동일합니다
- **RTA 칸이 신호를 못 받을 때 화면이 몇 초씩 멈추던 문제 해결** — 무한 재시도로 UI를 붙잡던 것을 자동으로 물러나게(장치·채널을 바꾸면 다시 시도)

### 🐛 버그 픽스 (추가)
- **정지 상태에서 "다시 캡처"가 기존 캡처를 지워버리던 문제 수정** — 측정이 멈춘 상태에서 캡처를 다시 캡처하면 **저장해둔 곡선이 −96dB 평평한 선으로 덮어써지고 그대로 저장**되던 심각한 문제를 막았습니다. 이제 라이브 데이터가 없으면 캡처·재캡처가 되지 않고 안내가 뜹니다(예전엔 아무것도 저장되지 않았는데도 "✓ Captured"가 뜨기도 했습니다)
- **장시간 측정 자동복구가 Transfer Function 별도 창·Stereo 탭에서 동작하지 않던 문제 수정** — 팝아웃 상태의 TF와 Stereo Loudness도 이제 같은 자동복구를 받습니다
- **측정 카드를 지운 뒤 새 카드가 지워진 카드의 기준 신호를 쓰던 문제 수정** — 조용히 잘못된 Transfer Function이 나올 수 있던 문제
- **마이크 보정에서 오프셋이 범위를 넘으면 잘린 값을 저장하면서도 "맞음(✓)"이라 표시하던 문제 수정** — 이제 실제 적용값을 보여주고, 잘렸으면 경고합니다
- **오라제이션 개선** — 측정 전에 한 번 열었으면 이후 측정을 마쳐도 Room 버튼이 계속 비활성이던 문제, 캡처를 지웠을 때 다른 캡처가 재생되거나 아무 반응이 없던 문제 수정
- **SPL 미터·알람 창을 여닫을 때 메모리가 계속 쌓이던 문제 수정**
- **딜레이 찾기가 실패하면 버튼이 영구히 눌리지 않던 문제 수정** · **캡처 삭제 중 빠르게 다시 누르면 오류가 나던 문제 수정** · **측정 중 보정 채널을 바꾸면 버튼이 "Measuring…"에 멈춰 있던 문제 수정**
- **입력칸에서 ↑↓ 키가 먹지 않던 문제 수정** — 숫자 입력칸(예: Transfer Function의 Delay)·드롭다운·목록·대화상자에서 위/아래 화살표를 눌러도 **값이 바뀌지 않고, 대신 Spectrum의 dB 범위가 조용히 움직이던** 문제를 바로잡았습니다
- **`⌘?`(설명서 열기)가 단축키 목록을 열던 문제 수정**
- **단축키 목록(`?`)을 실제 동작에 맞게 전면 정리** — `S`는 메인 창에서만 동작한다는 점, `Space`는 Stereo 탭에서 동작하지 않는다는 점, `↑↓`는 **커서를 올린 그래프**의 세로축이 움직인다는 점(Magnitude=dB / Phase=위상 / IR=시간 또는 dB), 팝아웃 단축키가 **토글**이라는 점 등을 바로잡고 빠진 항목을 채웠습니다. 앱 목록과 설명서가 이제 같은 내용입니다
- **라이선스가 어느 날 갑자기 막힐 수 있던 위험 제거** — 맥·윈도우에서 컴퓨터 고유번호를 읽는 명령이 일시적으로 응답하지 않으면 **컴퓨터 이름으로 대체**하고 있었습니다. 컴퓨터 이름은 바꿀 수 있어서, 정상 라이선스가 "이 컴퓨터용이 아닙니다"로 막힐 수 있었습니다. 이제 **한 번 확인된 고유번호를 기억해 두고**, 조회가 실패할 때만 그 값을 씁니다(정상 조회되면 항상 실제 값이 우선이라 다른 컴퓨터로 옮겨 쓰는 건 여전히 불가능합니다)
- **단축키 도움말(`?`) 보강** — 쇼 모드(⌘⇧F)·탭별 별도 창(⌘⇧S/T/L)·Spectrum dB 범위 이동(↑↓)·Transfer Function 주파수축 확대(⌘＋/－)가 목록에 빠져 있던 것을 채웠습니다
- **True Peak 리셋이 PLR/PSR에 반영되지 않던 문제 수정** · **쇼 모드 LEQ 메뉴가 영어 모드에서도 한국어로 나오던 문제 수정**

### 🖥️ 시작 속도 · 화면 선명도 (빌드 전 3차 점검)
빌드 직전에 각도를 바꿔 **화면 버벅임·시작 시간·레티나 표시**를 다시 점검해 나온 것들입니다.
- **앱 시작이 약 4배 빨라졌습니다** — 실행 후 창이 뜨기까지 **약 4.4초 → 1.1초**. 무거운 초기화를 실제로 필요할 때까지 미루도록 정리했습니다(첫 측정 시작이 느려지지 않도록 백그라운드 예열도 함께)
- **레티나(고해상도) 화면에서 그래프가 실제 해상도로 그려집니다** — **스펙트로그램**과 **저장된 캡처 곡선**이 그동안 저해상도로 그린 뒤 늘려 표시돼 뭉개져 보였습니다. 이제 화면의 실제 픽셀로 직접 그려 **가는 톤·좁은 피크·캡처 곡선이 또렷**합니다. 속도는 그대로입니다
- **창 크기를 조절할 때가 부드러워졌습니다** — 드래그하는 내내 무거운 다시 그리기가 반복되던 것을 크기가 정해진 뒤 한 번만 하도록 바꿨습니다. **스펙트로그램의 60초 기록이 창을 1픽셀만 움직여도 사라지던 문제**도 해결(이제 폭이 바뀌어도 그대로 이어집니다)
- **SPL LEQ 값이 실제보다 크게 나오던 문제 수정 (정확성)** — LEQ 평균 구간을 고정값으로 가정해 실제보다 **6~16% 긴 구간을 평균**하고 있었습니다(진행 바도 같은 비율로 어긋남). 실제 갱신 속도를 측정해 계산하도록 바꿔 **60초 설정이 정확히 60초**가 됩니다
- **인터페이스를 바꾸거나 다시 연결할 때 멈춰 있던 시간이 짧아졌습니다** — 오디오 정리 대기가 최대 0.5초씩 쌓이던 것을 줄였습니다
- **테마(다크/라이트) 전환 시 화면이 여러 번 깜빡이던 것 정리** · **라이트 모드에서 Transfer Function 커서가 배경에 묻히던 것** 수정
- **팝아웃(분리) 창 잔손질** — 여닫을 때마다 창이 쌓이던 메모리 누수, 팝아웃 버튼이 눌린 채 고착되던 것, 분리한 Spectrum 창에서 우클릭 메뉴·단축키가 안 먹던 것 수정. 벡터스코프·라우드니스 레이더 창도 풀스크린 위 표시 동기화 대상에 포함
- **TF 캡처 저장 파일이 가벼워졌습니다** — 곡선을 숫자 나열 대신 압축된 형식으로 저장해 파일 크기·저장 시간이 줄었습니다(기존 파일도 그대로 열립니다)

---

## v2.0.1 — 구조 재정비 · Smaart 정합 기능  *(2026.08.31)*
> ℹ️ v1.9는 별도 출시하지 않고 **2.0에 함께 담았습니다** — v1.9 작업 항목이 이 섹션에 통합돼 있습니다.

내부적으로 **앱 전체를 모듈로 재정비**해(단일 2만여 줄 → 기능별 패키지) 앞으로의 개발·안정성 토대를 다졌고, 프로 측정기(Smaart)와의 격차를 좁히는 기능들을 더했습니다. *동작은 그대로 — 같은 앱, 더 단단한 속.*

### ✨ 새 기능
- **글랜스 쇼 모드 (FOH 풀스크린)** — 객석 건너편에서도 읽히는 **거대한 SPL 숫자 + 라이브 스펙트럼**을 풀스크린으로 띄웁니다. 한계 대비 **초록(여유)→노랑(접근)→빨강(초과)** 신호색으로, 공연 중 음압을 멀리서 한눈에 감시. `View` 메뉴 → **Show Mode** 또는 **⌘⇧F**, 나가기는 Esc/F. 헤드라인 숫자는 차분하게 평활돼 멀리서도 안정적으로 읽힙니다
- **코히런스 블랭킹 (Transfer Function)** — TF 크기 곡선을 **신뢰도(코히런스)에 따라 진하게/흐리게** 표시합니다. 믿을 수 있는 구간은 선명하게, 신뢰도 낮은 구간(저역·반사·측정 한계)은 자연스럽게 흐려져 — **어디를 믿고 어디를 걸러야 할지** 한눈에. Smaart의 trace blanking 방식(연속 페이드). Magnitude 그래프 **우클릭 → Coherence blanking**으로 켜고 끕니다
- **THD 측정 (총고조파왜곡)** — 사인 신호의 **배음 왜곡률(%)**을 스펙트럼 커서 리드아웃에 표시합니다. 커서를 기본파 봉우리에 올리면 그 주파수 기준으로 `THD n%`가 한 줄 뜹니다(배음 2·3·4·5차 자동 측정). 스피커·앰프의 왜곡 성능 확인용. 평소엔 깔끔하게 숨겨두고, 스펙트럼 **우클릭 → Show THD**로 켤 때만 표시
- **Transfer Function 레벨바 Peak 표시** — Reference·측정 입력 레벨바에 **순간 최대(Peak)를 흰 틱**으로 함께 표시합니다. 채움 막대는 평균(RMS), 흰 틱은 순간 피크 — **평균은 안전해 보여도 순간 피크가 클리핑 근처면 바로 보입니다**(Smaart식). 틱은 최고치를 잠깐 멈춰(peak-hold) 읽기 편하게 천천히 내려옵니다

### 🚀 개선
- **[v2.0.1] SPL Alarm 창 리디자인** — 개념·기능은 그대로 두고 **보이는 방식**을 새로 다듬었습니다. 번지던 신호등 글로우를 **또렷한 3분할 상태 바**로 바꾸고, 배경은 **평소(안전)엔 깔끔한 중립 다크**로 두되 **한계에 가깝거나(AMBER) 초과(OVER)할 때만 상태색 워시로 경고**하도록 했습니다(초록이 늘 배경에 깔리던 것 제거 — 평소 차분, 위험 시 부각). 수치 숫자는 세리프에서 **깔끔한 sans**로, LEQ 진행 바는 **세그먼트 바**로, 타이틀바의 고정 핀·설정 아이콘은 앱 전체와 같은 **Lucide 라인 아이콘**으로 통일했습니다(고정 켜짐만 파란 강조)
- **[v2.0.1] SPL 미터 카드도 같은 언어로 통일** — SPL 미터 창의 각 지표 카드를 새 Alarm 창과 **한 가족처럼** 맞췄습니다. 수치를 **sans 숫자**로(세리프 → Alarm과 동일), 하단 존 글로우는 **안전할 땐 없애고 주의(노랑)·위험(빨강)일 때만** 표시(초록 상시노출 제거), 카드 라운드도 결을 맞췄습니다. 지표별 색(A=소프트블루/C=소프트와인)은 그대로 유지
- **커서 값 표시 안정화** — 그래프에 커서를 올렸을 때 뜨는 값(dB·THD)과 우상단 최대 주파수 배지가 매 순간 튀지 않고 **차분하게** 바뀝니다. 화면 갱신 속도와 무관하게 일정한 체감으로 읽기 편하게(시간기반 평활). 커서를 옮기면 새 위치 값으로 즉시 반응
- **커서 리드아웃 — 주파수를 메인으로 크게** — 커서 태그에서 **주파수(음이름 포함)를 가장 큰 글씨(18pt)로, 곡선 색으로** 강조하고 dB 값은 그 아래 보조로 차분하게 표시합니다(예전엔 dB가 강조·주파수가 흐림). 어느 주파수를 보고 있는지가 한눈에 들어옵니다 — 스펙트럼·Transfer Function 공통
- **내부 구조 재정비** — 앱 전체를 하나의 큰 파일에서 **기능별 모듈 패키지**로 분리했습니다. 사용자에게 보이는 동작은 100% 그대로지만, 앞으로 기능 추가·수정이 더 안전하고 빨라집니다


측정을 **귀로 듣는** 오라제이션을 새로 넣고, Transfer Function에 주파수축 줌/팬을 더했습니다. 그리고 전체 코드 점검으로 오래된 정확도·안정성 문제를 여러 건 바로잡았습니다.

### ✨ 새 기능
- **다채널 인터페이스 채널 선택 — 격자 피커** — 입력이 많은 인터페이스(예: 64채널)에서 채널을 고를 때, 화면 밖으로 넘치던 긴 세로 목록 대신 **번호를 격자로 펼쳐** 보여줍니다. **64채널도 8×8 격자로 한 화면에**, 스크롤·드래그 없이 **클릭 한 번**에 선택. (채널이 많을 때만 격자로 뜨고, 적으면 기존 목록.) 모든 드롭다운에는 스크롤 안전망도 더해, 목록이 길어도 화면을 넘지 않습니다
- **스펙트럼 소스별 개별 시작/정지** — 각 입력 카드에 **Start(전원 LED)** 버튼이 생겼습니다. 마이크(소스)마다 따로 측정을 켜고 끌 수 있어, 예를 들어 **1번만 측정하고 2번은 꺼두거나** 필요한 소스만 골라 돌립니다. 측정 중엔 LED가 **파랗게 빛나고** 꺼지면 흐린 링이 됩니다. 곡선 표시/숨김(✓)과는 별개(측정하면서 곡선만 숨기기 가능). 카드마다 켜므로 툴바의 전역 Start 버튼은 없앴고, **S 키**로 전부 한 번에 시작/정지할 수 있습니다
- **🎧 오라제이션 — 측정한 공간의 소리를 헤드폰으로 듣기** — 측정한 임펄스 응답(IR)에 아무 음악이나 통과시켜, **그 공연장·그 스피커로 재생된 것처럼** 헤드폰으로 들어봅니다. 그래프(눈)로만 보던 측정을 **소리(귀)로** 확인 — "객석 그 자리에 안 가봐도 그 소리를 듣는" 셈입니다. **원음(Dry) ↔ 공간(Room)을 버튼으로 A/B** 비교하고, **라이브 IR과 캡처들 중 골라** 좌석·공연장별로 들어볼 수 있습니다. Transfer Function 툴바의 **🎧 버튼**으로 엽니다. (측정한 PA로 다시 내보내면 공간이 두 번 걸리니 **헤드폰으로** 들으세요.)
- **Transfer Function 주파수축 줌/팬** — Magnitude·Phase 그래프를 특정 대역으로 확대해 자세히 봅니다. **마우스 휠**(커서 기준 확대·축소) · **드래그**(그 구간만 박스 줌) · **Shift+드래그/트랙패드 두 손가락**(좌우 이동) · **Cmd +/−**(단계 확대) · **더블클릭/Cmd 0**(전대역 리셋). 매그·위상·코히런스가 **같은 주파수축으로 연동**돼 함께 움직이고, 확대해도 커서·눈금·곡선이 모두 따라갑니다
- **🎚️ Transfer Function 라이브 멀티마이크 평균 (공간 평균)** — 마이크를 **객석 여러 위치**에 두고 동시에 측정할 때, 그 구역의 **평균 주파수 응답을 실시간 한 곡선(AVG)**으로 봅니다. TF 툴바의 **Σ 버튼**을 누르면 **현재 측정 중인 마이크들이 자동으로 평균에 포함**돼 **바로 평균이 시작**되고(다시 누르면 끔), 카드 목록 맨 아래에 **AVG 카드**(곡선색 체크박스 · 평균된 마이크 수 · 표시/숨김 ✓)가 떠서 평균 곡선을 마이크 카드처럼 관리합니다. 특정 마이크만 넣고 빼려면 켜질 때 함께 뜨는 **마이크 체크리스트**(또는 각 카드의 **Σ** 토글)에서 조정합니다. AVG 카드를 **우클릭**하면 평균 곡선 색을 바꿀 수 있습니다. 정지 곡선을 합치는 캡처 평균과 달리 측정 중 실시간으로 갱신됩니다. 위치마다 스피커까지 거리·딜레이가 달라도 **크기(파워)만 평균**하므로 위상 영향 없이 그 구역의 **평균 톤 밸런스**가 깔끔하게 잡힙니다. **평균만** 버튼으로 개별 곡선을 숨기고 굵은 AVG 한 줄만 볼 수 있습니다. 정지 곡선을 합치는 캡처 평균과 달리 **측정 중 실시간으로** 갱신됩니다
- **캡처 불러오기(Import)** — 내보냈던 캡처 CSV를 **다시 불러와 화면에 복원**합니다. Magnitude·Phase·Coherence는 물론, CSV엔 없던 **임펄스 응답(IR)도 크기+위상에서 되살려** 표시합니다. 캡처 드로어의 **↑ 버튼**으로 여러 캡처를 한 번에 불러옵니다
- **dB 세로축 수동 고정** — 세로축 상·하한을 **정확한 dB 값으로 직접 입력해 고정**합니다. 툴바의 **`dB` 버튼**(평소 `Auto`)을 눌러 상단·하단(예: +6 / −60)을 타이핑하면 그 범위로 잠기고(버튼이 파란 `+6 / −60` 표시로 바뀜), 측정값이 변해도 자동으로 안 바뀝니다. 해제는 대화상자의 **[자동]** 버튼 또는 dB 눈금 **더블클릭**. 스펙트럼·Transfer Function 공통, 잠금 값은 재시작에도 유지됩니다

### 🚀 개선
- **세 탭 상단 툴바 전면 리디자인 — 프로 계측기 스타일** — Spectrum·Transfer Function·Stereo Loudness의 상단 컨트롤 바를 하나의 새 디자인으로 통일했습니다. 둥근 버튼을 줄줄이 늘어놓던 방식 대신 **테두리 없는 컨트롤 + 각 기능 직관 아이콘 + 활성 상태만 파랑 강조(켜짐/꺼짐 토글은 작은 LED 점) + 계측기다운 모노스페이스 숫자**로 정돈해, 화면이 훨씬 차분하고 전문적입니다. 조작감은 그대로 — 값은 눌러서 드롭다운으로 고르고, 마우스를 올리면 클릭할 수 있는 영역이 은은하게 드러납니다
- **상단 탭 바도 같은 언어로 정돈** — 탭(Spectrum / Transfer Function / Stereo Loudness)을 회색 트랙 없이 **아이콘 + 이름**으로 바꾸고, 활성 탭은 **이름 폭에 딱 맞는 파란 밑줄**로 표시합니다. 겹치던 가로줄(툴바 하단선 등)을 정리해 상단이 한결 깔끔합니다. 탭 아이콘도 각 기능을 명확히(스펙트럼=막대, Transfer Function=기준↔측정 비교, Stereo=음량) 나타내고, 다른 컨트롤과 뜻이 겹치지 않게 정리했습니다
- **오른쪽 패널·마이크 카드·캡처 목록까지 같은 결로** — 오른쪽 정보 패널을 **하나의 외곽 프레임 + 얇은 구분선**으로 정리하고(레벨 표시는 그대로), 마이크 입력 카드는 **큰 숫자 레벨**로 읽기 쉽게 다듬었습니다. 캡처 목록은 무거운 상자·체크박스를 걷어내고 **배경 위에 곡선색 이름**만 얹은 담백한 형태로 — 왼쪽 **색 점을 눌러 표시/숨김**을 바로 토글합니다. 툴바·패널·목록 배경색을 하나로 통일했습니다
- **툴바 버튼 순서를 기능별로 정리** — 세 탭 모두 **성격이 같은 설정끼리 붙이고, 실행 버튼(Reset·Capture 등)은 따로 모아** 배치했습니다. 전에는 캡처·리셋이 표시 설정 사이에 끼어 있어 찾기 번거로웠던 것을 정돈 — 왼쪽에서 오른쪽으로 *분석 → 표시 → 실행* 흐름으로 읽힙니다
- **상단 브랜드 헤더도 같은 언어로** — 최상단 줄(테마 전환·언어·프리셋·저장·삭제·Calibration)의 파란 강조 필·테두리를 걷어내고 **테두리 없는 컨트롤 + 마우스 올리면 배경**으로 통일했습니다. 캡처 패널의 `+ Grp` 버튼도 같은 방식으로 정돈했습니다
- **Transfer Function 오른쪽 패널도 N2로** — 신호 발생기와 측정 카드를 앱 전체와 같은 결로 정리했습니다. **신호 타입(Pink/White/Sine/Sweep/파일)을 하나의 드롭다운으로 합쳐** 레벨과 한 줄에 배치(다섯 줄 → 세 줄로 컴팩트), 파란 강조 필·회색 배경을 걷어내고 테두리 있는 담백한 셀로 바꿨습니다. 측정 카드는 스펙트럼 카드와 똑같이 **중립 테두리 + 색 스와치 + 색 점을 눌러 측정 시작/정지**로 통일했습니다
- **소스별 색 변경 (Spectrum · Transfer Function)** — 카드마다 곡선 색을 따로 바꿀 수 있습니다. 카드를 **우클릭 → 색 변경…** (스펙트럼은 카드 선택 후 툴바 `Color`도 가능). 바꾼 색이 곡선은 물론 카드 표시(체크박스·색 점·번호)에 함께 반영되고, 재시작해도 유지됩니다. Transfer Function은 **1번(기준 측정)·추가 측정 카드 모두** 지원 — 여러 마이크를 원하는 색으로 구분해 겹쳐 봅니다
- **캡처를 많이 쌓아도 끊기지 않게** — 캡처가 수십·수백 개로 늘어도 추가하거나 선택할 때 화면이 멈칫하던 것을 근본 수정. 새 캡처를 그릴 때 **전체를 다시 그리지 않고 필요한 것만 얹도록** 바꿔, 개수와 무관하게 항상 매끄럽습니다(스펙트럼·TF 공통)
- **Transfer Function Reference·측정 미터 정렬** — 오른쪽 Measurement 패널에서 Reference를 측정 카드와 같은 카드 형태로 맞춰, **레벨 바가 좌우로 나란히** 정렬되도록 정리
- **스펙트럼 INPUT 카드 조작을 측정 카드와 동일하게 통일** — 스펙트럼 INPUT 소스 카드의 인라인 **✕ 삭제 버튼을 없애고**, 삭제·이름 변경을 **우클릭 메뉴**로 통일했습니다(Transfer Function 측정 카드와 동일). 모든 카드 헤더가 같은 모양이 되고, 실수로 지우기 쉬운 ✕ 대신 **의도적 우클릭**으로만 삭제합니다(이름 변경은 우클릭·더블클릭 둘 다)
- **커서 리드아웃 리디자인 — 커서에 붙는 태그** — 그래프에 마우스를 올렸을 때 뜨는 값 상자를 새로 그렸습니다. 화면 가운데 고정돼 파형(특히 IR의 0ms 임펄스)을 가리던 상자를 없애고, **커서 세로선 위에 작은 태그로 붙어 아래 꼭지(▽)로 그 지점을 가리키며 커서를 따라 움직입니다**. 값 색은 **그 곡선의 색**(1번=녹색/사용자색, 추가 카드=카드색)으로 표시해 어느 채널을 읽는지 한눈에 들어옵니다. 낡은 글로우 테두리·주황 값을 걷어내고 **축 라벨과 같은 글씨체**·하어라인으로 정돈 — IR·Magnitude·Phase·Spectrum 전 그래프 공통. IR은 커서가 있는 자리에만 태그가 떠서 **0ms 임펄스가 더는 가려지지 않습니다**. 또한 **캡쳐를 클릭해 선택한 상태에서 커서를 올리면 그 캡쳐의 값이 뜹니다**(이전엔 라이브 곡선만 읽어 캡쳐 위에선 값이 안 나왔음) — 캡쳐색으로 표시
- **아이콘 전반 정제** — 재생·정지 등 아이콘을 더 높은 해상도로 그리고 꼭짓점을 둥글려 **더 매끈하게** 다듬었습니다(앱 전체 공통)
- **딜레이 적용 시 크기 응답이 즉시 올바른 레벨로 (Smaart 방식 시간정렬)** — 이전엔 딜레이가 화면상 위상만 정렬하고 크기·코히런스는 어긋난 채 계산돼(특히 Adaptive 고역), 크기가 천천히 기어가듯 수렴했습니다. 이제 딜레이만큼 **FFT 이전 시간영역에서 정렬**해 위상뿐 아니라 **크기·코히런스도 딜레이를 넣는 순간 올바른 레벨로 딱 잡힙니다**. Reference·측정 미터와 IR 위치는 그대로
- **세부 다듬기** — TF Magnitude/Phase 그래프에 **줌·팬 조작 안내 툴팁**(휠·드래그·Shift팬·리셋), Group Delay 세로축 눈금을 **5ms 균일 간격**으로 정리, 오라제이션 정지 버튼 안내 툴팁
- **마지막 탭 기억** — 앱을 다시 열면 **마지막에 보던 탭**(Spectrum / Transfer Function / Stereo)으로 시작합니다. 탭 레이아웃·컨트롤 설정도 함께 복원돼 "보던 화면 그대로" 이어집니다
- **Transfer Function 응답 대칭화 (Smaart식)** — 크기 곡선이 **올라갈 때·내려갈 때 같은 속도로** 반응하도록 정리했습니다(이전의 비대칭 fast-release 제거). Response 설정이 양방향 속도를 함께 조절하며, 라이브 튜닝에 맞게 값도 짧게 조정(**Fast 0.5초 … Stable 8초, 기본 Normal 2초**) — 반응적이면서 부드럽게

### 🐛 버그 픽스 (전체 코드 점검)
- **[v2.0.1] 측정 마이크가 오래 뒤 조용히 멈추던 문제 수정 — 자동 복구** — 마이크를 오래 열어두면 측정이 어느 순간 소리 없이 멈추던 증상을 잡았습니다. 원인은 macOS가 측정 중(특히 화면이 꺼질 때) **내장 마이크를 잠깐 "사라졌다"고 알려오면** 앱이 측정을 정지시키는데, **장치가 곧 돌아와도 다시 켜주는 동작이 없어** 조용히 멈춘 채 방치되던 것입니다(정전됐다 전기가 들어와도 자동 재가동이 안 되던 셈). 이제 **장치가 잠깐 사라졌다 돌아오면 멈췄던 측정을 자동으로 다시 시작**합니다. 더불어 측정 중 시스템 유휴 절전 차단, 콜백이 순간 끊길 때 같은 장치 재오픈도 함께 적용했습니다. 실제로 장치가 계속 없을 때만(케이블 제거 등) '연결 끊김'으로 남습니다
- **우클릭 메뉴가 밝은 색으로 뜨던 문제 수정** — 카드 우클릭(이름 변경·삭제) 등 컨텍스트 메뉴와 일부 툴팁이 다크 테마와 달리 밝은 회색/흰색으로 표시되던 것을, 앱 전체(팝아웃·대화상자 포함) 어두운 테마로 통일
- **로그 스케일에서 스펙트럼 라이브 곡선이 안 뜨던 문제 수정** — 이번 버전 작업 중 생긴 회귀로, 기본(로그) 스케일에서 스펙트럼 측정 곡선·피크·커서가 표시되지 않던 것을 바로잡음
- **캡처가 전혀 되지 않던 문제 수정** — 내부 구조 재정비 과정에서 생긴 회귀로, 스펙트럼·Transfer Function에서 캡처(Space)를 눌러도 아무 일도 일어나지 않던 것을 바로잡음(캡처 색 팔레트 초기화 누락)
- **앱 안에서 릴리즈 노트·사용 설명서가 안 열리던 문제 수정** — 같은 회귀로, 메뉴의 **Release Notes**와 Help의 **사용 설명서**가 "파일을 찾을 수 없습니다"로 뜨던 것을 바로잡음(리소스 파일 경로가 잘못된 폴더를 가리키던 문제)
- **캡처 색이 라이브 곡선 색과 겹치던 문제 수정** — 캡처 색을 자동으로 고를 때 **지금 화면에서 쓰는 라이브 색 전부**(스펙트럼 바 색·Transfer Function 기준 곡선·모든 카드 색)를 피하도록 개선. 특히 두 번째 캡처가 라이브 초록과 똑같은 초록으로 나오던 것을 해결 — 캡처마다 라이브·다른 캡처와 최대한 구분되는 색이 잡힙니다
- **라우드니스(LUFS) 표준 정합 — 약 3 LU 상향** — 좌우 채널 합산 방식이 국제표준(ITU-R BS.1770)과 달라 **모든 라우드니스 값이 약 3 LU 낮게** 나오던 문제 해결. 이제 다른 표준 라우드니스 미터·방송 납품 기준(−23 LUFS 등)과 값이 맞습니다 (LRA는 범위값이라 그대로, PLR은 그만큼 조정)
- **USB 장치 자동복구가 스펙트럼 단독 측정에서 동작하지 않던 문제 수정** — 스펙트럼만 측정하다 USB 인터페이스를 뽑았을 때 무음으로 멈춘 채 복구되지 않던 것을 바로잡음
- **Stereo Loudness 오른쪽 채널을 Ch 1로 지정하면 Ch 2로 잘못 잡히던 문제 수정**
- **측정 카드를 멈춘 채 다른 카드만 켜고 캡처하면 아무것도 안 되던 문제 수정** — 이제 표시 중인 카드가 있으면 캡처됩니다
- **라우드니스 적분/LEQ 리셋 정리** — 적분을 리셋해도 PLR·트루피크가 이전 값을 쓰던 것, LEQ 시간 리셋이 최대값(Max)을 안 지우던 것 수정
- **측정 카드를 삭제한 뒤 캡처하면 유령 곡선이 생기던 문제 수정**
- **주파수축 확대 시 화면 밖 곡선이 왼쪽 여백으로 삐져나가던 문제 수정** — 곡선을 그래프 영역으로 정확히 잘라 표시
- **설정 저장 안정성 보강** — 디스크가 가득 차거나 권한 문제가 있어도 앱이 죽지 않고, 저장 도중 문제가 생겨도 기존 설정이 보존되도록(원자적 저장) 개선
- **Transfer Function RTA 칸이 빈 화면으로 안 뜨던 문제 수정** — 마이크 신호가 들어오는데도 RTA(옥타브 스펙트럼) 칸이 "Press Start"만 표시되던 문제 해결(빈 화면 표시 플래그가 RTA에서 해제되지 않던 버그)
- **그래프 맨 위 눈금·IR 양끝 눈금 잘림 정리** — Phase/Magnitude/Live IR 세로축 맨 위 값(+150° 등)이 카드 상단에 잘리던 것, IR 시간축 양끝 라벨이 축 값·모서리와 겹치던 것 정리
- **패널을 짧게 줄였을 때 축 눈금 글씨가 겹치던 문제 수정** — 세로축 라벨이 서로 뭉치거나(Magnitude·Phase), IR 바닥의 시간·거리 눈금이 카드 테두리에 겹치던 것 해결. 그리드 선은 그대로 두고 **글씨만 자동으로 성기게** 표시하도록 바꿔, 아무리 짧게 줄여도 안 겹칩니다(스펙트럼·TF 공통)
- **패널 제목 글씨를 눌러도 메뉴가 안 뜨던 문제 수정** — IR/Phase/Magnitude 제목(예: `Magnitude + Coherence ▾`)의 클릭 영역이 글씨 아래쪽을 못 덮어 좌상단 모서리를 눌러야 했던 것을 바로잡음. 이제 제목 글씨 어디를 눌러도 메뉴가 뜹니다. 제목 글씨도 **조금 키워** 더 잘 보이게 다듬음
- **Auto 딜레이 찾기가 값이 튀며 안 잡히던 문제 수정** — 딜레이 시간영역 정렬 도입 이후, Auto(Find Delay)를 누를 때마다 값이 참값으로 수렴하지 않고 두 값 사이를 오가던(예: `2.65 ↔ 0.46 ms`) 회귀를 바로잡음. 누적 스펙트럼이 이미 현재 딜레이로 정렬돼 있어 파인더가 "잔여값"만 보던 것을, **현재 적용 딜레이에 잔여를 더해** 항상 참 딜레이로 한 번에 고정되게 수정(Primary·추가카드·전체 찾기 3경로). 이후 위상이 평탄해지고 임펄스가 마커에 정확히 정렬됨
- **내부(SigGen)·다른 장치 레퍼런스 측정에서 위상/임펄스가 어긋나던 문제 수정** — 딜레이 정렬이 같은 장치(duplex) 경로에만 적용되고, 내부 신호원 레퍼런스나 출력·측정이 다른 장치인 경우엔 빠져 있어 위상이 과도하게 감기고 IR 임펄스가 딜레이의 2배 위치로 밀리던 것을 바로잡음(측정 경로 전체 정렬 일원화)

---

## v1.8 — SPL 미터 재디자인 · TF 측정 개선 · 안정성  *(2026.06)*
SPL 미터·TF 화면을 새로 다듬고, 측정 정확도와 장시간 안정성을 손봤습니다.

### ✨ 새 기능 · 개선
- **SPL 미터 디자인 정비** — SPL 미터 창을 한층 또렷하고 차분하게: 각 카드 아래쪽에 현재 레벨에 따른 **신호등 색 번짐**(안전=초록 / 주의=노랑 / 위험=빨강)을 더해 숫자를 읽지 않아도 상태가 한눈에 들어오고, 값 색을 LEVEL 패널과 같은 톤으로 통일(A가중 **소프트 블루** / C가중 **소프트 와인** / 광대역 **흰색** — 진한 파랑·보라를 덜어 눈이 편하게, 경고·피크 색은 유지). **검정 배경·회색 카드 → 회색 배경·검정 카드로 반전**해 다른 화면과 통일감을 주고(검정 카드 위라 글로우가 더 선명), 시계 카드는 **소프트 화이트**로 차분하게 정리. **Max 리셋을 카드별로** — 헷갈리던 타이틀바의 전역 'Reset Max' 버튼을 없애고 **각 카드에 자체 Max 리셋 버튼**을 두어 그 카드만 초기화. 리셋 버튼은 마우스를 올리면 **배경칠 없이 아이콘 색만** 또렷해지도록 다듬음. SPL 미터·알람 창의 **타이틀바 아이콘도 정제** — 상단고정 토글은 자물쇠 → **압정(핀)**, 설정은 슬라이더 → **기어**로 더 세련되고 의미가 분명하게
- **Transfer Function 3그래프 카드로 분리** — Live IR · Phase · Magnitude+Coherence 세 그래프를 **각각 떠 있는 둥근 카드**로 나눠 한눈에 구분되게 정리. 그래프마다 고유 색 제목(IR=청록 / Phase=보라 / Magnitude=파랑)으로 지금 보는 분석이 무엇인지 바로 보임. 클릭해 선택한 그래프는 상단에 **SPECTRA 시그니처 그라디언트 엣지**로 표시(기존 파란 사각 테두리 대체). 카드 사이 경계에도 같은 그라디언트를 아주 옅게 더해 브랜드 톤 통일
- **Transfer Function 화면 정리** — Magnitude+Coherence 그래프의 코히런스 기준선(점선)과 우측 % 눈금을 없애 화면을 깔끔하게 정리. 정확한 코히런스 값은 커서를 올리면 리드아웃에 %로 그대로 표시됨. 코히런스 표시도 **주황 선 → 블루 반투명 채움 밴드**로 바꿔(신뢰도 '영역'을 면으로), 초록 응답 곡선과 부딪히지 않게 정리
- **Transfer Function 측정 시작 시 자동 Y축 맞춤** — 측정(핑크/스윕)을 시작하면 Magnitude 그래프가 더블클릭 없이 **첫 데이터에서 한 번 자동으로 화면에 맞춰** 들어오도록 개선(측정 중 점프는 없음)
- **Transfer Function 곡선 하강 속도 2배** — 측정 곡선이 신호가 줄어들 때 **천천히 내려오던 것을 약 2배 빠르게**(fast release) 개선. 레벨이 떨어지는 구간만 빠르게 따라가고, 올라갈 때·안정 구간은 기존 평균 그대로라 안정성·정확도는 유지(Adaptive·Single 엔진 공통)
- **사이드바 섹션 헤더 정리** — LEVEL · INFO · INPUT 앞에 제각각이던 아이콘을 **단색 액센트 세로 바**로 통일해 더 깔끔하고 일관되게
- **LEVEL/INFO 값 색상 정돈** — 값 색을 정리: 광대역(SPL·Peak·Dominant)과 INFO 값은 **흰색**으로 또렷하게, A가중(dBA·LAeq)은 **소프트 블루**·C가중(dBC·LCeq)은 **소프트 와인**으로 차분하게 코딩. 채도 낮춰 눈이 편하고 위계가 살아남
- **LEVEL 헤더 버튼 차분하게** — SPL 미터(dB)·SPL 알람(신호등) 버튼을 평소엔 **은은한 회색**으로 두고, 마우스를 올리면 **소프트 블루로 살아나게** 정리. 차분해진 값 영역과 톤을 맞춤
- **SPL 미터 버튼 아이콘 변경** — LEVEL 패널의 SPL 미터 열기 버튼 아이콘을 "새 창 열기" 화살표에서 **"dB"** 표시로 바꿔, 무슨 기능인지 한눈에 알 수 있게 함
- **Transfer Function 별도 창에서도 캡쳐 패널 사용** — TF 탭을 별도 창으로 분리(팝아웃)하면 **캡쳐 목록 패널이 그 창으로 함께 따라가** 멀티모니터에서 캡쳐를 바로 켜고/끄고 관리할 수 있게 함. Spectrum 팝아웃 창에서는 무의미하던 캡쳐 패널 토글 아이콘을 정리(숨김)
- **SPL 미터 LEQ 카드 이름 정리** — LEQ 카드 제목을 'dB LAeq / dB LCeq' → **'LAeq / LCeq'**로 줄여 옆 카드(SPL A Slow 등)와 형식을 통일
- **SPL 알람 창 배경 통일** — 알람 창 배경을 검정 → **회색**으로 바꿔 SPL 미터 창과 같은 톤으로 정리(카드가 배경 위에 또렷하게 떠 보임)
- **SPL 알람 LAeq/LCeq 적분 진행 바 + 리셋** — 알람 지표가 LAeq·LCeq(적분)일 때 **카드 하단에 SPECTRA 그라디언트 진행 바 + 리셋(↻) 버튼**을 추가(SPL 미터 LEQ 카드와 동일). 얼마나 적분됐는지 한눈에 보이고, 버튼으로 적분을 0부터 다시 시작. 순간값 지표(SPL A/C Slow 등)일 땐 표시되지 않음

### 🐛 버그 픽스
- **라우드니스 장시간 구동 멈춤 수정** — Stereo Loudness를 오래(수 시간) 켜두면 화면이 점점 느려지다 멈추던 문제 해결. 적분(Integrated)·LRA 계산을 오디오 청크마다(초당 약 90회)가 아니라 새 측정 블록이 들어올 때(초당 10회)만 수행하도록 정리 — 경과 시간이 길어져도 부하가 일정하게 유지됨. 측정값은 동일
- **측정 미터·분석 간헐 멈춤 수정** — 같은 오디오 인터페이스로 신호를 내보내며 동시에 입력을 측정할 때(특히 스윕 측정 후 핑크/연속 신호로 전환), 간혹 **레퍼런스·측정 미터가 안 뜨고 Transfer Function 그래프가 빈 채로 멈추던** 문제 해결. 입력 스트림이 열렸는데도 오디오 콜백이 시작되지 않는 macOS CoreAudio 경합을 감지해 **자동으로 다시 열도록**(약 2초 내 복구) 보강
- **스윕 측정 후 위상 깨짐 수정 (자동 0ms 정렬)** — 정밀(Sweep) 측정은 왕복 지연을 이미 제거하는데, 핑크에서 넣어둔 딜레이가 스윕 위상에 잘못 더해져 **위상이 깨지던** 문제 해결. 측정이 끝나면 **카드 딜레이가 자동으로 0ms로 맞춰져** IR 임펄스가 0ms 중앙에 오고 위상도 정상 표시
- **스윕 ↔ 핑크 레벨 불일치 수정** — 정밀(Sweep) 측정의 Magnitude가 라이브(핑크)보다 약 33dB 낮게 떠 **두 곡선을 겹쳐 비교할 수 없던 문제** 해결. 스윕은 송출 디지털 신호 기준, 핑크는 캡쳐된 레퍼런스 채널 기준이라 0dB 기준이 달랐던 것을, 스윕을 **레퍼런스 채널 레벨로 정규화**해 핑크와 같은 절대 레벨로 표시 → 핑크 캡쳐와 스윕 캡쳐를 같은 높이에서 직접 비교 가능
- **신호 제너레이터 기본 소스 정리** — 앱 시작 시 신호 소스가 Pink·Sweep **두 개가 동시에 켜져 보이던 문제** 수정. 항상 하나만 선택되며 기본은 **Pink**(Sweep은 누를 때 켜지는 1회 측정 모드)
- **제너레이터 File 소스 복원 정리** — 재시작·프리셋 로드 시 **File이 선택된 채로 떠도 정작 불러온 파일은 없어** 무동작이던 문제 수정(파일 버퍼는 저장되지 않음). 이제 Sweep과 동일하게 복원 시 **Pink로 시작**하고, File은 그 세션에서 직접 불러올 때만 선택됨
- **삭제한 스펙트럼 소스 카드가 되살아나던 문제 수정** — 입력 소스 카드를 삭제하고 앱을 재시작하면 지운 카드가 **다시 나타나던 문제** 해결. 소스 변경(추가/삭제/이름/채널)이 마지막 사용값(세션)에 즉시 반영되도록 보강 → 종료 시점의 카드 구성 그대로 복원됨
- **시작 전 빈 화면에 초록 바닥선이 보이던 문제 수정** — 옥타브(RTA) 뷰에서 측정을 시작하기 전에도 그래프 맨 아래에 **초록색 바닥 막대선**이 깔려 보이던 문제 해결. 시작 전(빈 상태)에는 라이브 막대와 우상단 주파수 배지를 그리지 않아 "Press Start" 안내만 깔끔하게 표시됨. 특히 캘리브레이션이 켜져 화면이 노이즈 플로어로 확대될 때 크게 거슬리던 부분 정리
- **Start/Stop 버튼 파란 테두리 깜빡임 수정** — 측정 카드·제너레이터·메인의 Start/Stop 버튼을 누르면 간헐적으로 생기던 macOS 파란 포커스 링 제거(전 트랜스포트 버튼 통일)
- **라우드니스 COMPLIANCE 배지 매끄럽게** — Stereo Loudness의 COMPLIANCE 상태 원형 배지 테두리가 들쭉날쭉(계단현상)하던 문제 해결. 원을 안티앨리어싱으로 직접 그리도록 바꿔 어느 크기에서도 매끈한 원으로 표시
- **정밀 스윕 1초·2초 결과 일치** — 같은 스피커를 1초 스윕과 2초 스윕으로 재면 저역·레벨이 서로 어긋나던 문제 해결. 분석 창 길이를 스윕 길이와 **무관하게 고정**해 어떤 길이든 같은 해상도·같은 임펄스 위치로 나오고, 레벨 정규화가 **측정이 무너지는 고역 구간에 휘둘리지 않도록** 보강 → 1초·2초 캡쳐를 겹쳐도 중역이 그대로 포개짐(길이가 짧을수록 고역 해상이 낮은 건 스윕의 물리적 특성)
- **스윕 임펄스(IR) 더 깔끔하게** — 정밀 스윕 임펄스 응답의 측정 대역 경계에서 생기던 잔물결(링잉)을 **경계 테이퍼**로 완화해 임펄스가 한층 깨끗하게 표시됨
- **스윕 ↔ 핑크 전환 정리 (3종)** — ①스윕 측정 후 핑크로 돌아와 마이크 위치가 바뀌어 딜레이를 다시 찾아도 **적용되지 않던 문제** 해결 ②측정 카드 Start를 누르지 않아도 스윕이 측정되던 것을 정리 — 이제 **Play를 누르면 측정 카드가 자동으로 시작** ③그 결과 스윕→핑크 전환 시 **핑크가 간헐적으로 측정되지 않던 문제**도 함께 해소
- **미세 스무딩(1/48)에서 버벅임·소리 끊김 수정** — Transfer Function 스무딩을 1/48처럼 아주 곱게 두면 곡선이 뾰족해져 그리는 비용이 폭증해 **핑크 노이즈가 끊기고 화면이 버벅이던 문제** 해결. 라이브 곡선을 가볍게 그리도록 최적화(디테일·포인트는 그대로 유지) → 1/48에서도 부드럽게 동작

---

## v1.7 — 적응형 엔진 · 프리셋 · 다국어  *(2026.06)*
라이브에 강한 새 TF 엔진, 세 탭 설정을 기억하는 프리셋, 한/영 언어 전환, 라우드니스 화면 재정비.

- **TF Adaptive 엔진 (신규·기본)** — Transfer Function에 **멀티레이트(적응 해상도)** 라이브 엔진 추가. 저역은 고해상도, 고역은 빠른 응답으로 한 화면에 → 현장 튜닝(이동·EQ 조정 중)에 강함. 툴바 **Engine** 셀렉터로 **Single ↔ Adaptive** 전환(기본 Adaptive). 딜레이 파인더·IR·위상 모두 Single과 동일하게 동작
- **정밀 스윕(Farina ESS)** — **Sweep** 버튼으로 지수 스윕 1-shot 측정 → 선형 무왜곡 TF + **THD/SNR** 표시 (정밀·THD는 스윕, 라이브 현장은 Adaptive — REW/Smaart식 워크플로)
- **응답 속도(Response) 컨트롤** — 기존 **Avg → Response**로 재설계. **Fast · Quick · Normal · Smooth · Stable** 한 단어로 직관 선택(기본 Normal=16s). 바꾸면 **즉시** 체감(누적 유지 — 재수렴/쓸어내림 없음), 곡선은 **30fps 보간**으로 끊김 없이 부드럽게
- **스펙트럼 표시 자연스럽게 — 빠른 상승·느린 하강** — 스펙트럼 막대/곡선이 소리에 **빠르게 반응해 올라오고**, 잔향처럼 **천천히 부드럽게 내려오도록** 응답(ballistic)을 피크미터식으로 재설계. 내려오는 속도는 **Response**로 조절(Fastest 약 0.4초 … Slowest 약 2.5초), 올라오는 부드러움도 다듬어 막대가 톱니처럼 튀지 않게. 멀티소스(추가 카드)도 동일 응답으로 일치. 기존 **Avg(평균) 컨트롤은 제거** — 새 방식의 느린 하강이 평균 안정화를 대체하고, 응답 조절은 **Response 하나로 일원화**
- **설정 자동 기억** — 세 탭(Spectrum·TF·Stereo)의 모든 컨트롤 값을 자동 저장 → 앱을 다시 열면 **지난 세션 그대로 복원** (장치·채널 포함, 없으면 현재 유지)
- **이름 프리셋 (신규)** — 현재 세 탭 설정을 **이름 붙여 저장**하고 드롭다운에서 한 번에 불러오기/삭제. 상단 헤더의 **Preset** 바(저장·삭제 아이콘). "클럽 튜닝", "방송 믹스" 식으로 현장별 세팅을 즉시 전환
- **한국어/영어 언어 전환 (신규)** — 상단 헤더 **지구본 버튼**으로 KO ↔ EN 전환(설정 저장 후 자동 재시작). 첫 실행은 **OS 언어**를 따름. 한글 모드에선 **메인 화면·도구창은 영어**(친숙·간결), **마우스 hover 툴팁·설정 다이얼로그·우클릭 메뉴는 한국어**(초보자 친화 쉬운 말)로 표시
- **라우드니스 화면 재정비** — PROGRAM LOUDNESS 거대 숫자를 **AVG(누적 평균=납품 기준) | LIVE(실시간 Short-term) 2분할 동시 표기**로(토글 없이 한눈에 — 가운데 구분선, COMPLIANCE 판정은 AVG 기준), **레이더 외곽 레벨미터를 타겟 중심으로 재배치**(설정한 타겟 LUFS가 항상 12시 상단 중앙 — 눈금·채움 막대·피크홀드가 타겟 따라 함께 이동. 타겟 -16이면 -16이 가운데), 히스토리 그래프를 **부드러운 곡선 + 레이더와 같은 60초** 윈도우로, **벡터스코프·레이더 확대**(창 키우면 더 커짐), 창을 최소~최대로 조절해도 안 잘리게 레이아웃 정비
- **창 최대화로 꽉 채우기** — 헤더 **더블클릭 = 최대화**(메뉴바 아래로 화면 가득). 기존 전체화면이 시스템 메뉴바에 헤더가 가려지던 문제 해소 — 메뉴는 항상 접근 가능 (Show Mode는 전체화면 유지)
- **탭 별도 창(팝아웃) 정비** — 세 탭(Spectrum·TF·Stereo)을 별도 창으로 띄울 때: 흰 네이티브 타이틀바 → **다크 + 브랜드 헤더**(그라디언트 마크 + SPECTRA + 탭이름, 가운데 정렬)로 통일하고 OS 기본 제목 텍스트는 숨김, **타이틀바 우측 ⌃ 버튼으로 툴바(메뉴) 접기/펴기**, **헤더 더블클릭 = 최대화↔복원**. 팝아웃 툴바 버튼이 검은 박스로 보이던 것과 TF 별도 창 타이틀바가 라이트 모드에서 검게 남던 문제도 수정
- **마이크 캘리브레이션 정리** — 캘리브레이션 창 안내·버튼을 쉬운 영어로(한글 모드에선 한국어)
- **라이트 모드 전체 정비** — 라이트 모드에서 일부만 밝던 부분까지 모두 테마 반영: CAPTURES 패널·SPL 미터·각종 다이얼로그·스크롤바·팝아웃 버튼·타이틀바·헤더 버튼(언어/저장/삭제)·Stereo 카드(PROGRAM LOUDNESS·지표·COMPLIANCE). 다크 모드는 그대로 유지
- **입력·UI 다듬기** — 캡처/프리셋 이름 입력칸에서 영문 글자(L·G·S)가 단축키로 잘못 먹던 문제 수정, Stereo 툴바의 미사용 토글 버튼 제거
- **측정 카드 입력 레벨미터 (게인 셋업)** — TF 측정 카드의 **Start를 누르면 그 카드 입력 레벨미터가 켜지고** Stop으로 끔(제너레이터 재생 여부와 무관). 측정 시작 전 콘솔·인터페이스 게인을 미리 맞추기 좋게
- **입력 레벨미터 피크 표시** — Spectrum 입력 레벨미터를 **RMS 채움 + 실제 피크 틱**으로(피크 헤드룸이 한눈에, Smaart식) → 게인 셋업 시 클리핑 여유 확인에 유용
- **추가 측정 카드 커서 정보 일원화** — 측정 카드를 여러 개 띄웠을 때 2번 이후 카드의 커서 리드아웃도 1번 카드처럼 **주파수·dB·위상·코히런스**를 모두 표시(이전엔 일부 빠졌음)
- **무끊김 출력 채널 변경** — 제너레이터 재생 중 출력 채널을 바꿔도 **끊김 없이 즉시** 새 채널로 전환(스트림 재시작 없음)
- **안정성 다듬기** — Adaptive 엔진에서 측정 카드 여러 개가 모두 그려지도록 수정, 입력 채널을 바꿀 때 장치가 끊긴 것으로 오인하던 문제 해결, 제너레이터를 켜고 끌 때 카드 레벨미터가 멈추던 문제 수정

---

## v1.6.1 — 캡쳐를 한 손에  *(직전)*
쌓인 캡쳐를 한 번에 다루도록. (+ 라이브 튜닝 디테일·브랜드 다듬기)

- **딜레이 거리(m) 표기** — TF 툴바 **Units** 토글(ms / ms·m / m)로 딜레이를 거리(미터)로도 표시. IR 마커·커서·시간축과 측정 카드 딜레이 칸에 함께 적용 (음속 343 m/s, Delay Finder 고급설정에서 변경 가능)
- **RTA 막대 입체감** — 옥타브(RTA) 막대를 평면 단색 → 세로 그라디언트(위 밝게/아래 어둡게)로 다듬어 더 또렷하게 (라이브·캡쳐·추가소스 일관)
- **시작 화면 갱신** — 항상 현재 버전이 표시되는 브랜드 스플래시
- **캡쳐 일괄 표시/숨김** — 캡쳐 패널 머리말의 그라디언트 웨이브 버튼으로 현재 탭 전체를 한 번에 켜고/끄기 (스마트 토글)
- **그룹별 일괄 토글** — 각 그룹 머리말에서 그 그룹만 한 번에 표시/숨김
- **캡쳐 색 구분 강화** — 캡쳐마다 라이브 입력 색과도, 다른 캡쳐와도 뚜렷이 구별되는 색을 자동 배치 (캡쳐가 많아져도 한눈에 구분)
- **캡쳐 위치 자유 지정** — 그룹 이름을 누르면 새 캡쳐가 그 그룹으로, 다시 누르면 미지정으로. 패널에 "캡쳐 위치"가 표시돼 한눈에 (접기/펼치기는 화살표로 분리)
- **딜레이 파인더 단축키** — TF 딜레이 찾기 창에서 **L**로 다시 찾고, **Enter**로 바로 적용
- **캡쳐 전체 삭제** — 캡쳐 패널에서 우클릭 → **Delete All** (확인 후 모두 삭제, Spectrum·TF 동일)
- **리캡쳐(제자리 다시 캡쳐)** — 기존 캡쳐의 색·이름·그룹은 그대로 두고 곡선만 현재 측정값으로 갱신. 캡쳐 패널 우클릭 → **Recapture** 또는 단축키 **R** (Space=새 캡쳐). EQ·게인을 만진 뒤 같은 트레이스를 지우고 다시 뜰 필요 없이 한 번에 업데이트 (Spectrum·TF 동일)
- **알림창 브랜딩** — 모든 팝업을 SPECTRA 톤으로 (다크 + 그라디언트 라인 + 상황별 아이콘 + 브랜드 버튼). 삭제 같은 위험 동작은 빨강으로 명확히
- **스펙트럼 브랜드 컬러** — FFT 곡선을 SPECTRA 시그니처 그라디언트(저역→고역)로 표시 + 곡선이 더 매끄러워짐
- **측정 레벨 정확도 향상** — 스펙트럼/옥타브 밴드 레벨을 표준 파워 정규화(윈도우 ENBW 보정)로 재계산. 순음과 광대역(핑크노이즈)이 같은 기준으로 읽혀, 캘리브레이션 한 번으로 절대 SPL이 정확하게 맞고 업계 표준 측정기와 레벨이 일치
- **릴리즈 노트 메뉴** — Help 메뉴 → "릴리즈 노트"에서 버전별 변경사항을 앱 안에서 바로 확인
- **LEQ 시간 미터** — LAeq·LCeq 카드 안에 SPECTRA 그라디언트 진행 바로 LEQ 적분 시간을 표시 + 리셋 버튼 (창은 컴팩트 크기로 열림)
- **SPL 미터 리사이즈 개선** — 카드가 창을 꽉 채우고, 창을 넓히면 숫자가 큼직하게 커지며 세로 가운데 정렬 (Smaart 스타일)
- **SPL 미터 시계 카드** — 설정에서 칸에 "Clock"을 골라 24시간 시계(HH:MM)를 큼직하게 표시 (다른 지표처럼 행×열 어디든 배치)
- **SPL 미터 지표 대폭 확장 (Smaart 스타일)** — SPL A/C Slow·SPL Slow(Z) + SPL A/C Fast·SPL Fast(Z) + Peak·Peak C·FS Peak 까지 칸별로 자유 선택. Fast(125ms)/Slow(1s) 시정수, 풀스케일 디지털 피크(dBFS) 포함
- **SPL 미터 측정 소스 선택** — SPL 미터가 어느 입력을 읽을지 설정창(⚙)의 **"측정 소스"** 드롭다운에서 선택 (Primary + 추가 입력 카드 중). 멀티 인터페이스에서 원하는 채널만 SPL로 측정 (카드 삭제 시 Primary로 자동 복귀)
- **SPL 임계 알람 창 (신규)** — 측정 레벨이 설정한 한계에 다가가면 **초록→노랑→빨강** 신호등으로 즉시 경고하는 독립 창. 큰 숫자 + 남은 여유/초과 dB + 상태(OK·AMBER·OVER), 초과 시 부드러운 깜빡임. **마스터 지표**(dB LAeq·LCeq·SPL A/C 등)·**한계**·**노랑 여유**·**LEQ 시간**을 ⚙에서 설정 (LEQ 지표는 `dB LAeq 10min`처럼 적분 시간 함께 표기). SPL 미터를 열지 않아도 **독립 동작**(자체 측정 엔진). **View 메뉴** 또는 **LEVEL 패널의 신호등 버튼**으로 열기 — 공연장 소음 한계 모니터링에 바로 활용
- **창 항상 위 고정 (자물쇠 토글)** — SPL 미터·알람 창 타이틀바의 **자물쇠 버튼**으로 본창 위에 항상 고정/해제 (잠금=파랑). 다른 작업 위에 띄워두고 모니터링 (켜고 끌 때 깜빡임 없음)
- **마우스 떠나면 카드만** — SPL 미터·알람 창에서 마우스가 벗어나면 타이틀바가 부드럽게 접혀 **숫자·신호등만 깔끔하게** 남고, 마우스를 올리면 다시 나타남 (FOH에서 멀리서 글랜스용)
- **커서에 음이름 표시** — 스펙트럼(FFT·옥타브·스펙트로그램)에서 커서를 올리면 주파수 옆에 **음이름**(예: `247 Hz · B3`)을 함께 표시. 룸모드·피드백·악기 주파수를 바로 식별
- **단축키 치트시트** — **?** 키(또는 Help → Keyboard Shortcuts)로 모든 단축키를 키캡 스타일로 한 화면에 (S·Space·R·G·L·화살표 등 발견성↑)
- **진짜 True Peak (dBTP)** — 라우드니스의 트루피크를 **4× 오버샘플(ITU-R BS.1770)**로 측정 → 샘플 사이 피크(inter-sample)까지 잡아냄. 기존 샘플 피크는 놓치던 클리핑 위험을 정확히 표시 (−1 dBTP 초과 시 빨강 경고)
- **라우드니스 타겟 프리셋 + 편차** — 타겟을 **EBU R128(−23) · ATSC A/85(−24) · 스트리밍(−14) · Apple Music(−16) · −18 LUFS** 중 선택. **Δ Target**(통합 라우드니스의 타겟 대비 ±LU)을 색으로 표시(±1 안 초록). **선택 즉시** 레이더 타겟 링 반영(측정 시작 안 해도)
- **LU 상대 표시 토글** — **LU** 버튼으로 M·S·I를 LUFS(절대) ↔ LU(타겟 기준 상대)로 전환 (방송 EBU 모드 방식)
- **신호 제너레이터에 사인파(Sine) 추가** — Pink·White·Sweep에 더해 **Sine** 선택 → 주파수 설정(10~24000Hz)으로 순음 출력. 단일 주파수 측정·캘리브레이션 확인에 바로 사용 (버튼에 설정 주파수 표시, 레벨은 기존 Level 컨트롤)
- **카드 삭제 버튼 통일** — Spectrum 소스 카드·TF 측정 카드·캡쳐 그룹의 삭제 버튼을 모두 동일한 **✕**로 통일 (평소 회색, 마우스 올리면 빨강)
- **입력 소스 카드 레이아웃 다듬기** — 레벨 미터가 카드 폭 끝까지 꽉 차고(dBFS 값은 헤더로 이동), 긴 장치명은 "MacBook P…"처럼 폭에 맞춰 깔끔히 생략, 카드가 INPUT 패널 안에서 좌우 가운데로 정렬
- **떠 있는 분석 창** — SPL 미터·알람은 본창(전체화면 포함) **위에 떠서** 함께 보이고, 벡터스코프·라우드니스 레이더 팝아웃은 본창을 최소화해도 독립으로 떠 있음 (Smaart 방식)
- **단축키 S = 시작/정지** — Spectrum·Stereo Loudness 탭에서 **S** 키로 측정 Start/Stop 토글 (버튼에 (S) 표기, 텍스트 입력 중엔 정상 타이핑)
- **TF 분석창 클릭 하이라이트** — Transfer Function 탭의 3개 분석창(Live IR·Phase·Magnitude+Coherence) 중 **클릭한 창의 외곽이 액센트 테두리로 강조** (어느 창을 보고 있는지 한눈에)
- **탭 별도 창 분리 (멀티모니터)** — Spectrum·Transfer Function·Stereo Loudness 각 탭을 **독립 창으로 떼어내** 다른 모니터에 배치. 공유 측정 엔진이라 분리해도 측정이 끊기지 않고, 창 위치·크기를 기억 (View 메뉴 또는 툴바 팝아웃 버튼 / 단축키 **Cmd+Shift+S·T·L**)
- **한 창에 동시 보기 (2칸 분할)** — 메인 화면을 세로 2칸으로 나눠 각 칸에 원하는 탭(Spectrum / Transfer Function / Stereo)을 골라 **동시에 측정·표시** (View 메뉴 → 동시 보기 / **Cmd+Shift+2**)
- **TF 자유 화면 (Smaart 스타일)** — Transfer Function 탭의 각 분석 칸 제목의 **▾**를 눌러 그 칸에 무엇을 띄울지 직접 선택: **Magnitude · Phase · Live IR · RTA · Off**. 보고 싶은 것만 골라 칸을 구성 (고른 개수만큼 칸이 늘고 줄어듦)
- **TF RTA 칸 (신규)** — 측정 마이크의 실시간 옥타브 스펙트럼(**RTA**)을 TF 탭 안에서 바로 확인. 측정 시작·신호 발생기 없이 칸만 켜면 마이크 입력이 즉시 표시 (스펙트럼 탭의 속도·평균·피크홀드·캘리브 설정을 그대로 따름)
- **TF 플롯 표시/숨김 토글** — TF 툴바의 **Show [IR][Phase][Mag]**로 보고 싶은 분석창만 남기기 (분할·작은 창에서 한 플롯을 꽉 채워 보기)

**버그 픽스**
- 측정을 멈춘(Stop) 뒤 그래프 시작 안내 화면이 다시 나타나지 않던 문제 수정
- 소스 이름 변경 시 입력칸(파란 테두리)이 카드 밖으로 삐져나오던 문제 수정
- SPL Meter 카드 안 리셋 아이콘이 찌그러져 보이던 문제 수정 (둥근 화살표로 또렷하게, 타이틀바 리셋과 동일 모양으로 통일)
- 라이트 모드에서 캡쳐 패널 일부(버튼·체크박스·구분선)가 검게 남던 문제 수정
- 스펙트럼 입력 소스 카드를 꺼도 그 곡선이 계속 표시되던 문제 수정
- 캡쳐를 모두 숨기면 라이브 곡선이 흐리게 남던 문제 수정 (숨긴 캡쳐가 포커스여도 라이브 선명)
- 첫 캡쳐 시 잠깐 느려지던 문제 수정 (색 팔레트 계산 최적화)
- 캡쳐 패널에서 선택한 캡쳐가 표시되도록 (클릭한 캡쳐에 활성 강조)
- 일부 창(TF Average·라이선스)에 제목 막대가 두 개로 겹쳐 보이던 문제 수정
- 탭(Spectrum·TF·Stereo)을 클릭하면 파란 포커스 사각형이 이중 네모로 겹쳐 보이던 문제 수정
- 벡터스코프·라우드니스 레이더 팝아웃 창의 흰색 타이틀바를 다크 브랜드 타이틀바로 통일
- 전체화면에서 큰 소리 입력 시 화면이 버벅이던 문제 개선
- TF 탭 **RTA 칸**이 실시간 입력에서 버벅이던 문제 수정 (스펙트럼 탭과 동일한 렌더 방식으로 일정한 프레임 유지)
- TF 탭 **RTA 칸**을 오래 켜두면 다른 오디오 장치(아이폰·헤드폰 등) 연결 시 화면이 멈추던 문제 수정 (장치 변경 후 마이크 입력을 자동으로 다시 연결)
- 본창이 전체화면일 때 SPL 미터·알람이 전체화면 크기로 뜨던 문제 수정 (정상 크기로 본창 위에)
- SPL 미터·알람 창을 외부 모니터로 옮길 수 있도록 수정 (전체화면 위 플로팅과 양립 — 창모드에선 자유 이동, 전체화면 화면 밖으로 끌면 자동 분리)
- 벡터스코프와 라우드니스 레이더의 원 크기가 서로 다르던 것 동일하게 통일
- 라우드니스 타겟을 바꿔도 측정 시작 전에는 화면(레이더 링)에 반영되지 않던 문제 수정 (즉시 반영)
- TF 신호 제너레이터의 **출력 채널**을 재생 중에 바꿔도 바로 반영되도록 수정 (이전엔 Stop→Play 해야 적용되던 것)
- 측정하지 않는 동안 USB 오디오 인터페이스를 뽑았다 다시 꽂으면 장치 목록에 안 나타나던 문제 수정 (시스템 장치 변경을 감지해 자동 갱신)
- 측정 중에 USB 인터페이스가 빠지면 측정을 안전하게 정지하고, 다시 꽂으면 자동으로 복구되도록 개선 (일부 인터페이스가 분리 후에도 무음 신호를 계속 보내 끊김을 놓치던 문제 해결)

## v1.5 — 다듬어진 첫인상  *(2026.06)*
설치부터 실행까지, 모든 접점에 SPECTRA를 입혔습니다.

- **브랜드 About 화면** — 버전·라이선스·머신 ID를 한눈에 (우측 아래 License)
- **앱 이름 SPECTRA로 통일** — 메뉴 막대·Finder에서 SPECTRA로 표시
- **시작 안내 화면** — 측정 전 그래프에 은은한 가이드
- **브랜드 설치 화면(DMG)** — "Applications로 드래그" 한 번에 설치
- 코드 서명·노타라이즈 준비 (배포 신뢰성 강화)

## v1.4 — 선이 또렷해지다  *(2026.06)*
아이콘 하나까지 다시 그렸습니다.

- 전 UI 아이콘을 **정교한 라인 아이콘**으로 교체
- 우측 패널(정보·입력)·팝아웃 아이콘 리프레시
- **3개 탭 드롭다운 스타일 완전 통일**

## v1.3 — 빛 속에서도 선명하게  *(2026.06)*
라이트 테마를 처음부터 다시 설계했습니다.

- **라이트 테마 'Crisp White'** — 흰 패널이 떠 보이는 레이어드 디자인
- 벡터스코프·라우드니스 레이더 **본격 라이트 전환** (보석톤 컬러)
- 라이트 모드 곳곳의 가독성·색 일관성 정리

## v1.2 — 한 끗의 차이  *(2026.06)*
- 레벨 미터 디자인 통일 (Spectrum ↔ Transfer Function)
- 재생·정지 버튼과 아이콘을 브랜드 톤으로 정리
- 팝아웃(새 창) 아이콘 개선

## v1.1 — 측정이 편해지다  *(2026.06)*
- **SPL Meter 자유 배치** — 행×열 그리드로 원하는 지표만 크게
- 마이크 캘리브레이션 **채널별 관리** (다채널 인터페이스 대응)
- 라우드니스 팝아웃 전체화면 정렬 개선

---

*See your sound.*
