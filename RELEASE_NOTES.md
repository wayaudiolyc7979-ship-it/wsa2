# SPECTRA — 릴리스 노트

> **See your sound.** · Spectrum Analyzer · by WAYAUDIO

브랜드 버전(HTML)은 [`RELEASE_NOTES.html`](RELEASE_NOTES.html), 베타 시절 개발 로그는 [`docs/beta_dev_log.md`](docs/beta_dev_log.md) 참고.

---

## v1.8 — SPL 미터 재디자인 · TF 측정 개선 · 안정성  *(최신, 2026.06)*
SPL 미터·TF 화면을 새로 다듬고, 측정 정확도와 장시간 안정성을 손봤습니다.

### ✨ 새 기능 · 개선
- **SPL 미터 디자인 정비** — SPL 미터 창을 한층 또렷하고 차분하게: 각 카드 아래쪽에 현재 레벨에 따른 **신호등 색 번짐**(안전=초록 / 주의=노랑 / 위험=빨강)을 더해 숫자를 읽지 않아도 상태가 한눈에 들어오고, 값 색을 LEVEL 패널과 같은 톤으로 통일(A가중 **소프트 블루** / C가중 **소프트 와인** / 광대역 **흰색** — 진한 파랑·보라를 덜어 눈이 편하게, 경고·피크 색은 유지). **검정 배경·회색 카드 → 회색 배경·검정 카드로 반전**해 다른 화면과 통일감을 주고(검정 카드 위라 글로우가 더 선명), 시계 카드는 **소프트 화이트**로 차분하게 정리. **Max 리셋을 카드별로** — 헷갈리던 타이틀바의 전역 'Reset Max' 버튼을 없애고 **각 카드에 자체 Max 리셋 버튼**을 두어 그 카드만 초기화. 리셋 버튼은 마우스를 올리면 **배경칠 없이 아이콘 색만** 또렷해지도록 다듬음. SPL 미터·알람 창의 **타이틀바 아이콘도 정제** — 상단고정 토글은 자물쇠 → **압정(핀)**, 설정은 슬라이더 → **기어**로 더 세련되고 의미가 분명하게
- **Transfer Function 3그래프 카드로 분리** — Live IR · Phase · Magnitude+Coherence 세 그래프를 **각각 떠 있는 둥근 카드**로 나눠 한눈에 구분되게 정리. 그래프마다 고유 색 제목(IR=청록 / Phase=보라 / Magnitude=파랑)으로 지금 보는 분석이 무엇인지 바로 보임. 클릭해 선택한 그래프는 상단에 **SPECTRA 시그니처 그라디언트 엣지**로 표시(기존 파란 사각 테두리 대체). 카드 사이 경계에도 같은 그라디언트를 아주 옅게 더해 브랜드 톤 통일
- **Transfer Function 화면 정리** — Magnitude+Coherence 그래프의 코히런스 기준선(점선)과 우측 % 눈금을 없애 화면을 깔끔하게 정리. 정확한 코히런스 값은 커서를 올리면 리드아웃에 %로 그대로 표시됨. 코히런스 표시도 **주황 선 → 블루 반투명 채움 밴드**로 바꿔(신뢰도 '영역'을 면으로), 초록 응답 곡선과 부딪히지 않게 정리
- **Transfer Function 측정 시작 시 자동 Y축 맞춤** — 측정(핑크/스윕)을 시작하면 Magnitude 그래프가 더블클릭 없이 **첫 데이터에서 한 번 자동으로 화면에 맞춰** 들어오도록 개선(측정 중 점프는 없음)
- **Transfer Function 곡선 하강 속도 2배** — 측정 곡선이 신호가 줄어들 때 **천천히 내려오던 것을 약 2배 빠르게**(fast release) 개선. 레벨이 떨어지는 구간만 빠르게 따라가고, 올라갈 때·안정 구간은 기존 평균 그대로라 안정성·정확도는 유지(Adaptive·Single 엔진 공통)
- **사이드바 섹션 헤더 정리** — LEVEL · INFO · INPUT 앞에 제각각이던 아이콘을 **단색 액센트 세로 바**로 통일해 더 깔끔하고 일관되게
- **LEVEL/INFO 값 색상 정돈** — 값 색을 정리: 광대역(SPL·Peak·Dominant)과 INFO 값은 **흰색**으로 또렷하게, A가중(dBA·LAeq)은 **소프트 블루**·C가중(dBC·LCeq)은 **소프트 와인**으로 차분하게 코딩. 채도 낮춰 눈이 편하고 위계가 살아남
- **LEVEL 헤더 버튼 차분하게** — SPL 미터(dB)·SPL 알람(신호등) 버튼을 평소엔 **은은한 회색**으로 두고, 마우스를 올리면 **소프트 블루로 살아나게** 정리. 차분해진 값 영역과 톤을 맞춤
- **SPL 미터 버튼 아이콘 변경** — LEVEL 패널의 SPL 미터 열기 버튼 아이콘을 "새 창 열기" 화살표에서 **"dB"** 표시로 바꿔, 무슨 기능인지 한눈에 알 수 있게 함

### 🐛 버그 픽스
- **라우드니스 장시간 구동 멈춤 수정** — Stereo Loudness를 오래(수 시간) 켜두면 화면이 점점 느려지다 멈추던 문제 해결. 적분(Integrated)·LRA 계산을 오디오 청크마다(초당 약 90회)가 아니라 새 측정 블록이 들어올 때(초당 10회)만 수행하도록 정리 — 경과 시간이 길어져도 부하가 일정하게 유지됨. 측정값은 동일
- **측정 미터·분석 간헐 멈춤 수정** — 같은 오디오 인터페이스로 신호를 내보내며 동시에 입력을 측정할 때(특히 스윕 측정 후 핑크/연속 신호로 전환), 간혹 **레퍼런스·측정 미터가 안 뜨고 Transfer Function 그래프가 빈 채로 멈추던** 문제 해결. 입력 스트림이 열렸는데도 오디오 콜백이 시작되지 않는 macOS CoreAudio 경합을 감지해 **자동으로 다시 열도록**(약 2초 내 복구) 보강
- **스윕 측정 후 위상 깨짐 수정 (자동 0ms 정렬)** — 정밀(Sweep) 측정은 왕복 지연을 이미 제거하는데, 핑크에서 넣어둔 딜레이가 스윕 위상에 잘못 더해져 **위상이 깨지던** 문제 해결. 측정이 끝나면 **카드 딜레이가 자동으로 0ms로 맞춰져** IR 임펄스가 0ms 중앙에 오고 위상도 정상 표시
- **스윕 ↔ 핑크 레벨 불일치 수정** — 정밀(Sweep) 측정의 Magnitude가 라이브(핑크)보다 약 33dB 낮게 떠 **두 곡선을 겹쳐 비교할 수 없던 문제** 해결. 스윕은 송출 디지털 신호 기준, 핑크는 캡쳐된 레퍼런스 채널 기준이라 0dB 기준이 달랐던 것을, 스윕을 **레퍼런스 채널 레벨로 정규화**해 핑크와 같은 절대 레벨로 표시 → 핑크 캡쳐와 스윕 캡쳐를 같은 높이에서 직접 비교 가능
- **신호 제너레이터 기본 소스 정리** — 앱 시작 시 신호 소스가 Pink·Sweep **두 개가 동시에 켜져 보이던 문제** 수정. 항상 하나만 선택되며 기본은 **Pink**(Sweep은 누를 때 켜지는 1회 측정 모드)
- **Start/Stop 버튼 파란 테두리 깜빡임 수정** — 측정 카드·제너레이터·메인의 Start/Stop 버튼을 누르면 간헐적으로 생기던 macOS 파란 포커스 링 제거(전 트랜스포트 버튼 통일)
- **라우드니스 COMPLIANCE 배지 매끄럽게** — Stereo Loudness의 COMPLIANCE 상태 원형 배지 테두리가 들쭉날쭉(계단현상)하던 문제 해결. 원을 안티앨리어싱으로 직접 그리도록 바꿔 어느 크기에서도 매끈한 원으로 표시

---

## v1.7 — 적응형 엔진 · 프리셋 · 다국어  *(2026.06)*
라이브에 강한 새 TF 엔진, 세 탭 설정을 기억하는 프리셋, 한/영 언어 전환, 라우드니스 화면 재정비.

- **TF Adaptive 엔진 (신규·기본)** — Transfer Function에 **멀티레이트(적응 해상도)** 라이브 엔진 추가. 저역은 고해상도, 고역은 빠른 응답으로 한 화면에 → 현장 튜닝(이동·EQ 조정 중)에 강함. 툴바 **Engine** 셀렉터로 **Single ↔ Adaptive** 전환(기본 Adaptive). 딜레이 파인더·IR·위상 모두 Single과 동일하게 동작
- **정밀 스윕(Farina ESS)** — **Sweep** 버튼으로 지수 스윕 1-shot 측정 → 선형 무왜곡 TF + **THD/SNR** 표시 (정밀·THD는 스윕, 라이브 현장은 Adaptive — REW/Smaart식 워크플로)
- **응답 속도(Response) 컨트롤** — 기존 **Avg → Response**로 재설계. **Fast · Quick · Normal · Smooth · Stable** 한 단어로 직관 선택(기본 Normal=16s). 바꾸면 **즉시** 체감(누적 유지 — 재수렴/쓸어내림 없음), 곡선은 **30fps 보간**으로 끊김 없이 부드럽게
- **스펙트럼 표시 자연스럽게 — 빠른 상승·느린 하강** — 스펙트럼 막대/곡선이 소리에 **빠르게 반응해 올라오고**, 잔향처럼 **천천히 부드럽게 내려오도록** 응답(ballistic)을 피크미터식으로 재설계. 내려오는 속도는 **Response**로 조절(Fastest 약 0.4초 … Slowest 약 2.5초), 올라오는 부드러움도 다듬어 막대가 톱니처럼 튀지 않게. 멀티소스(추가 카드)도 동일 응답으로 일치. 기존 **Avg(평균) 컨트롤은 제거** — 새 방식의 느린 하강이 평균 안정화를 대체하고, 응답 조절은 **Response 하나로 일원화**
- **설정 자동 기억** — 세 탭(Spectrum·TF·Stereo)의 모든 컨트롤 값을 자동 저장 → 앱을 다시 열면 **지난 세션 그대로 복원** (장치·채널 포함, 없으면 현재 유지)
- **이름 프리셋 (신규)** — 현재 세 탭 설정을 **이름 붙여 저장**하고 드롭다운에서 한 번에 불러오기/삭제. 상단 헤더의 **Preset** 바(저장·삭제 아이콘). "클럽 튜닝", "방송 믹스" 식으로 현장별 세팅을 즉시 전환
- **한국어/영어 언어 전환 (신규)** — 상단 헤더 **지구본 버튼**으로 KO ↔ EN 전환(설정 저장 후 자동 재시작). 첫 실행은 **OS 언어**를 따름. 한글 모드에선 **메인 화면·도구창은 영어**(친숙·간결), **마우스 hover 툴팁·설정 다이얼로그·우클릭 메뉴는 한국어**(초보자 친화 쉬운 말)로 표시
- **라우드니스 화면 재정비** — PROGRAM LOUDNESS 거대 숫자를 **AVG(누적 평균=납품 기준) | LIVE(실시간 Short-term) 2분할 동시 표기**로(토글 없이 한눈에 — 가운데 구분선, COMPLIANCE 판정은 AVG 기준), **레이더 외곽 레벨미터를 타겟 중심으로 재배치**(설정한 타겟 LUFS가 항상 12시 상단 중앙 — 눈금·채움 막대·피크홀드가 타겟 따라 함께 이동. 타겟 -16이면 -16이 가운데), 히스토리 그래프를 **부드러운 곡선 + 레이더와 같은 60초** 윈도우로, **벡터스코프·레이더 확대**(창 키우면 더 커짐), 창을 최소~최대로 조절해도 안 잘리게 레이아웃 정비
- **창 최대화로 꽉 채우기** — 헤더 **더블클릭 = 최대화**(메뉴바 아래로 화면 가득). 기존 전체화면이 시스템 메뉴바에 헤더가 가려지던 문제 해소 — 메뉴는 항상 접근 가능 (Show Mode는 전체화면 유지)
- **탭 별도 창(팝아웃) 정비** — 세 탭(Spectrum·TF·Stereo)을 별도 창으로 띄울 때: 흰 네이티브 타이틀바 → **다크 + 브랜드 헤더**(그라디언트 마크 + SPECTRA + 탭이름, 가운데 정렬)로 통일하고 OS 기본 제목 텍스트는 숨김, **타이틀바 우측 ⌃ 버튼으로 툴바(메뉴) 접기/펴기**, **헤더 더블클릭 = 최대화↔복원**. 팝아웃 툴바 버튼이 검은 박스로 보이던 것과 TF 별도 창 타이틀바가 라이트 모드에서 검게 남던 문제도 수정
- **마이크 캘리브레이션 정리** — 캘리브레이션 창 안내·버튼을 쉬운 영어로(한글 모드에선 한국어)
- **라이트 모드 전체 정비** — 라이트 모드에서 일부만 밝던 부분까지 모두 테마 반영: CAPTURES 패널·SPL 미터·각종 다이얼로그·스크롤바·팝아웃 버튼·타이틀바·헤더 버튼(언어/저장/삭제)·Stereo 카드(PROGRAM LOUDNESS·지표·COMPLIANCE). 다크 모드는 그대로 유지
- **입력·UI 다듬기** — 캡처/프리셋 이름 입력칸에서 영문 글자(L·G·S)가 단축키로 잘못 먹던 문제 수정, Stereo 툴바의 미사용 토글 버튼 제거
- **측정 카드 입력 레벨미터 (게인 셋업)** — TF 측정 카드의 **Start를 누르면 그 카드 입력 레벨미터가 켜지고** Stop으로 끔(제너레이터 재생 여부와 무관). 측정 시작 전 콘솔·인터페이스 게인을 미리 맞추기 좋게
- **입력 레벨미터 피크 표시** — Spectrum 입력 레벨미터를 **RMS 채움 + 실제 피크 틱**으로(피크 헤드룸이 한눈에, Smaart식) → 게인 셋업 시 클리핑 여유 확인에 유용
- **추가 측정 카드 커서 정보 일원화** — 측정 카드를 여러 개 띄웠을 때 2번 이후 카드의 커서 리드아웃도 1번 카드처럼 **주파수·dB·위상·코히런스**를 모두 표시(이전엔 일부 빠졌음)
- **무끊김 출력 채널 변경** — 제너레이터 재생 중 출력 채널을 바꿔도 **끊김 없이 즉시** 새 채널로 전환(스트림 재시작 없음)
- **안정성 다듬기** — Adaptive 엔진에서 측정 카드 여러 개가 모두 그려지도록 수정, 입력 채널을 바꿀 때 장치가 끊긴 것으로 오인하던 문제 해결, 제너레이터를 켜고 끌 때 카드 레벨미터가 멈추던 문제 수정

---

## v1.6.1 — 캡쳐를 한 손에  *(직전)*
쌓인 캡쳐를 한 번에 다루도록. (+ 라이브 튜닝 디테일·브랜드 다듬기)

- **딜레이 거리(m) 표기** — TF 툴바 **Units** 토글(ms / ms·m / m)로 딜레이를 거리(미터)로도 표시. IR 마커·커서·시간축과 측정 카드 딜레이 칸에 함께 적용 (음속 343 m/s, Delay Finder 고급설정에서 변경 가능)
- **RTA 막대 입체감** — 옥타브(RTA) 막대를 평면 단색 → 세로 그라디언트(위 밝게/아래 어둡게)로 다듬어 더 또렷하게 (라이브·캡쳐·추가소스 일관)
- **시작 화면 갱신** — 항상 현재 버전이 표시되는 브랜드 스플래시
- **캡쳐 일괄 표시/숨김** — 캡쳐 패널 머리말의 그라디언트 웨이브 버튼으로 현재 탭 전체를 한 번에 켜고/끄기 (스마트 토글)
- **그룹별 일괄 토글** — 각 그룹 머리말에서 그 그룹만 한 번에 표시/숨김
- **캡쳐 색 구분 강화** — 캡쳐마다 라이브 입력 색과도, 다른 캡쳐와도 뚜렷이 구별되는 색을 자동 배치 (캡쳐가 많아져도 한눈에 구분)
- **캡쳐 위치 자유 지정** — 그룹 이름을 누르면 새 캡쳐가 그 그룹으로, 다시 누르면 미지정으로. 패널에 "캡쳐 위치"가 표시돼 한눈에 (접기/펼치기는 화살표로 분리)
- **딜레이 파인더 단축키** — TF 딜레이 찾기 창에서 **L**로 다시 찾고, **Enter**로 바로 적용
- **캡쳐 전체 삭제** — 캡쳐 패널에서 우클릭 → **Delete All** (확인 후 모두 삭제, Spectrum·TF 동일)
- **리캡쳐(제자리 다시 캡쳐)** — 기존 캡쳐의 색·이름·그룹은 그대로 두고 곡선만 현재 측정값으로 갱신. 캡쳐 패널 우클릭 → **Recapture** 또는 단축키 **R** (Space=새 캡쳐). EQ·게인을 만진 뒤 같은 트레이스를 지우고 다시 뜰 필요 없이 한 번에 업데이트 (Spectrum·TF 동일)
- **알림창 브랜딩** — 모든 팝업을 SPECTRA 톤으로 (다크 + 그라디언트 라인 + 상황별 아이콘 + 브랜드 버튼). 삭제 같은 위험 동작은 빨강으로 명확히
- **스펙트럼 브랜드 컬러** — FFT 곡선을 SPECTRA 시그니처 그라디언트(저역→고역)로 표시 + 곡선이 더 매끄러워짐
- **측정 레벨 정확도 향상** — 스펙트럼/옥타브 밴드 레벨을 표준 파워 정규화(윈도우 ENBW 보정)로 재계산. 순음과 광대역(핑크노이즈)이 같은 기준으로 읽혀, 캘리브레이션 한 번으로 절대 SPL이 정확하게 맞고 업계 표준 측정기와 레벨이 일치
- **릴리즈 노트 메뉴** — Help 메뉴 → "릴리즈 노트"에서 버전별 변경사항을 앱 안에서 바로 확인
- **LEQ 시간 미터** — LAeq·LCeq 카드 안에 SPECTRA 그라디언트 진행 바로 LEQ 적분 시간을 표시 + 리셋 버튼 (창은 컴팩트 크기로 열림)
- **SPL 미터 리사이즈 개선** — 카드가 창을 꽉 채우고, 창을 넓히면 숫자가 큼직하게 커지며 세로 가운데 정렬 (Smaart 스타일)
- **SPL 미터 시계 카드** — 설정에서 칸에 "Clock"을 골라 24시간 시계(HH:MM)를 큼직하게 표시 (다른 지표처럼 행×열 어디든 배치)
- **SPL 미터 지표 대폭 확장 (Smaart 스타일)** — SPL A/C Slow·SPL Slow(Z) + SPL A/C Fast·SPL Fast(Z) + Peak·Peak C·FS Peak 까지 칸별로 자유 선택. Fast(125ms)/Slow(1s) 시정수, 풀스케일 디지털 피크(dBFS) 포함
- **SPL 미터 측정 소스 선택** — SPL 미터가 어느 입력을 읽을지 설정창(⚙)의 **"측정 소스"** 드롭다운에서 선택 (Primary + 추가 입력 카드 중). 멀티 인터페이스에서 원하는 채널만 SPL로 측정 (카드 삭제 시 Primary로 자동 복귀)
- **SPL 임계 알람 창 (신규)** — 측정 레벨이 설정한 한계에 다가가면 **초록→노랑→빨강** 신호등으로 즉시 경고하는 독립 창. 큰 숫자 + 남은 여유/초과 dB + 상태(OK·AMBER·OVER), 초과 시 부드러운 깜빡임. **마스터 지표**(dB LAeq·LCeq·SPL A/C 등)·**한계**·**노랑 여유**·**LEQ 시간**을 ⚙에서 설정 (LEQ 지표는 `dB LAeq 10min`처럼 적분 시간 함께 표기). SPL 미터를 열지 않아도 **독립 동작**(자체 측정 엔진). **View 메뉴** 또는 **LEVEL 패널의 신호등 버튼**으로 열기 — 공연장 소음 한계 모니터링에 바로 활용
- **창 항상 위 고정 (자물쇠 토글)** — SPL 미터·알람 창 타이틀바의 **자물쇠 버튼**으로 본창 위에 항상 고정/해제 (잠금=파랑). 다른 작업 위에 띄워두고 모니터링 (켜고 끌 때 깜빡임 없음)
- **마우스 떠나면 카드만** — SPL 미터·알람 창에서 마우스가 벗어나면 타이틀바가 부드럽게 접혀 **숫자·신호등만 깔끔하게** 남고, 마우스를 올리면 다시 나타남 (FOH에서 멀리서 글랜스용)
- **커서에 음이름 표시** — 스펙트럼(FFT·옥타브·스펙트로그램)에서 커서를 올리면 주파수 옆에 **음이름**(예: `247 Hz · B3`)을 함께 표시. 룸모드·피드백·악기 주파수를 바로 식별
- **단축키 치트시트** — **?** 키(또는 Help → Keyboard Shortcuts)로 모든 단축키를 키캡 스타일로 한 화면에 (S·Space·R·G·L·화살표 등 발견성↑)
- **진짜 True Peak (dBTP)** — 라우드니스의 트루피크를 **4× 오버샘플(ITU-R BS.1770)**로 측정 → 샘플 사이 피크(inter-sample)까지 잡아냄. 기존 샘플 피크는 놓치던 클리핑 위험을 정확히 표시 (−1 dBTP 초과 시 빨강 경고)
- **라우드니스 타겟 프리셋 + 편차** — 타겟을 **EBU R128(−23) · ATSC A/85(−24) · 스트리밍(−14) · Apple Music(−16) · −18 LUFS** 중 선택. **Δ Target**(통합 라우드니스의 타겟 대비 ±LU)을 색으로 표시(±1 안 초록). **선택 즉시** 레이더 타겟 링 반영(측정 시작 안 해도)
- **LU 상대 표시 토글** — **LU** 버튼으로 M·S·I를 LUFS(절대) ↔ LU(타겟 기준 상대)로 전환 (방송 EBU 모드 방식)
- **신호 제너레이터에 사인파(Sine) 추가** — Pink·White·Sweep에 더해 **Sine** 선택 → 주파수 설정(10~24000Hz)으로 순음 출력. 단일 주파수 측정·캘리브레이션 확인에 바로 사용 (버튼에 설정 주파수 표시, 레벨은 기존 Level 컨트롤)
- **카드 삭제 버튼 통일** — Spectrum 소스 카드·TF 측정 카드·캡쳐 그룹의 삭제 버튼을 모두 동일한 **✕**로 통일 (평소 회색, 마우스 올리면 빨강)
- **입력 소스 카드 레이아웃 다듬기** — 레벨 미터가 카드 폭 끝까지 꽉 차고(dBFS 값은 헤더로 이동), 긴 장치명은 "MacBook P…"처럼 폭에 맞춰 깔끔히 생략, 카드가 INPUT 패널 안에서 좌우 가운데로 정렬
- **떠 있는 분석 창** — SPL 미터·알람은 본창(전체화면 포함) **위에 떠서** 함께 보이고, 벡터스코프·라우드니스 레이더 팝아웃은 본창을 최소화해도 독립으로 떠 있음 (Smaart 방식)
- **단축키 S = 시작/정지** — Spectrum·Stereo Loudness 탭에서 **S** 키로 측정 Start/Stop 토글 (버튼에 (S) 표기, 텍스트 입력 중엔 정상 타이핑)
- **TF 분석창 클릭 하이라이트** — Transfer Function 탭의 3개 분석창(Live IR·Phase·Magnitude+Coherence) 중 **클릭한 창의 외곽이 액센트 테두리로 강조** (어느 창을 보고 있는지 한눈에)
- **탭 별도 창 분리 (멀티모니터)** — Spectrum·Transfer Function·Stereo Loudness 각 탭을 **독립 창으로 떼어내** 다른 모니터에 배치. 공유 측정 엔진이라 분리해도 측정이 끊기지 않고, 창 위치·크기를 기억 (View 메뉴 또는 툴바 팝아웃 버튼 / 단축키 **Cmd+Shift+S·T·L**)
- **한 창에 동시 보기 (2칸 분할)** — 메인 화면을 세로 2칸으로 나눠 각 칸에 원하는 탭(Spectrum / Transfer Function / Stereo)을 골라 **동시에 측정·표시** (View 메뉴 → 동시 보기 / **Cmd+Shift+2**)
- **TF 자유 화면 (Smaart 스타일)** — Transfer Function 탭의 각 분석 칸 제목의 **▾**를 눌러 그 칸에 무엇을 띄울지 직접 선택: **Magnitude · Phase · Live IR · RTA · Off**. 보고 싶은 것만 골라 칸을 구성 (고른 개수만큼 칸이 늘고 줄어듦)
- **TF RTA 칸 (신규)** — 측정 마이크의 실시간 옥타브 스펙트럼(**RTA**)을 TF 탭 안에서 바로 확인. 측정 시작·신호 발생기 없이 칸만 켜면 마이크 입력이 즉시 표시 (스펙트럼 탭의 속도·평균·피크홀드·캘리브 설정을 그대로 따름)
- **TF 플롯 표시/숨김 토글** — TF 툴바의 **Show [IR][Phase][Mag]**로 보고 싶은 분석창만 남기기 (분할·작은 창에서 한 플롯을 꽉 채워 보기)

**버그 픽스**
- 측정을 멈춘(Stop) 뒤 그래프 시작 안내 화면이 다시 나타나지 않던 문제 수정
- 소스 이름 변경 시 입력칸(파란 테두리)이 카드 밖으로 삐져나오던 문제 수정
- SPL Meter 카드 안 리셋 아이콘이 찌그러져 보이던 문제 수정 (둥근 화살표로 또렷하게, 타이틀바 리셋과 동일 모양으로 통일)
- 라이트 모드에서 캡쳐 패널 일부(버튼·체크박스·구분선)가 검게 남던 문제 수정
- 스펙트럼 입력 소스 카드를 꺼도 그 곡선이 계속 표시되던 문제 수정
- 캡쳐를 모두 숨기면 라이브 곡선이 흐리게 남던 문제 수정 (숨긴 캡쳐가 포커스여도 라이브 선명)
- 첫 캡쳐 시 잠깐 느려지던 문제 수정 (색 팔레트 계산 최적화)
- 캡쳐 패널에서 선택한 캡쳐가 표시되도록 (클릭한 캡쳐에 활성 강조)
- 일부 창(TF Average·라이선스)에 제목 막대가 두 개로 겹쳐 보이던 문제 수정
- 탭(Spectrum·TF·Stereo)을 클릭하면 파란 포커스 사각형이 이중 네모로 겹쳐 보이던 문제 수정
- 벡터스코프·라우드니스 레이더 팝아웃 창의 흰색 타이틀바를 다크 브랜드 타이틀바로 통일
- 전체화면에서 큰 소리 입력 시 화면이 버벅이던 문제 개선
- TF 탭 **RTA 칸**이 실시간 입력에서 버벅이던 문제 수정 (스펙트럼 탭과 동일한 렌더 방식으로 일정한 프레임 유지)
- TF 탭 **RTA 칸**을 오래 켜두면 다른 오디오 장치(아이폰·헤드폰 등) 연결 시 화면이 멈추던 문제 수정 (장치 변경 후 마이크 입력을 자동으로 다시 연결)
- 본창이 전체화면일 때 SPL 미터·알람이 전체화면 크기로 뜨던 문제 수정 (정상 크기로 본창 위에)
- SPL 미터·알람 창을 외부 모니터로 옮길 수 있도록 수정 (전체화면 위 플로팅과 양립 — 창모드에선 자유 이동, 전체화면 화면 밖으로 끌면 자동 분리)
- 벡터스코프와 라우드니스 레이더의 원 크기가 서로 다르던 것 동일하게 통일
- 라우드니스 타겟을 바꿔도 측정 시작 전에는 화면(레이더 링)에 반영되지 않던 문제 수정 (즉시 반영)
- TF 신호 제너레이터의 **출력 채널**을 재생 중에 바꿔도 바로 반영되도록 수정 (이전엔 Stop→Play 해야 적용되던 것)
- 측정하지 않는 동안 USB 오디오 인터페이스를 뽑았다 다시 꽂으면 장치 목록에 안 나타나던 문제 수정 (시스템 장치 변경을 감지해 자동 갱신)
- 측정 중에 USB 인터페이스가 빠지면 측정을 안전하게 정지하고, 다시 꽂으면 자동으로 복구되도록 개선 (일부 인터페이스가 분리 후에도 무음 신호를 계속 보내 끊김을 놓치던 문제 해결)

## v1.5 — 다듬어진 첫인상  *(2026.06)*
설치부터 실행까지, 모든 접점에 SPECTRA를 입혔습니다.

- **브랜드 About 화면** — 버전·라이선스·머신 ID를 한눈에 (우측 아래 License)
- **앱 이름 SPECTRA로 통일** — 메뉴 막대·Finder에서 SPECTRA로 표시
- **시작 안내 화면** — 측정 전 그래프에 은은한 가이드
- **브랜드 설치 화면(DMG)** — "Applications로 드래그" 한 번에 설치
- 코드 서명·노타라이즈 준비 (배포 신뢰성 강화)

## v1.4 — 선이 또렷해지다  *(2026.06)*
아이콘 하나까지 다시 그렸습니다.

- 전 UI 아이콘을 **정교한 라인 아이콘**으로 교체
- 우측 패널(정보·입력)·팝아웃 아이콘 리프레시
- **3개 탭 드롭다운 스타일 완전 통일**

## v1.3 — 빛 속에서도 선명하게  *(2026.06)*
라이트 테마를 처음부터 다시 설계했습니다.

- **라이트 테마 'Crisp White'** — 흰 패널이 떠 보이는 레이어드 디자인
- 벡터스코프·라우드니스 레이더 **본격 라이트 전환** (보석톤 컬러)
- 라이트 모드 곳곳의 가독성·색 일관성 정리

## v1.2 — 한 끗의 차이  *(2026.06)*
- 레벨 미터 디자인 통일 (Spectrum ↔ Transfer Function)
- 재생·정지 버튼과 아이콘을 브랜드 톤으로 정리
- 팝아웃(새 창) 아이콘 개선

## v1.1 — 측정이 편해지다  *(2026.06)*
- **SPL Meter 자유 배치** — 행×열 그리드로 원하는 지표만 크게
- 마이크 캘리브레이션 **채널별 관리** (다채널 인터페이스 대응)
- 라우드니스 팝아웃 전체화면 정렬 개선

## v1.0 — SPECTRA, 정식 출발  *(2026.06)*
측정 도구에 이름과 얼굴을 갖췄습니다.

- **제품 브랜딩 'SPECTRA'** 확립 — 로고·색·타이포그래피
- 라이트 / 다크 테마
- **Transfer Function 측정 안정화** (노이즈 근본 해결)
- macOS **Apple Silicon · Intel** · Windows 지원

---

*See your sound.*
